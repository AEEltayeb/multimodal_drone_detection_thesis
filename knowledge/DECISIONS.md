# Decisions log

Every schema/organization change to the `knowledge/` system is recorded here, newest first.
Format: `YYYY-MM-DD — what changed — reason.`

- 2026-06-13 — **RULE: thesis proof artifacts must be RESIDENT in `ES_Drone_Thesis`.** Every artifact the thesis (or its audit / `runs/` / kb) cites as proof — generating scripts, results JSONs, CSVs, caches, weights — must physically live inside `ES_Drone_Thesis`. If a needed artifact exists only in the frozen old dir or elsewhere, **copy/move it in** (read from old, write into new; never write the archive — physical archiving is `/sweep` only). Raw shared datasets under `G:/drone/...` stay external (not repo artifacts); it is the *derived* proof that must be resident. The audit's `CITED_PATHS` residency block enforces this for cited paths and must be extended whenever a new artifact is cited. Reason: the thesis has to be reproducible and defensible from its own directory alone; a citation that resolves only in the archive is a dangling proof. Memory mirror: `project_thesis_artifact_residency.md`.
- 2026-06-11 — **Repo REORGANIZED for reader legibility (same day as the migration, applied on top of it).** (a) **`models/` = all weights by role**: `pretrained/` (yolo11n/26n), `rgb/` (16 Yolo26n_* run dirs, from `RGB model/`), `ir/` (corrective_finetune from `runs/` + IR_dsetV4-6 + IR_final_cleaned), `routers/` (former classifier/fusion_models + robust8*.joblib from eval/results/_routing_pipeline_cmp), `verifiers/` (`rgb_v5/` from eval/results/_v5_selcom_pure_1x8/classifiers + `ir_aligned/` from mri/results/.../classifiers), `patches/` (former classifier/runs/patches). Undo map: `models/_undo_reorg_moves.ps1`. (b) **Renames**: `RGB model/`→`training/` (+`dataset preparation`→`dataset_preparation`), `ir_gui/`→`gui/`, label reviewer surfaced `scripts/label_reviewer`→top-level `label_reviewer/` (entrypoint inside; sys.path fixed), root runners→`scripts/`. `eval/` and `classifier/` names FROZEN (46/34 files import them via sys.path). (c) **One-shot path rewrite** `_tools/reorg_2026_06.py`: 449+2 occurrences in 114 live files (py/ps1/yaml + gui settings); historical manifests/results untouched. (d) **kb re-point** `_tools/reorg_2026_06_kb.py`: 88 column updates via kb.py set; validate clean. (e) **Junctions as backup** at 5 old weight paths (runs/corrective_finetune, classifier/fusion_models, classifier/runs/patches, mri/.../classifiers, eval/.../classifiers) — removable via rmdir. (f) **Sweep** `_tools/reorg_2026_06_sweep.py` per green-lit `docs/analysis/2026-06-11_sweep_list.md`: **117 scripts archived** to `archive/2026-06-11/` (git mv; recorded rows set lifecycle=archived), **7 rescued** (convert_svanstrom_paired, extract_antiuav_crops, extract_background_crops, clean_patches_consensus → scripts/dataset_preparation/; check_gt_alignment, check_offset_per_seq → scripts/; render_example_images recorded in place). **`gui/flet_app/` KEPT** — pyside_app.py imports flet_app.settings_dialog (extraction = future refactor). eval/ loose .py 99→56, classifier/ 97→32. (g) `.gitignore`: ir_gui→gui rules, label_reviewer blanket ignore → outputs-only, training/*.yaml. README rewritten as the repo map. Verified after: tier-1 replay reproduces, thesis builds, 15 figures regenerate, kb validate clean. Reason: weights were scattered across 6 parents and 200 loose scripts buried the load-bearing code; a reader should open the repo and find GUI/label-reviewer/MRI/thesis/models immediately.
- 2026-06-11 — **Workspace MIGRATED to `ES_Drone_Thesis` (local clone + curate; old `ES_Drone_Detection` = frozen cold archive, never written).** Mechanics: local `git clone` (full history, HEAD `1dcbbbd`; **zero remotes** — GitHub deliberately unwired, old repo keeps the `drone_detection_thesis` remote) + robocopy of the uncommitted working state and all gitignored artifacts (knowledge/, thesis_eval/ incl. caches+results, eval/results 1.1 GB, classifier fusion_models+patches, mri/results, models/, `runs/corrective_finetune/finetune_v*`, RGB production run + per-run `best.pt` for all 15 comparison models, video-eval datasets confuser_videos + drone-video-tests v1/v2, demo_videos, .claude/, CLAUDE.md, root yolo*.pt). **Left behind:** archive/ (31.6 GB), runs/ bulk + corrective-finetune embedded datasets (~5.5 GB), classifier/runs minus patches (2.4 GB), label_reviewer/ session outputs (2.3M files; the TOOL `scripts/label_reviewer/` is tracked and came via clone), datasets/ training derivatives (negRGB/negTraffic/small_objects/goldv2), ir_gui/demo_outputs, junk. ~48 GB → ~6 GB. **Path fixes in NEW copy only:** `ir_gui/fusion_settings.json` (7 weight paths re-rooted) and `docs/generate_thesis_figures.py` (3 hardcoded `WORKSPACE/"ES_Drone_Detection"` refs → repo-relative `REPO`). **Verified in new dir:** thesis builds (`thesis_working.pdf` 2550 KB), Tier-1 replay reproduces from migrated caches (12 surfaces, ~60 s zero-GPU; svanström cascade sa32 P=0.9494 headline intact), all 15 figures regenerate from new-dir data, `kb.py validate` clean, production-stack weights all present. **Known cosmetic:** ~290 files show `M` in the new clone that don't in the old — pre-existing latent EOL diffs (LF working files vs CRLF blobs; old repo hides them via stat cache); content byte-identical, normalizes on next commit. Reason: 48 GB workspace crowded with dead bulk; the thesis needs a clean reproducible home with full history, with the old dir kept bit-identical as the recovery archive.
- 2026-06-10 — **Tier-2 RE-SCOPED to Svanström-only (cost math at the measured rate).** Tier-1 antiuav measured **0.77 fps paired** (4000 frames in 87 min; I/O-bound — GPU ~2% util). At that rate naïve Tier-2 full-frames across all 9 surfaces ≈ **52 GPU-hours**, of which Anti-UAV alone = 85,374 paired frames ≈ **31 h on a saturated 0.994 surface** (ledger `antiuav-saturated`) — indefensible. Decision: (a) **Tier-1 strided n=4000 IS the final thesis number for all non-sequence surfaces** (binomial CI ≤ ±1.5 pp at n=4000; selcom 311 / rgb-confuser 2,633 / gray-confuser 2,633 are already FULL below the cap; temporal is meaningless on photo-style test splits and confuser image piles); **Anti-UAV full is DROPPED** (Tier-1 4k + a saturation footnote). (b) **Tier 2 = svanström paired + svanström_gray ONLY, consecutive frames** (~10.4 h + ~5 h full, or ~8 h total with complete-sequence subsetting — 2-of-3 voting is valid on complete sequences) → produces the temporal/segment rows (C→F→T vs F→C→T) and svanström's final digits. Speed levers if needed: drop patch_v2 scoring at Tier-2 (its comparison is already told at Tier-1) + stage svanström to local SSD. Side-fact: `IR_confusers` physically holds **5,237** images vs 5,938 in datasets.csv — reconcile before the dataset appendix. `THESIS_EVAL_LEDGER.md` §0 banner updated.
- 2026-06-10 — **Two-tier run plan + temporal deferred.** Eval runs in two tiers: **Tier 1 = ~4k STRIDED screening** (every Nth frame, NOT first-4k; all-if-smaller) for per-frame P/R/F1 NOW; **Tier 2 = FULL consecutive frames LATER** (gated on Tier-1 results) for the final thesis numbers. **Temporal/segment-level (C→F→T vs F→C→T) is a PLACEHOLDER until Tier 2** — 2-of-3 voting needs consecutive frames and cannot run on a strided sample. Reason: striding breaks temporal continuity, and full-frames is expensive → gate it on the cheap strided screen. `THESIS_EVAL_LEDGER.md` §0 banner + Part B updated; tasks #3/#4 updated.
- 2026-06-09 — **A+D spine + GUI + temporal + per-modality scoring locked; production provenance verified.** (a) Spine finalized **A+D**: the SYSTEM (dual-modality pipeline + operator GUI) is the headline, produced/maintained by a self-improving loop (HITL data engine + Model MRI); **contributions C1 pipeline+GUI / C2 Model MRI / C3 HITL** — MRI elevated to a named contribution, cross-modal grayscale transfer DEMOTED to a *finding* (supersedes the earlier "grayscale = headline" hybrid). (b) **Temporal** added to the pipeline eval: segment-level 3-frame 2-of-3 (the PySide GUI alert-gate), order A/B **C→F→T vs F→C→T**, full fusion pipeline **PAIRED-ONLY** (Anti-UAV, Svanström) + grayscale-where-it-works. (c) **Scoring = per-modality, NO UNION** (trust-RGB→RGB GT only, trust-IR→IR GT only, trust-both→both; bare→each modality vs own GT; never score a rejected modality). (d) **Frame budget = ~4k cap** (all-if-smaller; supersedes the §0 "full split" for rgb/ir test). (e) **Filter corrected**: production patch verifier = **5-class MobileNetV3-Small (drone/background/airplane/helicopter/bird), fail-open**, ~44,918 RGB+IR crops mined from per-class video clips (airplane_rgb*/IR_AIRPLANE_*/IR_DRONE_*, etc.) — NOT the detector corpora. (f) **Production provenance verified**: FT4 = `ft3_1280` + 300 confuser hard-negs (freeze=15, 3ep, regression-gated; `auto_confuser_ft4.py`); mlp_v5 = FT4 p3+p5 distill on the **11-source drone+confuser** corpus with selcom drones purified to pure CCTV (`distill_v5_swap_selcom.py`); robust8 = full56/optimal_v1 + is_grayscale (`train_routing_robust.py`). Reason: lock ALL methodology before building `thesis_eval/`. Spec in `knowledge/THESIS_EVAL_LEDGER.md` § THESIS EVAL STRUCTURE.
- 2026-06-09 — **THESIS EVAL STRUCTURE locked** + thesis surface set tightened. The empirical chapter is reorganised into **three parts on two axes**: Part A **per-model in-domain A/B** (three versions per model — RGB baseline/retrained_v2/ft4 · IR V2/V5/v3b · classifier sa32/robust6/robust8 · verifier bare/patch_v2/mlp_v5), Part B **OOD ablation** (bare→+classifier→+filter on RGB+IR confusers), Part C **grayscale = best-results highlight** (cross-modal transfer, bootstrap CI). **Canonical surface set cut to 8** (rgb test, ir test, Anti-UAV, Svanström, SelCom, RGB confusers, IR confusers, grayscale); **video, Roboflow, rgb_bird_confuser demoted to internal/supporting** (birds are a *category* across the confuser sets, not a dataset). Detectors share splits → true A/B; **classifier/verifier scored by pipeline EFFECT (Part B), not standalone accuracy** (own-held-out not cross-comparable). Reason: the empirical chapter mixed A/B, ablation, and surfaces inconsistently → this enforces one structure. Spec in `knowledge/THESIS_EVAL_LEDGER.md` § THESIS EVAL STRUCTURE. Working thesis = `docs/thesis_working_distilling_overleaf/` (Overleaf split). Narrative spine pending user confirm (system-vehicle / grayscale-headline hybrid recommended). Outstanding compute: Anti-UAV RGB bare @640 (thesis still @1280).
- 2026-06-09 — eval methodology + IR-verifier simplification. (a) The routing eval (`compare_routing_pipeline.py`) now **LOADS the shipped robust8** (`models.csv` weights_path) instead of re-training it in STEP 1 → pure **eval-only**; normalized the saved `robust8.joblib` stored τ **0.1→0.20** to match the documented ship setting (`robust8_tau0.10.joblib` backup kept). (b) Adopted a **single IR verifier**: `mlp_v5_ir_aligned` (`ALIGNED_THR`) now used for BOTH thermal and grayscale — **dropped the separate `mlp_aligned_gray`** — in `compare_routing_pipeline.py` AND `pipeline_eval_offline.py`. Reason: an eval must score the frozen production classifier, not a re-trained proxy; and the single aligned verifier is **recall-safe on grayscale** (mini: gray_svan drone R 0.548→0.505 vs the dedicated gray verifier's 0.548→**0.164** over-veto), trading weaker grayscale-confuser suppression (gray_confuser FP 20→68). Recorded as ledger `mini-offline-verifier-matrix-1k` (triage/open).
- 2026-06-09 — locked the eval **IMGSZ STANDARD** and recorded its configs/datasets. Standard: **640 default; 1280 only for low-native-resolution sources** (Svanström-RGB 640×512 → upscale resolves sub-floor drones; SelCom-RGB poor CCTV) — Anti-UAV (1920×1080), video (16:9), all confusers, and all IR are **640**. New `eval_configs`: `antiuav_iou_640_4k` (Anti-UAV @640 n=4000, CHANGE from 1280), `confuser_zoo_640` (restate from `confuser_zoo_1280`), `ir_confusers_640`, `rgb_bird_confuser_640`. New `datasets`: `IR_confusers` (5938 thermal airplane/bird/heli; replaces cbam-180 which is small + in-domain) and `rgb_bird_confuser` (1731 RGB; registered as confuser, eval-standalone-first per user). Extended scripts: `pipeline_cache.py` (+ir_confusers,+rgb_bird_confuser surfaces) and `compare_routing_pipeline.py` (video imgsz 1280→640, +rgb_bird_confuser). Reason: methodology must justify any imgsz deviation; the thesis's Anti-UAV/confuser-zoo 1280 had no resolution justification → must be re-stated @640 (IR already 640). Standard + 4 stage-tables live in `knowledge/THESIS_EVAL_LEDGER.md` §0; a 1000-frame triage MINI run (clearly flagged non-standard) was launched to populate placeholders before the full standard run.
- 2026-06-09 — created `knowledge/THESIS_EVAL_LEDGER.md` (hand-maintained index of every thesis eval by pipeline stage — Stage 1 model-alone / Stage 2 +trust-classifier / Stage 3 classifier+filter / Stage 4 model+filter — with a §0 per-dataset "eval constraint" standard: one canonical `eval_config` + frame budget per dataset) — reason: the 2026-06-09 production-stack eval (`_routing_pipeline_cmp`, n=4000) was found NOT comparable to existing thesis tables (Svanström stride≈7 vs canonical stride-9 `svan_iop_1280_s9`; Anti-UAV stride≈21 vs stride-5; offline verifier-matrix n=1000) — the ledger fixes one config per dataset so all four stage-tables share frames. §0 standard is PROPOSED (Anti-UAV n, RGB-confuser imgsz, RGB-in-domain n still OPEN), not yet locked. The routing run itself recorded as ledger `robust8-full-pipeline-cmp` (status=open; numbers internally-comparable only, need a re-run at the standard).
- 2026-06-05 — added `datasets` table to kb.py (cols: id/name/modality/role/size/physical_root/scoring/provenance/redistributable/thesis_ref/notes; required name+modality) — reason: the 7 thesis corpora + Roboflow/YouTube were mapped only in prose (`app:datasets`) and scattered physical paths (`eval/config.yaml`, across G:/drone + C:/Desktop + in-repo); a normalized table makes `eval_configs.dataset` a real FK and lets the dataset appendix generate without drift. Populated 8 rows from `docs/analysis/2026-06-05_datasets_registry.md`. Physical `datasets/` bundle deferred (backlog task 1).
- 2026-05-30 — EVIDENCE_LEDGER.md marked safe-to-archive (fully migrated); V5 (mlp_v5 pure_1x8) signed off production=yes (v5.2 variants net-negative, not shipped); rgb_m0_baseline confirmed MD5-dup of baseline -> safe-to-archive.
- 2026-05-30 — built thesis automation: added `claims` + `figures` tables to kb.py; engine `_tools/thesis_audit.py` (number-vs-evals + figure provenance, permissive matcher); `thesis` skill (modes audit/draft/novelty/email, evidence-only, edits `docs/thesis_working.tex` copy of thesis_chapters.tex). First audit: 792/795 numbers matched, 3 to verify, 9 figures (orphan = generating-script mapping not yet wired).
- 2026-05-30 — full forensic reconciliation applied (3 read-only census agents w/ scoped shell perms): complete RGB+IR detector lineage (24 IR detectors incl. corrective chain + archive), classifier zoo (~40: phase1/2, reliability, failure, fusion_no_fn family, patch CNN lineage, V5/V4 variants), and remaining ledger sections (§6/§8.3/§9.5/§12/dual-clf/distill-CV/§13.2). Fixed ir_v3 conflation (dataset-V3 vs corrective-v3). Final: 116 scripts / 99 models / 26 configs / 116 evals / 39 findings. EVIDENCE_LEDGER now fully extracted -> eligible for safe-to-archive on user green light.
- 2026-05-30 — closed audit threads: trust-classifier evals (3way + confuser zoo), IR version lineage V2-V6, un-ledgered/orphan models, superseded overlap-group scripts, eval cache_path wiring, and remaining ledger sections (§8 Roboflow, §9.4 corrected video, FT4 freeze) — via idempotent one-off scripts under `_tools/`. Corrected the `ir-grayscale-fallback` finding to §9.4 framing (matches, not beats). Final: 113 scripts / 29 models / 13 configs / 53 evals / 20 findings.
- 2026-05-30 — purpose-aware rankings: `confusion-filter` ranks by lowest halluc_rate, detection purposes by best F1 — reason: ranking a confuser-filter by drone-F1 conflates detector quality with the filter's actual job (suppressing FPs).
- 2026-05-30 — populated tables via two idempotent one-off scripts (`_tools/migrate_evidence_ledger.py`, `_tools/populate_census.py`) + 3 parallel read-only census agents — reason: reproducible, re-runnable population with provenance; agents returned rows, single writer (kb) avoided write races. EVIDENCE_LEDGER migration is a headline subset only; the file stays `active`.
- 2026-05-30 — added `set` subcommand to kb.py — reason: lifecycle transitions + corrections must go through the tool, never hand-edits.
- 2026-05-30 — created `knowledge/` system (tables: scripts, models, eval_configs, evals, ledger; views: rankings, comparisons; PROJECT_STATE + this log) — reason: ad-hoc EVIDENCE_LEDGER + scattered docs kept drifting; no enforced method, no provenance, script sprawl. Spec in `knowledge/README.md`.

## 2026-06-12 — `runs/` canonical provenance layer
The thesis review asked "how does a reader confirm the numbers?". Decision: top-level `runs/`
directory = frozen evidence snapshot for the thesis (copies of `thesis_eval/results/*.{json,md}` +
`conf_sweep/`), indexed by `runs/README.md` (headline number -> file cell -> replay command -> audit
check). Pre-existing `runs/corrective_finetune/` (v3b training record) kept in place as training-side
provenance. Thesis Appendix "Number Provenance and Reproducibility" (app:provenance) mirrors the map.
Live replays still regenerate into `thesis_eval/results/`; after any number-touching change, refresh
the copies in `runs/` and rerun `thesis_eval/_audit_headline_numbers.py` (now 77 checks).
