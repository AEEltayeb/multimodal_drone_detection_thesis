<!-- The "Resume Here" block is updated every session-end (hook/`/resume`).
     Production stack + open threads are curated. The rankings/comparisons/table views
     under knowledge/views/ and knowledge/*.md are GENERATED — read those, don't edit. -->

# Project State

## 📍 Resume Here
_Updated: 2026-06-14 (PUBLISH — ES_Drone_Thesis pushed to GitHub, runnable clone; prior: SESSION 9b)_

> **2026-06-14 (PUBLISH — pushed to GitHub `AEEltayeb/dorne_thesis`):** Made the repo a runnable, self-
> contained clone (weights + thesis + figures + all code, ~1.19 GB, plain git, NO LFS; `main` @ `fe4d735`).
> Remote `origin` added (was zero remotes); auth cached via Git Credential Manager. Replaced the inherited
> ES_Drone_Detection `.gitignore` (it hid `models/`/`*.pt`/`*.csv` → only 686 of 148k files tracked) with a
> purpose-built one that EXCLUDES raw `datasets/`, videos, regenerable caches (`thesis_eval/cache`,
> `eval/results`, `eval/cache`, `mri/results`), patch training-crops (`models/patches/{ir,rgb,*_synth_review}/`
> — the `.pt` weights + OOD calib at `models/patches/` root ARE kept), and the long-path ablation dumps
> (`docs/analysis/full_pipeline_ablations/{csvs,raw_results}/`, which exceed Windows MAX_PATH 260 and would
> break clone checkout). GUI portability (PySide path only): `gui/fusion_settings.json` paths now repo-relative,
> resolved in `flet_app/settings_dialog.load_settings()` against `REPO_ROOT`. Verified filter→classifier cascade
> works with the no-reject router (`thesis_eval/_cascade_noreject_smoke.py`; never rejects). 11 legacy scripts
> repointed off `ES_Drone_Detection`. `requirements.txt` expanded (PySide6/flet/scipy/seaborn/…) + README
> "Run on a fresh clone". Push needed **HTTP/1.1 + postBuffer 2 GB** (HTTP/2 aborted: curl 28). Memory:
> `project_github_remote.md`. Known bloat (kept, not blocking): `classifier/fusion_models/` dups
> `models/routers/` (~160 MB; live eval scripts still reference the old path).

> **2026-06-14 (SESSION 9b — shipped COMPOSITION = `filt→clf[robust8-nr]`):** User spotted that filter-then-
> router beats router-then-filter for the no-reject router. Confirmed by extending `part_c` to compute
> `filt→clf` confuser fire (re-ran rgb/ir confusers → results_noreject, parity clean): `filt→clf[nr]` wins on
> drones (Svanström 0.930→**0.944**, DUT 0.792→**0.837**, recall 0.960→**0.991**, Anti-UAV tie) and is
> **identical** on confusers (RGB 0.011 / IR 0.237 — a no-reject router never vetoes a confuser frame, so order
> is immaterial there). Switched shipped composition to `filt→clf` everywhere (re-bolded production rows,
> RQ3 0.944 now edges IR-only 0.940, abstract 0.944/recall 0.991, composition-order + RQ1 + DUT readings +
> captions + conclusion + appendix updated; `part_c` harness now emits filt→clf for f8 routers). Audit
> **200/200**, build **146pp**. clf→filt rows kept as the alternate order; robust8 still the reject-class
> ablation (and stronger on the confuser-rich paired flagship: filt→clf 0.963 vs 0.944).

> **2026-06-14 (SESSION 9 — `robust8-nr` is the SHIPPED trust router):** User decided (data-backed; I ran a
> hybrid sweep proving "nr + harder filter" is dominated — nr confuser floor 0.0038 never reaches robust8's
> 0.0015 and bleeds recall) to ship the **no-reject** router globally: robust8's 8 features with the `reject`
> class dropped, argmax route (no τ), per-frame filter owns FP rejection. Model
> `models/routers/robust8_noreject_drop/model.joblib`; numbers `thesis_eval/results_noreject/`. `robust8`
> (with reject) KEPT as the headline ablation and recommended on confuser-rich PAIRED streams. Thesis display
> name = **`robust8-nr`**. Did: nr rows in ALL classifier tables (svan/antiuav/dut/confusers/solo/temporal/
> rq3/modality_ab), nr bolded production + robust8 un-bolded, the 4 `rej≥0.8` rows + paragraph DELETED;
> §sec:classifier_results EXPANDED (no-reject design, reject-as-liability, grayscale-hole now dissolved, cost);
> five-readings + RQ2/RQ3 reframed; `fig:robust8_operating`→design-history; abstract/conclusion/§3.8/appendix
> updated. ACCEPTED headline moves: Svan flagship F1 0.949→0.930, confuser 30.4%→1.1% (0.15%=robust8 ablation),
> mean composed drone F1 0.733→0.834. Extended `notes_round1_replays.py` part_modality_ab to emit the nr arm.
> Audit **198/198** (nr cells + results_noreject paths); build **145pp 0 err**; kb `robust8_nr_drop`
> production=yes (dup `_2` archived), `robust8` production=no, ledger `noreject-router-over-reject`→shipped,
> `reject-floor-recall-recovery`→superseded. **OPEN/flagged:** `tab:robust6_pipeline` + `tab:classifiers` are
> predecessor/GPU-harness design-evolution tables — nr NOT added (forward-note only); user may authorise an
> `eval/overnight_ablation.py` GPU run to put nr there. `fig_pipeline_ablation` still drawn for the robust8
> cascade (recaptioned; regenerate for nr if wanted). Also still open from before: task #13 screenshots,
> title-page TODOs, submission ToC build.

> **2026-06-14 (SESSION 8c — filter operating-point FIGURE, keep all numbers):** User wanted the filter to
> read as non-harmful. Honest resolution (a single threshold CANNOT improve everywhere — filter is a
> precision tool: helps on confuser-rich, neutral on saturated, costs a little on confuser-free): KEEP all
> shipped numbers (0.25/0.05/0.25) and ADD `fig:filter_operating` + a "recall/FP dial" paragraph in Ch4
> §sec:verifier_results. Figure = 3-panel per-filter P(drone) sweep (drone detection recall vs confuser
> fire-rate, shipped point marked), generated by **resident** `eval/filter_operating_sweep.py` →
> `figures/fig_filter_operating.pdf` + `eval/results/filter_operating_sweep.json` (zero-GPU, from caches;
> RGB fire@0.25=0.011 matches tab:ablation_confusers). Story: RGB flat high-recall shoulder (recall-
> transparent, free to recover), IR-thermal fire flat at every t (airplane coverage gap, not tunable),
> grayscale steep recall slope (shipped 0.25 keeps only 46.7%; 0.05→61.8% at FP 0.008→0.036). Paragraph
> states the dial both ways honestly (threshold-limited RGB recoverable; coverage-limited IR/gray only
> partial). Audit **180/180** (+6 caption cells +3 cited paths); build **141pp 0 err**; kb figures
> `fig_filter_operating` + ledger `filter-threshold-sweep`. No re-baseline; no headline moved.


> **2026-06-14 (SESSION 8b — two asks):** (A) **Split the mlp filter-only row** in the 3 paired ablation
> tables into `filt only (mlp_v5, RGB)` and `filt only (aligned/aligned_gray, IR)` (patch stays one row).
> Added `filt_mlp_rgb`/`filt_mlp_ir` cells to `part_b` (TA(3, rgb_flt, ir_all) / TA(3, rgb_all, ir_flt)),
> temp-reran + parity-merged into tier1/results_dut (`merge_split_cells.py`), refroze runs/. Insight: on
> Svanström the RGB filter does ALL the work (filt_mlp_rgb 0.907 ≈ both; filt_mlp_ir 0.742 ≈ bare —
> thermal filter does nothing there); on Anti-UAV both ≈ bare (saturated); on DUT each alone is a smaller
> recall tax than both. Audit +6 cells. (B) **Per-filter P(drone) F1 sweep** (`diag_filter_sweep.py`,
> detection-level pooled PR, zero-GPU, DIAGNOSTIC ONLY — no thesis/threshold change): RGB mlp_v5 shipped
> 0.25 ≈ optimal (F1 .976; max .984 @~0.05-0.08, flat); IR-thermal aligned F1 FLAT .939-.944 everywhere
> (airplane gap), shipped 0.05 = max recall; **grayscale aligned_gray shipped 0.25 OVER-VETOES (recall
> .467), F1-max @0.02 → recall .710 / F1 .632→.788 at FP 21→198** (the one threshold genuinely mis-set
> for recall). kb ledger `filter-threshold-sweep`. Diagnostic scripts in ES_Drone_Detection. Audit
> **171/171**, build **140pp 0 err**, em-dash clean. Notes file ends at line 911.


> **2026-06-14 (notes SESSION 8, lines 866–911 — Ch4 ablation clarity + methodology threshold fix; NO reruns):**
> Driven by the user rule **[[explain-what-reader-would-ask]]** (if the user had to ask, the thesis is unclear there — explain it in prose, don't just fix the number). **N1 (threshold confusion):** the §pipeline_ablation "Stack under test" para (empirical.tex l.12) now distinguishes the two stages — detectors are cached at **conf 0.25 for BOTH** (verified `pipeline_cache_unified.py` l.14–16; deployment IR floor 0.40 but eval insensitive since the filter rescores, §design_rationale), and the **filter** thresholds (mlp_v5 RGB 0.25; aligned thermal 0.05 / grayscale 0.25) are P(drone) thresholds, so the IR `0.05` is a filter threshold NOT the IR detector floor. (My first draft wrongly said IR detector @0.40 in these tables + used a non-existent label `sec:deferred_suppression`; correct label is `sec:design_rationale`.) **N2:** reject-floor added to the ablation-arms list. **N3:** removed "(one x across all four tables)" from svan caption. **N4:** 95% bootstrap CI now defined at first use (central 95% over 1000 frame resamples; non-overlap ⇒ beyond sampling noise). **N5/N7/N9:** added **RGB-only + IR-only** isolated rows (vs own GT) to the 3 paired ablation tables (svan 0.607/0.940, antiuav 0.985/0.961, dut 0.899/0.596[gray fallback]); captions now carry the legend (clf=+router, filt=+filter, clf→filt=router→filter) + the **counting note** that paired bare/fused TP/FP/FN SUM both modalities (verified 1532+1610=3142), so they are not a frame count — RGB/IR-only isolate each model. **N6:** captions say "even-strided" (DUT = full split). **N8:** Svanström precision 0.609→0.939 now attributed RGB-side to the mlp_v5 filter (MRI-validated) + router. Audit **165/165** (+1 svan IR-only cell; rest already covered); build **140pp 0 err**; em-dash clean. No kb ledger (clarity only). Notes file ends at line 911.


> **2026-06-14 (ROUND 8 — reject-floor ablation arm `clf→filt[robust8,rej>=0.8]`):** From the DUT no-reject diagnostics, added ONE new ablation arm using a SINGLE global reject-probability floor **x\*=0.80** (honour robust8 `reject` only if P(reject)≥0.80, else route to the next-best single modality + that modality's filter) to the 4 full-pipeline tables: `tab:ablation_svanstrom`, `tab:ablation_antiuav`, `tab:ablation_dut` (P/R/F1), `tab:ablation_confusers` (RGB+IR fire; gray `---`). x\* picked by the **balanced** rule (max drone-F1 s.t. confuser fire ≤~1pp; fine sweep 0.6–0.9 → 0.80, IR fire +0.9pp the binding cap). Effect: DUT R **0.575→0.607**, F1 0.701→0.723; Svanström 0.949→0.948, Anti-UAV 0.984 (unchanged — rarely reject); confuser cost RGB 0 / IR +0.9pp (0.217→0.226). Framed as a **sensitivity/operating-point arm**, NOT a retuned headline (shipped cell stays `clf→filt[robust8]`; x\* chosen on test surfaces). Harness: `pipeline_eval_unified.py` gained `REJECT_FLOOR=0.80` + `reject_floor_labels()` + the cell in part_b/part_c. Regenerated into TEMP dirs, **parity-checked** (all pre-existing cells byte-identical) then merged the new cell into `results/tier1_results.json` + `results_dut/` via `ES_Drone_Detection/merge_rf_cell.py`; froze `runs/` copies. Audit +6 cells → **164/164**; build **139pp 0 err**; em-dash clean. kb ledger `reject-floor-recall-recovery`. Diagnostic/merge scripts live in `ES_Drone_Detection/` (uncited tooling). NOTE: a finer/clean win would need retraining robust8 WITH the filter P(drone) as a feature (CPU, not done).


> **2026-06-14 (DUT Anti-UAV test-split ablation added to Ch4 §pipeline_paired):** new `tab:ablation_dut` — the 960 full-pipeline ablation on the DUT Anti-UAV **test split** ($n=2{,}200$, 2{,}245 GT, IoU@0.5), RGB `ft4` in-domain + grayscale-IR `v3b` cross-domain fallback. Per user: "bare" row renamed to name the models ("RGB `ft4` + grayscale-IR `v3b`"); test-split-only stated. Reading: confuser-free ⇒ filters are a pure recall tax (filt_mlp weakest 0.692); router buys precision (0.864→0.895, F1 0.758→0.763); best single config = RGB-alone `ft4` 0.899 (fused bare 0.758 dragged down by gray-IR 0.596). Numbers verified vs `thesis_eval/results_dut/tier1_results.json` (frozen copy `runs/results_dut/`); kb already had datasets.dut_antiuav_test + 4 evals + ledger `dut-resolution-pipeline`. Audit +8 DUT cells +2 paths → **158/158**; build **138pp 0 err**. (640 numbers exist in kb but per user only the 960 ablation went into the thesis.)
>
> **2026-06-13 (notes ROUND 7 EXECUTED — thesis notes.txt lines 828–864; review window "§3.8.9 till Ch4", all edits in §3.8 + appendix glossary):**
> **DECISION (locked):** keep **robust8** as the shipped trust router (its Ch4 tables/numbers/`fig:robust8_operating` UNCHANGED — robust6's own measured numbers are worse on grayscale/ir surfaces: selcom 0.072 vs 0.221, ir_dset 0.878 vs 0.932, though headlines barely move svan 0.949→0.947 / antiuav 0.984→0.978). Scope = §3.8 methodology cleanup only: do NOT narrate the robust6→robust8 difference or any grayscale routing; no grayscale "model/classifier" anywhere (only grayscale-specific model is the aligned MLP filter `mlp_v5_ir_aligned`).
> **N1** (l.707): "consumes ... raw outputs **and the source frames**" → shipped classifier reads detector OUTPUTS ONLY; source-frame scene stats belong only to the deprecated hand-engineered variants (dropped as leakage). **N2:** deleted the Failure-model-scores feature bullet (`control_v3more_40feat`). **N3 (report-only):** `fusion_no_fn_v1.1` + `control_v3more_40feat` ARE defined (methodology l.722 + appendix glossary) — both kept (appear in Ch4 tables). **N4:** expanded LDA/PCA/AUROC on first §3.8 use (LDA was only defined far up at the MRI section l.463). **N5:** added robust6's 3 RGB twin rows to `tab:leakage` keep-block with REAL `feature_stats_ranked.csv` values (rgb_max_conf 0.816/0.004, rgb_best_log_bbox_area 0.806/0.059, rgb_best_aspect_ratio 0.778/0.011) — table now visibly contains robust6's RGB half; existing "IR-dominant corpus" sentence explains the lower RGB AUROC.
> **N6:** DELETED the "Closing the grayscale hole: robust8" paragraph + the Further-free-levers grayscale-routing sentence (macro-F1 0.36→0.61); replaced with ONE neutral clause "the shipped robust8 extends this six-feature core with two further free detector features." Appendix glossary robust8 entries (l.148, 256) softened (dropped is_grayscale / trust_rgb / "closes grayscale recall hole" rationale); fnfn/control40 glossary failure-model mentions removed.
> **N7 (advisory, no thesis edit):** classifier-clarity write-up — why the classifier section reads clearest (one question → one statistic (leakage ratio) → one table → named artifact falls out; method-before-result; source comment on every number) + emulable template + candidate sections (cascade, trust-fusion truth-table, mlp_v5 opener). **Lives in `ES_Drone_Detection/docs/analysis/2026-06-13_classifier_section_clarity.md` — NOT in the thesis repo** (advisory, uncited; per user, non-proof analysis goes in the old dir to keep `ES_Drone_Thesis` clean — only thesis-cited proof stays resident here).
> **Audit 148/148 unchanged** (no audit cell keyed on the removed grayscale numbers; line 152 reads robust8 which we kept). **Build 137pp, 0 errors, 0 concrete undefined refs.** Em-dash grep clean (only the 2 verbatim MRI quotes; I caught + fixed one prose `---` I introduced in the N1 edit). No reruns, no writes to the frozen archive.
> **STILL OPEN (carried):** Ch4 review pass — the empirical.tex "robust8: the shipped operating point" paragraph + `fig:robust8_operating` τ-sweep IS the Ch4 grayscale-hole story (left for when user reviews Ch4), plus prior flags **B** (robust6 0.179 prose vs robust8-only `tab:ablation_confusers`) and **C** (retrained_v2 svan recall 0.306 full vs 0.323 stride-7). Round-8 notes = anything after thesis notes.txt line 864. USER items: 3 GUI screenshots (#13), title-page TODOs, submission ToC build.
>
> **2026-06-13 (notes ROUND 6 EXECUTED so far — thesis notes.txt now ends at line 826):**
> **Note 1 (grayscale/IR filter reframe):** per user, REMOVED all data-scarcity framing ("grayscale supplied what scarce thermal data could not", "data-starved", "blocker") from §3.8.8 + empirical §4 §grayscale; §3.8.8 opening rewritten descriptively (what we did / findings / how). Confirmed in code (`thesis_eval/pipeline_eval_unified.py:68-72`) production ships **ONE** net `mlp_v5_ir_aligned` (two per-modality scalers), NOT two; dedicated `mlp_v5_gray` flipped active→superseded (kb). The dronediv-vs-aligned A/B on held-out CBAM is **CONFOUNDED** (dronediv trained on CBAM-train, aligned held it out) → can't isolate grayscale's thermal effect; user DECLINED the clean retrain; recorded ledger `cbam-ab-confounded`; `mri/train_aligned.py` gained a dormant `--no-gray` flag (future clean A/B) + `mri/configs/ab_cbam_heldout.yaml`.
> **Notes 2a/3a:** §3.8.8 MRI numbers VERIFIED vs `mri/results/ir_v3b_report/{stats.json,report.md}`: 14,697/1,386 dets, LDA 0.981, max F 5,370, halluc 1.8%; Jaccard 0.71–0.88, corr 0.93–0.99, cosine 0.012, transfer AUROC 0.500/0.707/0.919/0.974. No corrections.
> **Note 2b:** added to §3.8.8 — IR already cleaner than RGB so a thermal filter isn't required but helps; **every number names its surface** (1.8% on the Model-MRI thermal-confuser corpus / 30.4% on `rgb_confusers_merged` OOD / CBAM 48→15 held-out). Substantive: the 1.8% is near-domain; the hard OOD set fires far more — surface MUST be stated.
> **PITSTOP (note 3b) — inline-number→table scan:** `docs/analysis/2026-06-13_inline_number_table_scan.md`. Verdict: thesis already table-dense; only real gaps were the §3.8.8 MRI blobs. Built **3 new tables**: `tab:ir_mri_sep` (MRI separability) + `tab:gray_thermal_auroc` (transfer-AUROC ladder) in §3.8.8 (+8 audit cells), and `tab:resolution` (2×2 baseline/retrained_v2 × 640/1280) by `fig:resolution`; inline restatements de-dup'd to cross-refs in related-work + empirical + methodology.
> **Ch4 self-consistency sweep DONE** (the pre-review obligation): empirical.tex clean on em-dashes / terminology ("filter" in prose; "verifier" only in labels/comments) / fail-open (only the rejected variant) / within-model resolution / numbers-vs-audit / headline-F1-only / cross-refs. **Fixed D:** §4.3 "accepted only 15%" → "about one in eight" (matched R=0.128 / 7-of-8 missed). **FLAGGED for user's Ch4 read (not edited):** (B) §4 prose cites IR-confuser "robust6 0.179" (audited) but `tab:ablation_confusers` shows only robust8 composed 0.217 — add a robust6 row or reword; (C) retrained_v2 Svanström recall 0.306 (`tab:rgb_comparison`, full 28,710) vs 0.323 (`tab:resolution`, stride-7 n=4,102) — different samples, a one-line footnote would prevent confusion.
> **TWO STANDING RULES now persisted:** (1) **thesis proof artifacts MUST be resident in ES_Drone_Thesis** (copy over anything only in the old dir; DECISIONS 2026-06-13 + memory `thesis-artifact-residency`); (2) **every number states its dataset/surface inline** (memory `number-needs-dataset`; user confirmed as default).
> **kb this session:** ledger `cbam-ab-confounded` (new) + `ir-grayscale-harvest-solves-thermal-verifier` reconciled to 48→15; models `mlp_v5_gray` superseded; scripts `mri_train_aligned` reproduce_cmd; DECISIONS residency entry. kb validate clean.
> **Audit 148/148** (119 cells incl. +8 MRI + 2 CBAM-tex-consistency; 29 paths). Also fixed a latent audit bug this session: the validation loop ran BEFORE the SWEEP/CLEAN/RES/CBAM `CHECKS +=` blocks (counted-but-unvalidated) → moved loop to end; all now genuinely validated and pass. **Build 137pp, 0 errors, 0 concrete undefined refs** (persistent "undefined references" summary line = known benign phantom).
> **REMAINING:** USER task #13 three screenshots; title-page TODOs; submission build (\listoftables/\listoffigures + ×3 + bibtex). Decisions B/C await user's Ch4 read. Round-6 review resumes AFTER §3.8.8: rest of Ch3 (Trust Classifier, feature selection, patch/mlp_v5 filters), then Ch4, Ch5, appendices. Notes after line 826 = next batch (none yet).

> **2026-06-13 later (notes ROUND 5 EXECUTED — trust-fusion truth fixes + resolution 2×2):**
> Round-5 notes (thesis notes.txt lines 725–773, file ends there) all applied. **Prose truth fixes:** §3.8.8 +thermal-neutrality preempt (aligned filter ΔR −0.007/0/0 on thermal, CBAM FP 48→13 — grayscale capability cost nothing; predecessor dropped for recall-unsafety) + "(a per-feature z-score shift)"; §trust-fusion now lists all 8 robust8 features and states the router reads DETECTION EVIDENCE ONLY (scene stats only in sa32); "leakage-aware" jargon replaced everywhere with the explained scene-fingerprint-ratio screening; three-stance lesson → motivated the FILTER (router needed regardless); false "recalibration swap" clause deleted; "An MRI" cross-ref'd; baseline P0.940/R0.961/94.4% confirmed BARE (S1 RGB-alone) + source comment.
> **Resolution 2×2 (user GPU, `eval/svan_resolution_sweep.py`, stride-7 n=4,102, IoP@0.5, one harness):** baseline 0.684@640 → 0.964@1280 (+28pp); retrained_v2 0.070@640 → 0.323@1280 (+25pp). **Settles the old figure's sins:** the 0.072 attribution WAS retrained_v2 (confirmed under our harness), but "recall collapses 0.961→0.072" mixed models — baseline only *degrades* at 640, retrained_v2 collapses. fig6_6_resolution REDRAWN as grouped 2-model×2-size bars from the sweep JSON ("13.3×" annotation gone); §3.2.3 + drone-size caption + §resolution_arch + empirical three-stance ¶ + related-work §2 all rewritten within-model. Results: eval/results/svan_resolution_sweep.json (frozen copy runs/svan_resolution_sweep.json); kb evals svan_res_baseline_640 / svan_res_retrainedv2_640; tasks #32+#35 closed.
> **Audit 138/138** (109 cells incl. 5 RES + 29 paths). 136pp, 0 errors, 0 ??.
> **REMAINING:** task #13 USER three screenshots (reviewer home + launch, PySide); title-page TODOs; submission build (\listoftables/\listoffigures + pdflatex×3 + bibtex). thesis notes.txt fully processed through line 773 — anything after = round 6.

> **2026-06-13 (CLEAN SPLIT DONE + IN THESIS):** user ran the GPU build (57,542 auv + 5,557 svan frames, full, --no-patch); replay to `thesis_eval/results_clean/` (NOTE: always `--out results_clean` — `--only` into the default dir would clobber tier1_results.json). **Results:** auv clean ≥ headline on every arm (bare 0.9765 vs 0.9728, pipeline 0.9862 vs 0.9844 — ZERO inflation, overlapped segments were the harder ones); svan clean: v3b solo 0.8674 vs 0.9401 (≤7.3pp inflation, ~half is difficulty per the leakage-free RGB control −3.5pp), **production pipeline 0.9348 vs 0.9485 (−1.4pp, smallest drop of any arm)**, lift preserved 0.684→0.935; router orderings unchanged. filt_patch rows INVALID on these caches (no-patch). **Integrated into thesis:** §3.3 revived as Training–Evaluation Overlap Audit (overlap matrix incl. RGB-side auv row + clean-split definition ¶ + tab:clean_split 8 rows + 3-conclusion verdict + router-residual disclosure); protocol table +2 rows; contribution 1 clause restored; empirical limitations ¶ re-numbered; app:provenance +2 rows. **Headline F1 stays the headline** — clean split lives only in §3.3 as the control. **Traceability package (user-requested): `runs/clean_split/`** = README (definition, regexes, 5-step re-derivation, caveats) + svanstrom/antiuav_clean_sequences.txt + manifest + frozen results json/md. kb: evals clean_svan_full/clean_auv_full + datasets.clean_split. **Audit 130/130** (104 cells incl. 12 CLEAN + frozen==live guards; 26 paths incl. runs/clean_split/*). 136pp, 0 errors, 0 ??.
> **Open:** task #32 baseline@640 svan GPU run (resolution figure); task #13 three screenshots; round-5 notes after line 724.

> **2026-06-12 night-3 (notes ROUND 4 EXECUTED — §3.4–§3.8 verification fixes, audit 108/108, 134pp):**
> Session-4 notes (thesis notes.txt lines 604–724) decided one-at-a-time and applied. Fixed factual errors: **"A1–A4" never existed** (real FT4 grid = R1/R2/R3/A1/A2 → new tab:ft4_variants with gates, source 2026-05-26_classifier_ft4_analysis.md); **label reviewer is Tkinter not PySide6**, path = top-level `label_reviewer/`, no re-class button (demotion = delete drone boxes), no defer/flag (bullet DELETED), export-to-dataset w/ automatic timestamped backup ADDED (verified in gui.py); reviewer figure split into TWO placeholders (fig:label_reviewer_home setup window + fig:label_reviewer_launch OpenCV canvas — USER captures, task #13 now 3 shots). §3.5 broadened to dataset-quality failure modes + "no formal quality assessment" honesty. §3.6 cites real manifest paths (eval/run_manifest.py, eval/results/<run>/manifest.json, cache sidecars) + names robust8/seed-42 as the only stochastic step. §3.7 scoped to exactly 3 models (mlp_v5, aligned, robust6/8 selection; NOT detectors/sa32); stats.json path mri/results/<run>/; z-score+CORAL attributed to mri/modality_align.py (they DO exist there). §3.7.2: MLP spelled out; **CBAM defined** in §3.1 IR_confusers (~1,775-frame Roboflow thermal confuser set, 180-frame probe = aligned filter's held-out gate); MRI-corrections example made concrete (35,098→32,931; figures from non-shipped corpus; top-discriminator = P5 ch.154 F=42,346, conf rank 6). §3.8.1: RGB item names the corpus, IR "finetuned"→"trained", "brittle on OOD thermal"→measured asymmetry framing. **fig_pipeline.tex REDRAWN**: clf→filter order (production; was wrongly filter→clf) + "confuser filter (MLP)" label, recompiled + synced to docs/figures. Fail-open scoped: detectors fail open; production MLP filter = confidence-gated veto (fail-open variant evaluated and REJECTED).
> **Audit extended with PATH-EXISTENCE block: 108/108** (87 cells + 21 cited artifacts verified present in ES_Drone_Thesis — user requirement: everything the thesis cites must live in this repo). 134pp, 0 errors, 0 ??.
> **STILL PENDING:** task #30 USER clean-split GPU build (must run FROM ES_Drone_Thesis — first attempt ran in the frozen archive and did 0/12; correct first output line = "[clean-split svanstrom_clean] 54 sequences / 5557 frames"); after replay REPORT NUMBERS TO USER BEFORE thesis edits. Task #32 baseline@640 svan run (resolution figure). Round-5 notes start after line 724.

> **2026-06-12 night-2 (CLEAN-SPLIT pivot — thesis rolled back to headline-only, full held-out eval pending):**
> User ruled the cached-subset leakage replays (n=767/2,669) too small to cite. **Thesis now quotes headline F1 ONLY**: §3.3 reverted to the short "Svanström Usage Audit" (false "fully clean" NOT restored — replaced by "least overlap + clean-split eval in progress" + % TBD:), empirical external-validity ¶ de-numbered, contribution-1 clause removed, app:provenance leakage rows removed, runs/README rows marked pending. **2.8pp swing KEPT** (full-surface metric). Audit trimmed to **87/87 pass**; **131pp, 0 errors, 0 ??**. Round-3 leakage numbers live ONLY in runs/leakage_controlled.* + kb (evals marked PULLED FROM THESIS).
> **NEW: maximal held-out clean split built into the cache builder** — `pipeline_cache_unified.py` surfaces `svanstrom_clean` (54 seqs / **5,557 frames**) + `antiuav_clean` (61 segments / **57,542 frames**), sequence-level zero-IR-train-overlap, derived live via leakage_controlled_replay.train_contaminated_ids; split manifest = thesis_eval/results/clean_split_manifest.json; meta now records patch_scored. Dry-count verified exact.
> **PENDING — USER RUNS (GPU, hours):** `py -u thesis_eval/pipeline_cache_unified.py --only svanstrom_clean,antiuav_clean --full --no-patch --overwrite` then zero-GPU `py -u thesis_eval/pipeline_eval_unified.py --only svanstrom_clean,antiuav_clean` → **report numbers to user in chat BEFORE any thesis edit**; only after their go-ahead re-add §3.3 overlap audit + clean-split numbers + audit cells + runs//app:provenance + kb evals.

> **2026-06-12 latest (notes ROUND 3 EXECUTED — leakage audit, audit 103/103, 132pp clean):**
> Round-3 notes (thesis notes.txt lines 499–602) decided one-at-a-time with user and applied. **The big finding: §3.3's "Anti-UAV cascade numbers are fully clean" was FALSE** — measured: IR train holds 22,603 eval-release Anti-UAV frames (30/90 eval segments, 6.3% exact-frame) and 17,314 svan frames (+1,325 czoom; **37.3% of svan eval frames are EXACT training images**); the router was mined from the very Anti-UAV test dir (61/90 segments) and 214/273 svan seqs. **But conclusions survive on clean subsets** (new `thesis_eval/leakage_controlled_replay.py`, kb: leak_svan_detclean/leak_auv_detclean + 2 ledger rows): auv det-clean (60 segs, n=2,669) bare 0.975/pipeline 0.985 ≈ headline; svan det-clean (52 seqs, n=767) **v3b own-GT 0.940→0.862 (~8pp inflation measured)** but pipeline 0.696→0.929 vs 0.949 headline; cascade-clean (also outside router train groups) auv 0.964→0.975 (n=833), svan 0.952 (n=136). Filters: mlp_v5 clean everywhere (pure selcom); aligned clean on auv (val-only, val∩test=0), in-distribution on svan; patch in-distribution on BOTH (crop sources incl. svan+auv — old "Clean" cell was wrong too).
> **Thesis changes:** §3.3 renamed "Training–Evaluation Overlap Audit" = component×surface matrix (all cells measured) + 4 paragraphs (control method / auv verdict / svan inflation finding / cascade-clean); empirical external-validity ¶ rewritten; contribution 1 + tab:datasets + dv5 row + appendix IR-corpus line all pointed at the audit. **N7: scoring swing recomputed — 2.8pp current stack** (dual 0.921 vs trust-aware 0.949, identical P 0.939; 27.7pp kept as dated May-10 @640 historical note). **NEW 4th contribution** = curated data artifacts (HITL corpora + YouTube OOD benchmark; "three"→"four"). Also: consumer-grade→provenance facts (§3.1.8); IoP "required by svan, SelCom same-rule-for-consistency, IoU not separately run"; CI ¶ expanded plain-language; f16 clause names MLP filter; patch-prob "for comparison"; per-modality GT explained; fig 3.5 regenerated (y=fraction of GT boxes, n=boxes in legend, caption fixed — old "large fraction below floor" claim contradicted the plot, now "median 28px near floor + sub-floor tail"); figs 3.5/3.6 now \ref'd + relocated (resolution ¶ / architecture overview).
> **Audit 83→103 checks ALL PASS** · **132pp, 0 errors, 0 ??** · runs/ +leakage_controlled.{json,md} + README rows + app:provenance rows.
> **NEXT:** user reviews PDF; round 4 = anything after line 602 in thesis notes.txt; USER items unchanged (GUI screenshots, title-page TODOs, submission ToC build). After ANY number edit: refresh runs/ + rerun audit (103 checks).

> _previous:_

> **2026-06-12 later (notes ROUND 2 EXECUTED — N1–N12, audit 83/83, 130pp clean):**
> Session-2 notes (thesis notes.txt lines 403–499) decided one-at-a-time with user and applied. **Headline correction: the Svanström "as reported ~0.93/~0.92" comparison rows were WRONG** — actual ICPR paper (fetched, ar5iv): visible F1 **0.7849 avg** (0.8682 close), IR **0.7601 avg** (0.8845 close), YOLOv2 @416/256, IoU@0.5, bboxPrecisionRecall; Table 2.2 fixed (averages, per user), caveats rewritten with the KNOWN protocol + "our numbers exceed their averages" claim (caveats carry attribution). Their 120+120-clip eval split is UNPUBLISHED (GitHub repo checked) → cannot adopt; our full-corpus eval is RGB-leakage-free. MATLAB groundTruth conversion disclosure added (our converter ≠ their pipeline, originals at G:/drone/svanstrom_paired).
> **Verified + fixed:** zero Svanström in the 172k RGB corpus (dataset.yaml; thesis sentence corrected — fully OOD for every RGB variant); confuser test tally 1,754/2,633 svan (66.6%) + per-model OOD status paragraph (baseline fully OOD / hardneg+retrained = held-out in-distribution / ft4 = 2/3 fully OOD per its no-svan safeguard); bird negatives = AirBird+FBD-SV+WosDetC (per pre-finetune report D:/Downloads/Drone_detection_report.pdf, which also verified the training-recipe table value-for-value, P0.978/R0.915/mAP50 0.951 test val added).
> **Roboflow nine-set audit REMOVED ENTIRELY** (user: unverified quality): datasets row+subsection+appendix §+all Ch4 cameos (R=0.726/0.746, selcom_960 0.84, patch 2–7% OOD, IR R=0.519/0.264 brittleness) — replaced with verified in-domain evidence (retrained_v2 0.949>baseline 0.942) or honest "unmeasured on verified data" limitations. Confuser-corpus SOURCE rows (Airplane/New_Dataset Roboflow) kept.
> **New replay:** `thesis_eval/negative_frame_fire.py` → v3b fires on **1.64%** of its 1,400 negative test frames (sea/sky/building; eval `notes_negative_frame_fire`) — backs §3.1.3. Also added there: IR 0–1.7% on shared svan confuser seqs vs RGB 65–74% + 29.4% airplane-dominated IR_confusers; dv5 = dataset version 5 (HITL-graduated) explained; Ch3 opener concretised; tab:datasets +train/val/test column (ir counts verified 107,809/11,709/9,612); SelCom stream details trimmed (codec/bitrate/pixfmt cut) + "not deployed, no thermal at site" fixed in methodology too; YouTube ID column in both appendix clip tables (from _Media_ markers; 2 compilations not preserved); "open evaluation items" updated (per-cat IR breakdown now DONE).
> **Audit 46→77→83 checks ALL PASS** · 130pp, 0 errors, 0 ?? · runs/ refreshed (negative_frame_fire.json) · §2.9 "remind me" note RESOLVED.
> **NEXT:** user reviews PDF; notes file may grow (round 3 = Ch3 §3.2 onward presumably); USER items unchanged (GUI screenshots, title-page TODOs, submission ToC build).

> _previous:_

> **2026-06-12 (notes-round-1 EXECUTED — thesis now 130pp, audit 77/77):**
> User's review notes (`C:\Users\User\Desktop\thesis notes.txt`, Abstract→Ch2§9, ~50 points) were answered point-by-point in chat, planned together, and APPLIED. **More notes exist in that file beyond Ch2 — user said "that's for after/later"; NEXT SESSION starts there.**
> **New evidence (all zero-GPU replays, kb-recorded):** `thesis_eval/notes_round1_replays.py` → modality A/B under COVERAGE scoring (svan rgb-only 0.458 / ir-only 0.632 / routed+filt **0.921**; ledger `routing-beats-single-modality-coverage`), per-size px buckets (**rgb_test mlp carve-out = small-drone concentrated**, <16px R 0.78→0.26; ledger `rgbtest-carveout-small-drone`), per-category confuser fire (rgb bird 39%/heli 58%/airplane 23.4% → composed 0). `failure_profile_aggregate.py` → 282 manual seq background tags: **confuser fire is background-invariant 65–74%** (ledger `confuser-fp-background-invariant`). `gen_dataset_figures.py` → fig_datasets_pie + fig_confuser_examples + fig_confuser_fp_examples.
> **Thesis changes:** abstract rewritten (strong-baselines framing, per-stage purposes, HITL fixed-split n=9,612 + scarcity); Ch1 problem statement (corpus named, MRI-contradiction fixed, per-size + A/B numbers, SelCom = "RGB footage provided, not deployed", ft4 0.145→0.591 no-regression 0.942→0.929); RQ1 = "where retraining fails"; **GLOBAL rename verifier→(confuser) filter** (identifiers/labels untouched); Ch2 fully reworked per notes (DETR build-order honesty, imgsz+SAHI cite, Rozantsev/Schumann/Drone-vs-Bird verbatim-checked via web, taha2019 "hardly comparable" quote added, Anti-UAV @640+in-distribution disclosure, Roboflow IR numbers REMOVED, seagull clip named 0.837/0.840, cascade ≠ high-recall-first framing, sa32-vs-robust8 feature attribution, Svanström protocol = MATLAB bboxPrecisionRecall in tab caveats, +IR-confuser row); 3 NEW empirical subsections (sec:modality_ab, sec:per_size, sec:failure_profile); 2 new bib entries (akyon2022sahi, csis2025dronewar fiber-optic).
> **Provenance layer (user-requested):** top-level **`runs/`** = frozen results snapshot + README number→file→command map; new **Appendix app:provenance** mirrors it; audit script extended **46→77 checks, ALL PASS**. DECISIONS.md entry added.
> **Build: 130pp, 0 errors, 0 ?? in PDF** (one phantom "undefined references" log line with zero concrete instances — biblatex/bibtex-fallback quirk). Em-dashes: only the 2 verbatim MRI quotes (legit).
> **NEXT:** (1) read the post-Ch2 notes in thesis notes.txt → next fix round with user; (2) USER: GUI screenshots (fig:label_reviewer, fig:pyside_gui) + title-page TODOs; (3) submission build = enable \listoftables/\listoffigures + pdflatex×3+bibtex; (4) after ANY number edit: refresh runs/ copies + rerun audit (77 checks).

> _previous:_
_Updated: 2026-06-11_

> **2026-06-11 later (REPO REORGANIZED — new layout, learn these paths):**
> **All weights live in `models/` by role**: rgb detector `models/rgb/Yolo26n_selcom_confuser_ft4_1280/weights/best.pt` · ir `models/ir/corrective_finetune/finetune_v3b/weights/best.pt` · router `models/routers/robust8.joblib` · verifiers `models/verifiers/{rgb_v5/mlp_v5.pt, ir_aligned/mlp_aligned*.pt}` · patches `models/patches/`. **Renames**: `RGB model/`→`training/`, `ir_gui/`→`gui/`, label reviewer = top-level `label_reviewer/`. `eval/`+`classifier/` names frozen (import web). 117 one-off scripts swept to `archive/2026-06-11/` (7 rescued to scripts/); eval/ 99→56, classifier/ 97→32 loose. Junctions at the 5 old weight paths = backup only (rmdir to remove). `gui/flet_app/` kept (pyside imports its settings_dialog — future extraction). 449 path refs rewritten in live code; 88 kb columns re-pointed; **verified: replay + thesis build + figures + kb validate all green post-reorg**. README = the repo map. Details: DECISIONS 2026-06-11 reorg entry + `docs/analysis/2026-06-11_sweep_list.md`.
> **NEXT:** user smoke-tests `py gui/pyside_app.py` + `py label_reviewer/review_labels_gui.py`; consider committing the reorg as one commit (repo has no remotes).

> **2026-06-11 (WORKSPACE MIGRATED — you are in `ES_Drone_Thesis` now):**
> This directory is the ACTIVE workspace: a local clone of `ES_Drone_Detection` (full git history, HEAD `1dcbbbd`) + all thesis-pertinent artifacts copied over (~6 GB vs the old 48 GB). The old sibling dir is a **frozen cold archive — bit-identical, never written; anything not copied is still there** (archive/ 31.6 GB, runs/ bulk, label_reviewer/ session outputs, datasets/ training derivatives). **This repo has ZERO git remotes** — it cannot touch GitHub; the old dir keeps the `drone_detection_thesis` remote. Details in `DECISIONS.md` (2026-06-11 entry).
> **Verified working here:** thesis builds (`thesis_working.pdf`), Tier-1 replay reproduces from caches (svan cascade P=0.9494), all 15 figures regenerate from THIS dir's data, kb validate clean, production weights all present. **Fixed in this copy:** `ir_gui/fusion_settings.json` (7 weight paths) + `docs/generate_thesis_figures.py` (now repo-relative). **Still to eyeball (user):** launch `py ir_gui/pyside_app.py` once to confirm GUI loads weights.
> **Git status notes:** your ~200 pending changes carried over uncommitted, exactly as in the old dir; plus ~290 extra `M` entries = pre-existing latent EOL diffs (content-identical; they normalize whenever you commit). Commit here whenever ready — it goes nowhere (no remotes); to publish, create a NEW GitHub repo and `git remote add`.
> **NEXT (unchanged from 2026-06-10):** thesis work continues here — empirical.tex restructure, B/C/D fills, Tier-2 svanström when ready.

> **2026-06-10 (sad paths filled · unified cache running · Tier-2 re-scoped · mini-preview + completion plan delivered):**
> (1) **Part A is COMPLETE & recorded**: RGB sad-path top-up run via `eval/eval_model.py` → new evals `rgb_rgbds_baseline` (0.942), `rgb_rgbds_retrainedv2` (**0.949 in-domain — collapse is OOD-only**), `selcom_retrainedv2_1280` (**0.007**, 1 TP/295) + ledger **`rgb-collapse-ood-specific`** (the thesis hook: in-domain selection picks the detector that fails deployed); harness self-validated (baseline selcom reproduced 0.145). IR arc already complete (V2 0.503 / V5 0.737 / v3b 0.967 @640).
> (2) **`thesis_eval/` built** (both scripts recorded in scripts.csv): `pipeline_cache_unified.py` (detect-once cache; RUNNING — `antiuav.pkl` landed 01:48, 4000 fr, stride 21, **0.77 fps paired, I/O-bound GPU~2%**; remaining 8 surfaces ≈ 4–5 h; user's live process has the OLD silent code — patched version adds heartbeat + smallest-first for future runs) and `pipeline_eval_unified.py` (ZERO-GPU replay → Parts A/B/C/D + speed; per-modality `score_trust_aware`, NO union; ready — run the moment the cache lands).
> (3) **Tier-2 RE-SCOPED** (DECISIONS + §0 banner): naïve full = 52 GPU-h → **Tier-1 4k strided = FINAL for all non-sequence surfaces; Anti-UAV full DROPPED (saturated); Tier 2 = svanström paired+gray only (~8–16 h) for temporal + final svanström digits.**
> (4) **Author-facing deliverables awaiting decisions**: `docs/thesis_mini_preview.pdf` (compressed IMRAD preview of the A+D story; sign-off gates the Overleaf expansion) + `docs/analysis/2026-06-10_thesis_completion_plan.md` (the 5-beat story, coverage matrix have/in-flight/gap, 3-copies resolution → **Overleaf split = THE thesis**, frozen monoliths pending rename-OK, gap-driven run list). Open decisions: ① story green-light ② freeze-rename the 2 old copies ③ temporal-as-placeholder + Tier-2-svanström.
> **NEXT:** cache finishes → `py -u thesis_eval/pipeline_eval_unified.py` → fill B/C/D → skeleton restructure of `empirical.tex` (CUT Roboflow/real-video/cascade-video sections, ADD Part A/B/C/D, intro 4→3 contributions — can start before numbers). Side-fact to reconcile: `IR_confusers` physically 5,237 imgs vs 5,938 in datasets.csv.

> **2026-06-09 (Overleaf split + cleanup + full audit + eval-comparability):** Active thesis is now the **Overleaf-mirror folder `docs/thesis_working_distilling_overleaf/`** (`main.tex` + `chapters/{introduction,related_work,methodology,empirical,conclusion,appendices}.tex` + `figures/` + `references.bib`); single-file `thesis_working_distilling.tex` RETIRED to `archive/2026-06-08/docs/`. Build = pdflatex **~5×** from inside the folder (biblatex needs the passes; 3 under-converges to ~124pp) → **123pp, 0 errors**. Cleanups (all verified-compiling, see [[thesis-revision-active-state-pointer]]): Evidence-Ledger PURGED from visible text (~86 `\textsc{Ledger}`/bare-`\S<n>`/prose; the 160 invisible `% [source:]` comments KEPT); **em-dashes removed from prose** (~82; tables keep `& --- &`); **`\S`/§→"Section"** (226 refs); **Models appendix curated 104→16** (`tab:models_evaluated`; CBAM→0.846/15); body model-lists trimmed; **TikZ externalized to PDF** (Overleaf free-tier timeout fix); split for per-chapter `\includeonly`. **Full thesis audit** (4 agents) → `docs/analysis/2026-06-09_thesis_final_audit.md`: ~78 claims traceable-OK/0 contradictions; headline issue = **production stack never evaluated end-to-end** (abstract = sa32+patch; conclusion ships robust8+mlp); rec. narrative = **lead with scientific findings (Spine A: 28pp scoring-audit + cross-modal transfer)**; 9 backing flags (esp. **0.895@patch_thr=0.9 has no matching eval row** — only `svan_s3_sa32_thr08`=thr-0.8=0.896). **Eval comparability:** production numbers EXIST in caches (`_offline_pipeline` n=1000; `_email_recompute` full; `_routing_pipeline_cmp` robust8 n=4000) but are **NOT comparable to thesis tables** (stride/imgsz/grain differ) → created **`knowledge/THESIS_EVAL_LEDGER.md`** (§0 per-dataset eval-constraint standard + 4 stage tables) + recorded ledger `robust8-full-pipeline-cmp` (status=open). **NEXT:** user reviews audit + eval-ledger; **LOCK §0 standard** (OPEN: Anti-UAV n, RGB-confuser imgsz, RGB-in-domain n) → re-run full pipeline at standard + add `ir_dset_final`/`rgb_dataset`/`cbam` surfaces + the **patch verifier** to `eval/compare_routing_pipeline.py` → then pick thesis tables from the eval-ledger. Other docs: `2026-06-09_production_stack_cached_numbers.md`, `2026-06-04_evaluation_standardization_plan.md`.

> **2026-06-08 (number audit + IMRAD 7→5 merge):** Full per-claim number audit of `docs/thesis_working_distilling.tex` (engine `thesis_audit.py` = 1090/1093 matched + 5 parallel agents): **no fabricated or wrong body numbers.** The two scary flags were FALSE ALARMS — verified vs the May-10 ablation manifest, `R=0.072@640` IS `retrained_v2` (the ledger NOTE `prov-may10-imgsz` is the misleading bit), and `0.585` IS `selcom_960` holdout (ledger `selcom960-cross-surface-winner`). Real fixes: app:datasets confuser total 1270→1250; **`app:models` regenerated** 6→5 production (+ fixed a `gen_models_appendix.py` re-run bug — `re.error: bad escape \c` from feeding LaTeX as a regex replacement; now uses a function replacement; also added an argv path so it can target the distilling copy); L259 typo `0.9647`→`0.9654`; **23 `clf_own_holdout` dead-cites** removed from `eval=` slots (27 valid `config=` kept); 3 Roboflow eval-citations corrected (L704/789/1516); recorded missing eval `ir_v3b_antiuav640_may10` (+ config `antiuav_iou_640_may10`). Audit record: `docs/analysis/2026-06-07_number_audit.md`. Then **IMRAD 7→5**: Methodology+System Architecture → **"Methodology and System Design"**; Dataset/HITL+Experimental Results → **"Empirical Evaluation"** (the two later chapters demoted to `\section`, headings cascaded `\section→\subsection`/`\subsection→\subsubsection`, 14 `Chapter~\ref`→`Section~\ref`, Thesis-Outline + 3 chapter intros reworded). **FINAL: 131 pages, 5 body chapters + 3 appendices, 0 errors / 0 broken refs.** Cumulative this arc: **159→131 (−28) keeping every analysis.** NEXT: user reviews the `thesis_working_distilling.tex` diff → ports to `thesis_chapters.tex`/Overleaf. Going below ~128 needs cutting an analysis (declined).

> **2026-06-07 (thesis compression + figure audit):** Active file is **`docs/thesis_working_distilling.tex`** (the reframe+compression copy; NOT `thesis_working.tex`). Compressed **159→133 pages keeping every analysis** ("keep everything" path; going below ~128 needs cutting an analysis, which was declined). (1) **Figure audit** by 9 parallel agents (keep+correctness scores) → **11 redundant figures cut** (IR-MRI LDA/ANOVA/heatmap/activation cluster, fusion-PCA, fail-open 3-panel, per-frame-segment, rgb-threestance); scorecard `docs/analysis/2026-06-07_figure_audit.md`. (2) **All 7 chapters prose-tightened** (~2,500 words of redundancy/filler/table-recitation; sequential sonnet agents). (3) **2 cascade-classifier tables merged** → `tab:cascade_classifier`. (4) **4 figure correctness-fixes** applied+re-rendered (drone_size_hist `\,px`→px; fig6_6 0.959→0.961; fig4 V3→0.648; fig6_3 +τ=0.5 datum) via `gen_drone_size_hist.py`+`generate_thesis_figures.py` (run with `PYTHONIOENCODING=utf-8`). (5) **Diff VERIFIED** vs `thesis_working.tex` baseline: 419/427 distinct decimals + all 6 source tables + all cites preserved; **DETR `carion2020detr` cite restored**; only loss flagged = conclusion recap `0.585` (untraceable to a table/ledger, NOT restored — needs source if real). Build **clean: 133pp, 0 errors/broken-refs/cites** (MiKTeX full build w/ bibtex; [[thesis-build-method]]). **NEXT:** user reviews `thesis_working_distilling.tex` diff → port accepted edits to `thesis_chapters.tex`/Overleaf; OPTIONAL 7→5 IMRAD chapter merge (~−3pp, heading-cascade risk); resolve `0.585` source.

> **2026-06-05 (finalizing):** `robust8`@τ0.20 chosen as the SINGLE production trust classifier (grayscale hole closed; `robust6`/`sa32` demoted in models.csv). Also closed this session: 28pp scoring-swing + IR-Svan@640 recorded, `prov-may10-imgsz` resolved=**640**, datasets registry delivered, 7 figures traced. **EXPERIMENTS FROZEN. THESIS FINALIZATION done** (robust6→robust8 propagated; IR Svan @1280→@640 relabel; robust8 + scoring-swing + feature-selection-completion contributions written; verifier-identity resolved as per-frame-mlp_v5=production / patch=as-measured; light-IMRAD Study-Design opener + Conclusion-Validity + resolution-confound Threats; hygiene clean, audit 1088/1091). **Cleanup done:** `knowledge/datasets.csv` kb-table (8 corpora, generates datasets.md) + thesis script manifest (31 scripts, 3 newly recorded). **/sweep:** nothing flagged — the 33 superseded models are thesis baselines and EVIDENCE_LEDGER is still cited (not dead weight). **Parked (backlog 6-9):** IMRAD 9→5 restructure, 18-figure rename-map, physical datasets/ bundle, untracked-script triage, edge latency. **Next:** compile thesis_working in Overleaf; review `git diff docs/thesis_working.tex`. _(Pietro-email context below is historical.)_

- **Last goal:** Recompute the supervisor-email pipeline tables with the NEW stack (robust6 + V5 MLP)
  and "sell" the new classifier + filter to Pietro (OOD + speed + feature-efficiency).
- **Last step done:** **DONE — consolidated comparison built** (`docs/analysis/2026-06-04_consolidated_sell_new_stack.md`).
  (1) Email's 3 domains recomputed full-set under **trust-aware** scoring (Anti-UAV 85k + Svan 28.7k):
  FILTER is a clear win (**Svan rgb_filter 0.699→0.869, +17pp**), robust6 classifier −1 to −3.4pp vs old
  fusion_no_fn on paired (its value is OOD). (2) **Critical bug found+fixed:** v1 cached feats as **float16**
  → MLP P(drone) saturated to 0 → all-zero RGB filter; must be **float32** (`mlp-feats-need-f32`). (3) OOD
  filter (CNN vs MLP, FT4-isolated): MLP **78–99%** suppression vs CNN's **~52% ceiling** (rgb image+video);
  IR domain-3 MLP recall-safe. (4) **Speed:** robust6 **404× faster/frame** (0.095 vs 38.3 ms; no scene
  reads); MLP filter **~11–72× faster/det**. (5) robust6 99.8% in-domain / +30% OOD with 6/40 feats
  (AUROC+leakage). All recorded: ledger `email-recompute-robust6-mlp`, `mlp-filter-beats-cnn-ood`,
  `robust6-speed-feature-efficiency`, `mlp-feats-need-f32`; evals `svan_{classifier_robust6,rgbfilter_mlp}_ta`,
  `antiuav_classifier_robust6_ta`. New scripts: `pipeline_cache_paired`, `pipeline_eval_paired`,
  `sweep_email_thresholds`, `sweep_rgb_filter_ood`, `eval_rgb_video_ood`, `bench_speed`.
- **Next action:** YOUR CALL — (a) draft Pietro reply from the consolidated doc; (b) IR domain-3 Pareto
  *curve* (have endpoints; needs cheap per-det cache to sweep); (c) record the remaining paired cells/OOD
  suppression as eval rows if wanted (headline ones done); (d) YouTube-RGB-named-clips eval (lower priority,
  drone_video_tests already covers RGB video).
- **Open threads:** robust6 paired-recall is ~1–3pp under fusion_no_fn (its OOD win isn't on these
  benchmarks — frame accordingly); RGB V5 GUI carve-out still unwired; `mlp_v5_ir_aligned` GUI IR branch
  scaffolded; EVIDENCE_LEDGER mid-migration.

---

## Production stack (current pick)
From `models.csv` (production=yes) + EVIDENCE_LEDGER §1 / §13.6. The V5 verifier is a
**candidate** pending your sign-off.

| Slot | Choice | Notes |
|---|---|---|
| RGB detector | `ft4` (Yolo26n_selcom_confuser_ft4_1280) | confuser-hardened; `baseline` for general RGB |
| IR detector | `ir_v3b` | F1 0.967 on IR_dset_final |
| Trust classifier | `robust8`@τ0.20 (8 free feats; grayscale-hardened) | **chosen 2026-06-05** (single prod model). `robust6` = real-video-confuser-robust comparison; `sa32`/`fnfn` superseded |
| Post-detection verifier | `mlp_v5` (per-frame) — **CANDIDATE, NOT signed off** | +8-10pp Svan F1, 7.5× cleaner confuser, 46-72× faster; `patch_v2` is current shipped verifier; **v5.2 train/eval in progress** |
| Cascade | `alert_gate_only` | per-frame filtering costs ~1pp F1 |
| imgsz | 1280 (Svan + selcom), 640 elsewhere | |

## Candidate thesis contributions
From `ledger.csv` (thesis_contribution set): resolution-driven small-drone recovery; scene-fingerprint
overfitting in fusion classifiers; condition-dependent trust calibration (sa32 vs fnfn); distilled
feature-space verifier (V5, production candidate); linear separability of detector ROI features (LDA);
cross-modal grayscale fallback; patch-verifier blind spot motivating distillation.

## Open threads
- **DECISION READY: mark `docs/EVIDENCE_LEDGER.md` safe-to-archive.** Now FULLY extracted
  (116 evals, 39 findings across §1-§13 + FT4/dual-clf/distill changelogs). Remaining un-migrated bits
  are placeholders/UNKNOWNs, already logged as `status=open` findings (8 of them). Say go and I'll
  `kb.py set` it safe-to-archive (then it moves to `archive/` only on the next `/sweep`).
- **`Get-FileHash` permission** needed to resolve the `rgb_m0_baseline` orphan (is it a stale `baseline` dupe?).
- **V5 verifier / v5.2:** `mlp_v5` is production=no (candidate, not signed off); v5.2 train/eval in progress.
- **Superseded scripts ready to promote:** 12 overlap-group losers are `lifecycle=superseded` (distill_v2/3/4,
  finetune one-offs, extract_patches v1, lean13/17 trainers, eval_video, model_results_comparison_v2 nb).
  Promote to `safe-to-archive` via `kb.py set` when you want, then `/sweep`.
- **Resurrect-worthy archive (archived ≠ obsolete — do NOT sweep):** `archive/models/*/best.pt`
  (only surviving IR-version weights; `ir_v2` registered pointing there), `archive/scripts/dataset*/`
  (dataset-build lineage for IR-V5 repro), `archive/corrective_finetune/` (reusable hard-neg pipeline).
- **Verify orphan:** `rgb_m0_baseline` (`models/RGB_M0_baseline/best.pt`) — confirm not a stale dupe of `baseline`.
- **Eval-target modes without model rows (intentional):** `ir_grayscale`, `ir_raw`, `selcom_640` are
  operating-point/deployment modes, captured for comparisons but not ranked as standalone models.
- **Un-evaluated registered models:** `optimal_v1`, `lean19_v2`, `split_v2v3`, `mlp_v5_ir` have rows but no eval rows yet.

## Recently resolved
- 2026-05-30: knowledge-system design locked + built + populated (`knowledge/README.md`).
