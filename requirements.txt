# ES Drone Detection — Python requirements
# ---------------------------------------------------------------------------
# PyTorch is installed SEPARATELY (CUDA or CPU build). Pick ONE first, then
# run  pip install -r requirements.txt
#
#   GPU (CUDA 12.1):  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CPU only:         pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# ---------------------------------------------------------------------------

# --- Core: detectors + trust router + verifiers (GUI + eval replay) ---
ultralytics
opencv-python
numpy
pandas
scikit-learn
xgboost
joblib

# --- Operator GUI (PySide "TALOS") ---
PySide6
flet            # flet_app/settings_dialog.py is imported by the PySide app

# --- Figures / Model MRI / statistics ---
matplotlib
scipy
seaborn
Pillow

# --- Web API + dataset-prep helpers (optional; gui/api.py + scripts/) ---
fastapi
pydantic
uvicorn
requests
yt-dlp
tqdm
