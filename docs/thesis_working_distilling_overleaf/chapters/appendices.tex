\appendix

\chapter{Datasets in Detail}
\label{app:datasets}

This appendix consolidates the per-source / per-clip dataset metadata that the methodology chapter summarises. Numbers come from the dataset YAMLs (\texttt{rgb\_dataset/\allowbreak dataset.yaml}, \texttt{IR\_dset\_final/\allowbreak dataset.yaml}), the documentation file shipped with the confuser corpus (\texttt{rgb\_confusers\_merged/\allowbreak dataset\_documentation.md}), the Svanstr\"om paired-corpus meta file, and the YouTube extraction manifests.

\section{Composite RGB Training Corpus}

The full distribution by source prefix is given in Table~\ref{tab:ds_rgb_components}; the split policy is 80 / 10 / 10 with sequence-disjoint partitioning where the source did not already supply its own splits. Six negative-source rows (VIRAT, UA-DETRAC, BDD100K) contribute the 21.9\% background-negative fraction. The AirBird subset (10{,}000 images) is the bird hard-negative channel the baseline detector relies on; the composite corpus does not contain Svanstr\"om bird / airplane / helicopter splits, which are therefore OOD at evaluation time.
% [source: dataset=rgb_dataset; file=rgb_dataset/dataset.yaml]

\section{Confuser Hard-Negative Corpus}

Per-source composition is in Table~\ref{tab:ds_confusers}. Split policy:

\begin{itemize}
    \item \textbf{Sources with existing splits} (Airplane Roboflow, New\_Dataset Roboflow): original train / valid / test partitions preserved.
    \item \textbf{Sources without splits}: split by video / sequence ID at 80 / 10 / 10 to prevent leakage.
    \begin{itemize}
        \item Svanstr\"om: 165 video sequences split by sequence ID.
        \item raihanrsd: 58 video folders split by folder.
        \item Helicopter Kaggle: 20 hash-based pseudo-groups.
    \end{itemize}
    \item \textbf{Mislabeled frame removal}: 10 frames in the airplane / New\_Dataset sources contained visible drones and were excised before training.
\end{itemize}

\noindent The mined-hard-negative process for \texttt{retrained\_v2} ran the baseline detector on every confuser image and kept the 11{,}729 frames (43.4\%) on which the baseline fired; those mined frames were merged with the composite RGB corpus to produce the final 183{,}751-image \texttt{retrain\_dataset}. \texttt{hardneg\_v3more} differs in using a smaller, less aggressive merge.
% [source: dataset=rgb_confusers_merged; file=rgb_confusers_merged/dataset_documentation.md]

\section{IR Detector Corpus}

The per-prefix distribution is in Table~\ref{tab:ds_ir_components}. The Svanstr\"om IR partition (\texttt{svan}, 21{,}637 frames across the corpus' train/val/test splits: 17{,}314 train, 2{,}050 val, 2{,}273 test, plus 1{,}325 zoom-augmented copies under \texttt{czoom}) is the largest single Svanstr\"om appearance in any training corpus in this thesis and is the reason Svanstr\"om IR evaluation is in-distribution for the IR detector (Table~\ref{tab:svanstrom_audit}).
% [source: ls G:/drone/IR_dset_final/{train,val,test}/images svan_* counts 17314/2050/2273; czoom_svan_* 1325; thesis_eval/results/leakage_controlled.json derived] The corpus state documented here is the state at the \texttt{Final} checkpoint; the V2--V6 revision history (Section~\ref{sec:ir_evolution}) operated on smaller predecessor variants of the same source families.

\section{Thermal Confuser Corpus (\texttt{IR\_confusers})}

5{,}938 thermal frames containing aerial confusers and no drones: 4{,}281 airplane, 1{,}200 bird, and 457 helicopter frames (category encoded in the filename prefix), exported from Roboflow-hosted thermal confuser video; source streams disjoint from every \texttt{ir\_dset\_final} source family. The evaluated split holds 5{,}237 frames, sampled to 4{,}000 by even spread (3{,}043 airplane / 871 bird / 86 helicopter). All frames carry empty YOLO labels; every detection on this surface is a false positive by construction. The corpus contributes no training data to any model; it is the OOD stress surface for the IR branch (Section~\ref{sec:ds_ir_confusers}). The thermal-native head is trained with the GT-aware CBAM \emph{train} split; its held-out gates are the disjoint \emph{CBAM valid} split (drone recall recovered $0.717 \to 0.967$) and the held-out \texttt{IR\_confusers} val/test re-mine ($90 \to 22$ residual fires, $94\%$ removed; Section~\ref{sec:ir_xmodal_verifier}).

\section{Svanstr\"om Paired}

28{,}710 frames, sampled at stride~3 from 279 source video sequences. Per-class instance counts: drone 11{,}695; airplane 6{,}090; bird 5{,}298; helicopter 5{,}627. Native resolution $640{\times}512$ (RGB) with a co-registered IR channel at the same resolution. Annotators drew loose boxes around small drones, motivating the IoP@0.5 scoring rule of Section~\ref{sec:metrics}. The dataset ships as source videos with MATLAB \texttt{groundTruth} annotation objects~\cite{svanstrom2022dronedataset}; the frames and YOLO labels used here were produced by this project's own conversion script, so box geometry may differ marginally from the authors' MATLAB pipeline, and the original videos are retained alongside the converted corpus.
% [source: github.com/DroneDetectionThesis/Drone-detection-dataset; conversion = project script; originals at G:/drone/svanstrom_paired]

\section{Anti-UAV RGBT}

The converted YOLO test + val splits used for evaluation comprise 85{,}374 paired frames at $1920{\times}1080$ (RGB) and $640{\times}512$ (IR). Anti-UAV is a tracking benchmark in its native form; the per-frame detection evaluation reported in this thesis is computed by re-scoring the tracking benchmark's frame-level GT boxes with the IoU@0.5 matching rule.

\section{DUT Anti-UAV}

The DUT Anti-UAV detection dataset~\cite{zhao2022dutantiuav} ships $10{,}000$ aerial RGB images with author-provided train / validation / test partitions. It enters this thesis twice. As a \emph{training} source, $5{,}200$ of its images contribute to the composite RGB corpus (the \texttt{dut} prefix in Table~\ref{tab:ds_rgb_components}), so the production RGB detector \texttt{ft4} is in-distribution on it. As an \emph{evaluation} surface, the official test split (the $2{,}200$ frames / $2{,}245$ ground-truth drones used in Table~\ref{tab:ablation_dut}) is the confuser-free, all-drone RGB check of Section~\ref{sec:pipeline_paired}, evaluated at \texttt{imgsz=960} with the thermal detector in its grayscale-fallback mode. No DUT frame used at evaluation time was a training image for the trust router or the per-frame filters.
% [source: github.com/wangdongdut/DUT-Anti-UAV; cite=zhao2022dutantiuav; eval split = thesis_eval/results_dut/tier1_results.json]

\section{SelCom CCTV}

Single-camera source: fixed perimeter installation, $1920{\times}1080$, 25~fps. Source clip $\sim$1:30, 2{,}076 extracted frames (1{,}953 positive + 123 explicit negative). Median drone $\sqrt{\text{area}} \approx 36.8$~px in the native frame (so $\sim$12~px at \texttt{imgsz=640}, $\sim$24~px at \texttt{imgsz=1280}). The 80 / 20 train / val split (20\% pure-SelCom validation, seed~0) is preserved across the \texttt{ft1} / \texttt{mixed\_ft1} / \texttt{mixed\_ft2} / \texttt{mixed\_ft2\_1280} variants; the strongest CCTV-evaluated fine-tune is \texttt{Yolo26n\_selcom\_mixed\_ft2\_1280}, a comparison detector; the production RGB detector is \texttt{ft4} (Section~\ref{sec:production_stack}).
% [source: dataset=selcom_cctv; file=SelCom CCTV manifest (see Section~\ref{sec:ds_selcom})]

\section{YouTube Real-Video Diagnostic}

\begin{table}[htbp]
    \centering
    \caption{Drone-positive YouTube clips. ``Extracted'' is the frame count used in evaluation after stride sampling; total native frame count is shown for context. ``Source'' is the YouTube video ID (prepend \texttt{https://youtu.be/}), recovered from the extraction manifest.}
    \label{tab:youtube_drone_clips}
    \footnotesize
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lrrrrl}
        \toprule
        Clip & FPS & Total frames & Stride & Extracted & Source \\
        \midrule
        \texttt{drone\_and\_bird\_sky\_and\_trees\_short}     & 30.0 &   506 & 0 (full) & 114 & \texttt{KoUgV6WhL8g} \\
        \texttt{drone\_attacked\_by\_bird\_mountain\_side\_view} & 30.0 & 400 & 3 & 133 & \texttt{yAbMFQrt98M} \\
        \texttt{drone\_over\_mountain\_attacked\_by\_birds}   & 30.0 &   325 & 3 & 108 & \texttt{1qRERzVpRFE} \\
        \texttt{drone\_seagull\_attack}                        & 30.0 & 1{,}146 & 0 (full) & 235 & \texttt{nTdbEPxlqPM} \\
        \texttt{drone\_takeoff\_from\_ground\_and\_not\_hand\_short} & 60.0 & 1{,}379 & 0 (full) & 163 & \texttt{85XdkfWsCuI} \\
        \texttt{drone\_takeoff\_short}                         & 60.0 & 637 & 0 (full) & 116 & \texttt{Qx1Hlot9Ye8} \\
        \texttt{drone\_takeoff\_short\_trees\_background\_dji} & 60.0 & 954 & 0 (full) & 166 & \texttt{J-gcjDUjg0A} \\
        \texttt{flock\_of\_birds\_attack\_drone}              & 30.0 & 338 & 3 & 112 & \texttt{OAjZ2bgh-Xc} \\
        \texttt{flock\_of\_seagulls\_attack\_drone\_beach}    & 60.0 & 1{,}010 & 3 & 336 & \texttt{DdkFYVsXAUQ} \\
        \texttt{two\_birds\_drone}                              & 30.0 & 456 & 0 (full) & 150 & \texttt{ngMjVZfdOc\_} \\
        \midrule
        \textbf{Total} & & 7{,}151 & & \textbf{1{,}633} & \\
        \bottomrule
    \end{tabular}}
    \normalsize
\end{table}
% [source: datasets/drone detection video tests/rgb/extraction_manifest.json (IDs from the _Media_ filename marker)]

\noindent Note: the evaluated drone-positive set is the 9-clip / 1{,}359-frame / 1{,}234-GT-instance subset of this table that re-ran cleanly through the corrected-scoring pipeline (\texttt{flock\_of\_birds\_attack\_drone} excluded; some clips hold slightly fewer frames on disk than at extraction time). All Chapter~\ref{ch:hitl} video numbers, including the temporal evaluation, use that evaluated subset.

\begin{table}[htbp]
    \centering
    \caption{Confuser-only YouTube clips. All frames are confuser-positive only; every detection in these clips is a false positive by construction. ``Source'' is the YouTube video ID (prepend \texttt{https://youtu.be/}); two compilation clips were saved without the ID marker.}
    \label{tab:youtube_confuser_clips}
    \footnotesize
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lrrrrl}
        \toprule
        Clip & FPS & Total frames & Stride & Extracted & Source \\
        \midrule
        \texttt{airplanes\_airplanes\_compilation}             & 25.0 & 6{,}248 & 25 & 249 & not preserved \\
        \texttt{airplanes\_distant\_airplane\_over\_head\_flying\_away} & 29.0 & 1{,}606 & 29 & 55 & \texttt{ANbApycXbJ4} \\
        \texttt{birds\_birds\_flying\_overhead\_various\_sizes\_short} & 30.0 & 201 & 10 & 20 & \texttt{WytG-t1S6G0} \\
        \texttt{birds\_birds\_in\_slow\_motion\_flying\_various\_sizes\_compilation} & 50.0 & 13{,}550 & 50 & 271 & \texttt{ZO5lV0gh5i4} \\
        \texttt{birds\_distant\_birds\_flying\_in\_the\_sky\_short} & 30.0 & 462 & 23 & 20 & \texttt{4zMELhui9DE} \\
        \texttt{birds\_flock\_of\_birds\_flying\_short}        & 30.0 & 259 & 12 & 21 & \texttt{vmHsvO83wrk} \\
        \texttt{birds\_flock\_of\_birds\_flying\_sunset}      & 24.0 & 492 & 24 & 20 & \texttt{q0mmJrkQYaU} \\
        \texttt{helicopters\_helicopter\_compilation}          & 25.0 & 13{,}871 & 25 & 554 & not preserved \\
        \texttt{helicopters\_helicopter\_overhead\_short}      & 25.0 & 260 & 13 & 20 & \texttt{xp8oiz8ymbU} \\
        \texttt{helicopters\_helicopter\_overhead\_very\_small\_airplane\_in\_background} & --- & --- & --- & 20 & \texttt{7mPDG7wrH5I} \\
        \midrule
        \textbf{Total} & & 36{,}949 & & \textbf{1{,}250} & \\
        \bottomrule
    \end{tabular}}
    \normalsize
\end{table}
% [source: datasets/confuser_videos/extraction_manifest.json (IDs from the _Media_ filename marker)]

\noindent The 10 confuser clips were drawn from public YouTube sources visually selected to span the three confuser categories at multiple ranges and viewing angles; the video IDs above are recovered from the \texttt{\_Media\_} marker the downloader embedded in the clip filenames, preserved in \texttt{datasets/confuser\_videos/extraction\_manifest.json}. The aggregate confuser-FPPI numbers in Section~\ref{ch:experiments} use the 1{,}250-frame totals.

\section{Licensing and Ethics}

The public benchmark datasets (Anti-UAV, Svanstr\"om, the Roboflow community sets) are used under their published research licenses; the IR component sources (FLIR, DV5, CST, etc.) are similarly research-license open. The SelCom CCTV stream is deployment-partner proprietary and is not redistributable; only summary metrics and qualitative figures derived from it appear in this thesis. The YouTube clips are short, low-resolution excerpts used under fair-use for research and contain no personally identifying information; the encoded source IDs in the clip filenames are preserved so the originals can be located by an interested party. None of the corpora include human faces or location markers beyond what is visible in the original public datasets.

% ======================================================================
% APPENDIX B — GLOSSARY
% ======================================================================
% === BEGIN app:models (generated by docs/gen_models_appendix.py) ===
\chapter{Models Evaluated}
\label{app:models}

This appendix lists the models the thesis actually discusses: the production pick at each cascade stage (\textbf{production}) and the comparison or ablation variants that carry a finding in the main text. The complete experimental registry, including every intermediate, superseded, and archived checkpoint, is retained in the project repository for reproducibility. The IR detector's full six-revision evolution is reported separately in Table~\ref{tab:ir_evolution}.

\footnotesize
\begin{longtable}{p{0.27\textwidth}lp{0.53\textwidth}}
\toprule
Model & Role & Note \\
\midrule
\endhead
\multicolumn{3}{l}{\textbf{RGB detectors (YOLOv26n)}} \\
\midrule
\texttt{ft4} & \textbf{production} & SelCom-plus-confuser fine-tune; the only confuser-injection recipe that passed every drone-recall regression gate. \\
\texttt{baseline} & comparison & Stage-1 reference; best small-drone recall at \texttt{imgsz=1280}; base for the SelCom fine-tunes. \\
\texttt{hardneg\_v3more} & comparison & Hard-negative mining: lowers helicopter and airplane fire but leaves bird fire essentially unchanged. \\
\texttt{retrained\_v2} & comparison & Aggressive confuser retrain; suppresses birds but collapses Svanstr\"om drone recall to $R=0.306$. \\
\texttt{selcom\_mixed\_ft2\_1280} & comparison & CCTV (SelCom) fine-tune weights at \texttt{imgsz=1280}. \\
\midrule
\multicolumn{3}{l}{\textbf{IR detectors (YOLOv26n, thermal)}} \\
\midrule
\texttt{ir\_v3b} & \textbf{production} & Production IR detector; $F1=0.967$ on the fixed IR test split; two-epoch corrective fine-tune of \texttt{Final}. \\
\texttt{Final} & comparison & Immediate parent of \texttt{v3b}; the full V2--V6 / \texttt{Final} trajectory is in Table~\ref{tab:ir_evolution}. \\
\midrule
\multicolumn{3}{l}{\textbf{Trust classifiers (XGBoost fusion / routing)}} \\
\midrule
\texttt{robust8-nr} & \textbf{production} & Shipped trust router: the eight \texttt{robust8} features with the \texttt{reject} class dropped (always routes RGB/IR/both; the filter owns false-positive rejection). \\
\texttt{robust8} & ablation & Four-class router (\texttt{robust6} $+$ two free features $+$ a tuned per-class threshold); recommended on confuser-rich paired streams, retained as the reject-class ablation. \\
\texttt{robust6} & comparison & Six statistically-selected, leakage-free features; the base extended into \texttt{robust8}/\texttt{robust8-nr}. \\
\texttt{sa32} & comparison & Strongest hand-engineered (32-feature) classifier; best in-domain on Svanstr\"om, but leaks scene statistics so its lead is not trusted to generalise to unseen surfaces; superseded in production by \texttt{robust8-nr}. \\
\texttt{control40} & comparison & 40-feature control: best raw drone metrics but the worst confuser rejection. \\
\texttt{fusion\_no\_fn\_v1.1} & comparison & Open-world fallback; the most conservative variant on the OOD confuser zoo. \\
\midrule
\multicolumn{3}{l}{\textbf{Confuser filters}} \\
\midrule
\texttt{mlp\_v5} (\texttt{v4} build) & \textbf{production} & Production RGB filter: a distilled MLP on the detector's own \texttt{p3}+\texttt{p5} features, run per-frame; the \texttt{v4} bird-split build closes the \texttt{rgb\_dataset\_test} coverage gap ($F1\;0.792\to0.916$, recall $0.691\to0.887$). \\
\texttt{mlp\_aligned\_thermalonly} & \textbf{production} & Thermal-native IR confuser filter (CBAM-trained, @0.05); held-out CBAM recall recovered $0.717\to0.967$ at FP $6$, with $94\%$ of held-out thermal confusers removed. \\
\texttt{patch\_v2} & comparison & Superseded MobileNetV3 patch filter; audited predecessor, the \texttt{v4} RGB filter removes its last fallback role. \\
\bottomrule
\caption{Models referenced in this thesis: the production pick at each cascade stage and the comparison or ablation variants discussed in the main text. The complete experimental registry is retained in the project repository.}
\label{tab:models_evaluated}
\end{longtable}
\normalsize

% === END app:models ===

\chapter{Model MRI Sample Report}
\label{app:mri_report}

This appendix reproduces the Model MRI's human-readable report for the production RGB filter's corpus (Section~\ref{sec:model_mri}): the statistics-before-training diagnosis that licensed training the filter. The detector is \texttt{Yolo26n\_selcom\_confuser\_ft4\_1280} and the corpus is the feature-only embedding set of $19{,}334$ drone and $13{,}597$ confuser detections. The verdict, the headline diagnostic signals, and the most discriminative features are reproduced below; the same run also writes \texttt{stats.json}, the trained MLP, and nine diagnostic figures (PCA and LDA projections, per-neuron KDEs, AUROC-by-layer, the verifier ROC, and the FP-reduction curve) under \texttt{mri/results/\allowbreak v5\_report\_regen/} in the repository. Figure~\ref{fig:mri_report} reproduces the verdict block in the main text.

\paragraph{Verdict.} \emph{Classifier strongly recommended: a large false-positive cut at low recall cost.} The recommendation rests on a train-set LDA separability of $0.952$ for a projected $97\%$ false-positive cut at a $1.1\%$ recall cost. The instrument issued this verdict \emph{before any training}; the shipped filter's measured behaviour on the canonical surfaces (Section~\ref{sec:verifier_results}, Table~\ref{tab:ablation_confusers}) matches the projection, and that agreement between pre-training projection and post-training measurement is itself part of the instrument's validation.

\begin{table}[htbp]
    \centering
    \caption{Model MRI diagnostic signals for the production RGB filter's corpus ($19{,}334$ drone / $13{,}597$ confuser detections). The ``projected'' rows are the instrument's pre-training estimates of the trained filter's behaviour.}
    \label{tab:mri_signals}
    \begin{tabular}{l c p{0.5\textwidth}}
        \toprule
        Signal & Value & Meaning \\
        \midrule
        Raw hallucination rate & $54.4\%$ & bare-detector FP per confuser image (\texttt{ft4} @1280, $n{=}2{,}633$) \\
        LDA separability & $0.952$ & train-set linear split of drone vs.\ confuser \\
        Silhouette & $0.067$ & unsupervised feature-space cluster separation \\
        Max ANOVA $F$ & $42{,}346$ & strongest single discriminative feature \\
        Meta-only max AUROC & $0.811$ & best detection-metadata feature alone \\
        YOLO-feature max AUROC & $0.844$ & best learned pyramid feature alone \\
        Projected FP cut & $97.4\%$ & confusers the trained filter would reject \\
        Recall retention & $98.9\%$ & true drones the trained filter keeps \\
        Projected FP rate & $1.4\%$ & hallucination remaining after the filter \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: mri/results/v5_report_regen/report.md (+ stats.json); run=mri/cli.py]

\paragraph{Top discriminative features (by ANOVA $F$).} \texttt{p5} channel~154; \texttt{p3} channel~31 (spatial cells 0--2); \texttt{p5} channels 79, 113, 240, 85, and 43; and the detection-metadata confidence scalar. A single \texttt{p3} channel firing across three spatial cells, alongside the high-$F$ \texttt{p5} channels, is the near-binary drone/not-drone switch the filter reads; the cross-validated F1 of the MLP trained on these features is reported in Section~\ref{sec:distill_verifier}.

\chapter{Number Provenance and Reproducibility}
\label{app:provenance}

Every headline number in this thesis is the value of a named cell in a canonical results file, and every such file is reproducible by a stated command. The map lives in the project repository as \texttt{runs/README.md}; this appendix reproduces it. Three layers make the chain auditable:

\begin{enumerate}
    \item \textbf{Unified detection cache.} \texttt{thesis\_eval/pipeline\_cache\_unified.py} runs the production detectors \emph{once} per evaluation surface and stores, per frame, every detection (box, confidence, the 517-D feature vector, patch-classifier probability), the trust classifier's input features, and the per-modality ground truth. Caches are tagged with the detector weight identity.
    \item \textbf{Deterministic replays.} Every evaluation in Chapter~\ref{ch:hitl} is a CPU re-score of that cache (no detector forward pass), so any reader with the repository can re-derive any table in about a minute per surface. The replay scripts and their outputs are listed in Table~\ref{tab:provenance}.
    \item \textbf{Automated audit.} \texttt{thesis\_eval/\_audit\_headline\_numbers.py} asserts, cell by cell, that the numbers printed in this document equal the values in the canonical results files; the submission build of this thesis passes that audit.
\end{enumerate}

\begin{table}[htbp]
    \centering
    \caption{Headline-number provenance: thesis claim $\to$ canonical results file (under \texttt{runs/}, frozen copies of \texttt{thesis\_eval/results/}) $\to$ reproduction command. All \emph{pipeline} replay commands are zero-GPU; the two rows marked \texttt{(GPU)} (the IR-detector trajectory and stage latencies) require the detectors and target hardware.}
    \label{tab:provenance}
    \footnotesize
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{p{4.4cm}p{4.6cm}p{4.6cm}}
        \toprule
        Thesis number & File (cell) & Reproduce \\
        \midrule
        Svanstr\"om composed F1 $0.742 \to 0.946$ (shipped \texttt{robust8-nr}, \texttt{filt$\to$clf}) & \texttt{results\_noreject/\allowbreak tier1\_results.json} (\texttt{svanstrom.B\_pipeline}) & \texttt{pipeline\_eval\_unified.py --only svanstrom} \\
        Confuser fire $30.4\% \to 1.4\%$ (shipped \texttt{robust8-nr}; $0.11\%$ \texttt{robust8} ablation) & \texttt{results\_noreject/\allowbreak tier1\_results.json} (\texttt{rgb\_confuser.C\_confuser}) & \texttt{pipeline\_eval\_unified.py --only rgb\_confuser} \\
        Anti-UAV no-harm $0.973 \to 0.984$ & \texttt{tier1\_results.json} (\texttt{antiuav.B\_pipeline}) & \texttt{pipeline\_eval\_unified.py --only antiuav} \\
        Grayscale 3-way $0.607/0.187/0.580$ & \texttt{tier1\_results.json} (D table) & \texttt{pipeline\_eval\_unified.py} \\
        Temporal 2-of-3 windows (Table~\ref{tab:temporal_production}) & \texttt{temporal\_results.json} & \texttt{temporal\_replay.py} \\
        Per-size buckets (Table~\ref{tab:per_size}) & \texttt{notes\_round1\_results.json} (\texttt{SZ\_per\_size}) & \texttt{notes\_round1\_replays.py} \\
        Low-conf mode (Table~\ref{tab:lowconf_selcom}) & \texttt{conf\_sweep/\allowbreak conf\_sweep\_results.json} & \texttt{conf\_sweep\_replay.py} \\
        IR HITL trajectory $0.503 \to 0.967$ & \texttt{knowledge/evals.csv} (\texttt{ir\_final\_*}, one fixed split) & \texttt{eval/ir\_version\_comparison.py} (GPU) \\
        Stage latencies (Table~\ref{tab:speed}) & \texttt{knowledge/ledger.csv} (speed rows) & \texttt{eval/bench\_speed.py} (GPU) \\
        Scoring swing $2.8$~pp (Section~\ref{sec:scoring_audit}) & \texttt{notes\_round1\_results.json} vs \texttt{tier1\_results.json} (routed \texttt{+filt} / \texttt{clf->filt}) & \texttt{notes\_round1\_replays.py} \\
        Overlap audit counts (Table~\ref{tab:svanstrom_audit}) & \texttt{clean\_split/README.md} $+$ \texttt{leakage\_controlled.json} & \texttt{leakage\_controlled\_replay.py} \\
        Clean-split results (Table~\ref{tab:clean_split}) & \texttt{clean\_split/\allowbreak clean\_split\_results.json} (sequence lists alongside) & cache \texttt{--only svanstrom\_clean,\allowbreak antiuav\_clean --full}, then \texttt{pipeline\_eval\_unified.py} \\
        \bottomrule
    \end{tabular}}
\end{table}

The \texttt{runs/} directory additionally holds the production IR detector's complete Ultralytics training record (\texttt{runs/corrective\_finetune/}: training arguments, loss and PR curves, confusion matrices, and acceptance reports for the \texttt{v3b} fine-tune), so the training side of the production stack is documented alongside the evaluation side. The relational evidence store \texttt{knowledge/} (CSV tables with generated views) records every evaluation and finding as a row carrying its source path and reproduction command.
% [source: runs/README.md (this appendix mirrors it)]

\chapter{Glossary of Abbreviations and Shorthand}
\label{app:glossary}

\begin{description}
    \item[C-UAS] Counter-Unmanned Aerial Systems. The defensive use-case context for this thesis.
    \item[UAV] Unmanned Aerial Vehicle, used interchangeably with ``drone'' for consumer-class quadcopters in this thesis.
    \item[YOLO] You Only Look Once. The single-stage object-detection family~\cite{redmon2016yolo}; this thesis uses YOLOv26n via the Ultralytics framework~\cite{ultralytics2024}.
    \item[CNN] Convolutional Neural Network. The patch filter is a MobileNetV3-class CNN~\cite{howard2019mobilenetv3}.
    \item[HITL] Human-in-the-Loop. The dataset / model co-development protocol of Section~\ref{sec:hitl_ir}.
    \item[OOD] Out-Of-Distribution. Imagery drawn from a different population than the model's training distribution.
    \item[FPPI] False Positives Per Image. The per-frame false-alarm count, used as the primary metric on confuser-only evaluation surfaces.
    \item[FPR] False Positive Rate. In this thesis: the frame-level fraction of confuser-only frames with at least one detection.
    \item[IoU] Intersection over Union. The standard detection-matching rule; used for Anti-UAV scoring.
    \item[IoP] Intersection over Prediction. The matching rule used for Svanstr\"om RGB scoring (Section~\ref{sec:metrics}); $\text{IoP} = |B_\text{pred} \cap B_\text{gt}| / |B_\text{pred}|$.
    \item[ft4] \texttt{Yolo26n\_selcom\_confuser\_ft4\_1280}. The production RGB detector (SelCom$+$confuser fine-tune); the only confuser-injection config that passed every drone-recall regression gate (Section~\ref{sec:training_recipes}).
    \item[robust6] The six-feature trust classifier (per-modality detection confidence $+$ box geometry), its features screened by the scene-fingerprint ratio (Section~\ref{sec:feature_selection}); the base that \texttt{robust8} extends.
    \item[robust8] The four-class trust classifier (Section~\ref{sec:feature_selection}): \texttt{robust6} plus two further free features and a tuned per-class decision threshold; retained as the reject-class ablation and recommended on confuser-rich paired streams.
    \item[robust8-nr] The shipped production trust router (Section~\ref{sec:classifier_results}): the eight \texttt{robust8} features with the \texttt{reject} class dropped, so it always routes RGB/IR/both and the per-frame filter owns false-positive rejection.
    \item[sa32] \texttt{scene\_aware\_\allowbreak v3more\_\allowbreak 32feat}. The strongest hand-engineered comparison trust classifier (32-feature XGBoost); it leaks scene statistics (discriminates \emph{which scene} a frame came from, not \emph{whether} its detections are trustworthy), so its in-domain lead is not trusted to generalise, and it is superseded in production by \texttt{robust8-nr} (Section~\ref{sec:feature_selection}).
    \item[fnfn] \texttt{fusion\_no\_fn\_v1.1}. The open-world fallback trust classifier (40-feature).
    \item[v3b] The production IR detector weights: 2-epoch corrective fine-tune on top of the \texttt{Final} IR checkpoint.
    \item[\texttt{mlp\_v5}] The distilled feature-space confuser filter (Section~\ref{sec:distill_verifier}): an MLP on the detector's fused \texttt{p3}+\texttt{p5} features that supersedes the v2 patch filter on confuser-rich surfaces and runs per-frame; production is the \texttt{v4} bird-split build.
    \item[IR filter] The thermal-native IR confuser filter (Section~\ref{sec:ir_xmodal_verifier}): a single head (\texttt{mlp\_aligned\_thermalonly}, CBAM-trained, @0.05) trained directly on thermal confuser crops.
    \item[selcom\_1280] The SelCom CCTV RGB fine-tune \texttt{Yolo26n\_\allowbreak selcom\_\allowbreak mixed\_\allowbreak ft2\_1280} evaluated at \texttt{imgsz=1280}; a comparison/ablation detector (the production RGB detector is \texttt{ft4}, Section~\ref{sec:production_stack}).
    \item[selcom\_640] The same SelCom weights evaluated at \texttt{imgsz=640} (resolution-ablation variant).
    \item[\texttt{imgsz}] Ultralytics short for input letterbox size. Drives small-object recall (Section~\ref{sec:resolution_arch}).
\end{description}

\chapter{Superseded Components and Design History}
\label{app:design_history}

This appendix collects the figures \emph{and narratives} documenting components and operating points that were superseded in the production pipeline, gathered here so the main text of Chapter~\ref{ch:hitl} stays focused on the shipped system.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{fig_robust8_operating_point}
    \caption{Design-history: the grayscale \texttt{trust\_rgb} hole was a decision-rule problem inside the four-class router, not a feature-space limit. Sweeping the per-class \texttt{trust\_rgb} threshold $\tau$ trades precision for recall along a well-separated curve; the reject-class \texttt{robust8} operated at $\tau{=}0.20$. The shipped \texttt{robust8-nr} drops the reject class and removes this knob entirely (the hole cannot form without a \texttt{reject} label). Source: \texttt{eval/compare\_routing\_pipeline.py}.}
    \label{fig:robust8_operating}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{fig8_failopen_expanded}
    \caption{Design-history: the recall/precision frontier on Svanstr\"om for the no-retrain fail-open gate (with the original \texttt{rgb\_confusers} reference, red, versus a reference expanded with held-out Svanstr\"om clutter, green), against the full-veto operating point (star) and the bare detector (square). This gate was the candidate fix \emph{before} the bird-split re-mine closed the coverage gap directly; it is retained only as the record of why a calibration patch was inferior to fixing the training coverage. Source: \texttt{eval/failopen\_expanded\_ref.py}.}
    \label{fig:failopen_expanded}
\end{figure}


\section{Patch Filter (Superseded Predecessor)}
\label{sec:patch_verifier_arch}

This section documents the \emph{predecessor} confuser filter, retained because part of the historical evidence was measured against it; the production filter is \texttt{mlp\_v5\_v4} (Section~\ref{sec:distill_verifier}).

The patch filter is a 4-class image classifier (\texttt{airplane} / \texttt{helicopter} / \texttt{bird} / \texttt{other}) on a MobileNetV3-Small backbone~\cite{howard2019mobilenetv3}, chosen because it fits within a YOLO-forward-pass budget on the GTX 1050 Ti development hardware. It was trained on \textbf{45{,}917 RGB+IR patches}: drone crops taken from the detector training corpora's ground-truth boxes, and bird/airplane/helicopter crops hard-negative-mined from detector false positives over the confuser corpus (Section~\ref{sec:ds_confusers}) and OOD YouTube video (\texttt{classifier/extract\_patches\_v2.py}); no CCTV crops are included. Per-modality twins are trained (\texttt{confuser\_filter4\_\{rgb,ir\}\_v2}). On its own validation split it reaches accuracy $0.975$, with per-class precision/recall of $0.907/0.971$ (airplane), $0.988/0.938$ (helicopter), and $0.893/0.712$ (bird); birds were already the weakest class at training time, foreshadowing the deployment gap.
% [source: models.csv patch_v2 (train_dataset=45,917 patches); cache=classifier/runs/patches/confuser_filter4_rgb_metrics.json]

At inference the filter consumes the YOLO crop and emits a softmax over the four classes; the veto uses $p_\text{confuser} = \max(p_\text{airplane}, p_\text{helicopter}, p_\text{bird})$ thresholded at \texttt{patch\_thr}. The \texttt{other} class is an explicit OOD outlet: crops that look neither like a trained confuser nor like a drone route to \texttt{other} instead of being forced into a confuser class. Four numbered versions exist; v2 is the audited production version (v3 over-vetoes and is never shipped; v4 ties v2). Its threshold sweep, per-category catch rates, and the cost analysis that motivated its replacement are in Section~\ref{sec:verifier_results}.
% [source: ledger=patch-version-ranking,patch-catch-below-bar]

\section{Alert-Gate Cascade (superseded patch path)}
\label{sec:alert_gate}

This section describes the MobileNetV3 \emph{patch} filter's position in the predecessor design, retained because part of the historical evidence was measured under it. Alert-gating was forced by the patch filter's cost (a second CNN over crop pixels): the filter was consulted only when the temporal smoother was about to emit an alert. Two alternative cascades were considered and rejected at the time (\texttt{filter\_then\_classifier} and \texttt{classifier\_then\_filter} as \emph{per-frame patch} stages), because the patch CNN's per-frame cost and its veto of marginal drone TPs cost $\approx$1~pp Svanstr\"om F1. The per-frame \texttt{mlp\_v5\_v4} filter removes the cost argument entirely, and the composition-order question is re-answered with it in Section~\ref{sec:pipeline_ablation}. The production GUI path is implemented in \texttt{ir\_gui/pyside\_engine.py} (\texttt{should\_suppress\_alert}).


\section{Superseded Confuser Filter (Patch CNN)}

\paragraph{The patch generation: useful, gated, and bounded.}
The patch filter's operating point was set by a threshold sweep on Svanstr\"om (stride 9, 3{,}190 frames): at \texttt{patch\_thr}$=$0.9 it catches $\sim$63\% of the classifier stage's residual confuser FPs at $-4.0$~pp drone F1, the production choice of its era; at $0.5$ the recall cost doubles. Its veto distribution is bimodal (confusers $\gtrsim 0.9$, drone TPs near $0$), which is what makes the threshold a usable knob.
% [source: ledger=patch-catch-below-bar; eval=svan_s3_sa32_thr08; config=svan_iop_1280_s9]

% tab:patch_audit removed (duplicate of fig:patch_catchbar, which shows the same per-bucket catch rates and the 5.4\% drone-TP veto).

% fig:patch_catchbar moved to Appendix~\ref{app:design_history} (design-history consolidation).

The audit's verdict (every confuser bucket below the decisiveness bar, airplanes worst, and a hard distribution bound: on SelCom-scale crops it is bit-for-bit the bare detector) is what motivated replacing pixel re-processing with feature reuse.
% [source: ledger=patch-catch-below-bar,patch-v2-neutral-selcom; eval=patch_catch_v2_svan,v5_selcom_patch]

\section{Trust-Classifier Design History}

\paragraph{The grayscale hole, and why the reject class caused it.}
\texttt{robust6} inherits one honest weakness from the four-class formulation: in the grayscale-IR fallback the thermal features go uninformative, so drone-only-in-RGB frames are routed to \texttt{reject} and grayscale recall collapses. \texttt{robust8} patched this \emph{within} the reject formulation by adding \texttt{rgb\_mean\_conf} and a per-class threshold $\tau$ on $P(\text{trust\_rgb})$: at $\tau=0.20$ it lifts Svanstr\"om-grayscale recall $0.577 \to 0.681$ (Figure~\ref{fig:robust8_operating}), at the structural cost of $7$--$10\times$ real-video-confuser fire on the grayscale slice ($0.006 \to 0.046$), because \texttt{rgb\_mean\_conf} reads as a drone on Svanstr\"om but as a confident bird on real-motion video. Dropping the reject class removes the hole at its source: a router that never rejects cannot route a grayscale drone to \texttt{reject}, so \texttt{robust8-nr} needs neither the $\tau$ dial nor the precision/recall trade the figure illustrates. The sweep is retained as the design-history record of why the reject formulation was awkward to operate.
% [source: ledger=robust8-grayscale-router; eval=svan_gray_robust8_t20_clffilter,video_confuser_robust8_t20_clffilter; run=eval/compare_routing_pipeline.py; cache=eval/results/_routing_pipeline_cmp/comparison.md]

% fig:robust8_operating moved to Appendix~\ref{app:design_history} (design-history consolidation).

\paragraph{A dual-classifier extension (validated, not shipped).}
Splitting the router into a paired model and a separate grayscale model strictly dominates single \texttt{sa32} on missing-modality cases (RGB-fallback bird-confuser drone $F1$ $0.247\to1.000$; \texttt{rgb\_dataset\_test} $0.690\to0.960$) with paired-core performance unchanged; grayscale airplane/helicopter cases remain weak ($F1\approx0.50$). It is recorded as a validated extension rather than the shipped default; \texttt{robust8}'s two extra features close most of the same gap without a second model.
% [source: ledger=dual-classifier-v3; eval=dualclf_v3_vs_sa32; config=dual_clf_v3_surfaces]

\paragraph{Design evolution: the OOD confuser zoo (predecessor configuration).}
The first quantitative router comparison ran the predecessor cascade (baseline RGB, alert-gated patch v2, \texttt{imgsz=1280}) over the confuser zoo. The open-world \texttt{fusion\_no\_fn\_v1.1} suppressed the bare $52.1\%$ fire to $1.6\%$ at the classifier stage ($0.8\%$ after the filter); \texttt{sa32} reached only $20.5\%$ ($10.3\%$), a $13\times$ gap on this surface.
% [source: ledger=cascade-confuser-collapse; eval=clfzoo_fnfn,clfzoo_sa32; run=eval/cumulative_halluc.py; cache=eval/results/_cumulative_halluc/confuser_fusion_no_fn_model_v1.1/summary.json; config=confuser_zoo_1280 (predecessor configuration)]

% tab:classifiers removed (design-evolution; the surface reversal is shown in fig:classifier_reversal and described in the surrounding paragraphs).

% fig:classifier_reversal moved to Appendix~\ref{app:design_history} (design-history consolidation).

\paragraph{The surface reversal.}
On real video the ranking inverted: \texttt{sa32} dominated segment drone $F1$ ($0.826$ baseline RGB) while \texttt{fusion\_no\_fn\_v1.1} accepted only about one in eight of the correct RGB drone TPs (segment $R=0.128$; seven of eight drone events missed) and \texttt{control\_v3more\_40feat} trailed \texttt{sa32} by 18~pp with no compensating win anywhere. The Svanstr\"om/zoo ordering does not select the production classifier; the operational surface does. \texttt{fusion\_no\_fn\_v1.1} is retained as the open-world fallback for deployments where missed drones cost less than false alarms. This reversal is what discredited hand-ranking classifiers on single benchmarks and motivated the statistical selection that produced \texttt{robust6}/\texttt{robust8} above. Even \texttt{sa32}'s real-video win is not bankable: its scene-statistic leakage (Section~\ref{sec:feature_selection}) lets it score partly by memorising surface identity, so its lead is not trusted to generalise beyond the surfaces measured.
% [source: ledger=three-classifier-realvideo; eval=pipe3clf_sa32,pipe3clf_fnfn; config=pipe_video_drone_iop]

\chapter{Full Per-Surface Ablation Tables}
\label{app:full_ablation}

The body reports the production pipeline in aggregate (Table~\ref{tab:ablation_overview}); this appendix gives the full per-surface ablation behind it, including every comparison arm (the reject-class \texttt{robust8}, \texttt{robust6}, the hand-engineered \texttt{sa32}, and the superseded patch filter). All cells replay the same unified detection cache (Section~\ref{sec:unified_cache}); the comparison arms are discussed in the component deep-dives of Chapter~\ref{ch:hitl}.

\begin{table}[htbp]
    \centering
    \caption{Full-pipeline ablation, Svanstr\"om paired ($n=4{,}000$ frames, even-strided across the full 28{,}710-frame set, not the first $4{,}000$; RGB @1280 / IR @640, IoP@0.5). \emph{RGB only}/\emph{IR only} score that detector alone against its own ground truth; every row below the second rule is trust-aware and \emph{sums both modalities} (RGB-side scored vs RGB GT $+$ IR-side vs IR GT), so its TP/FP/FN base is both modalities' drones combined, not the frame count. \texttt{clf} $=$ $+$trust router; \texttt{filt} $=$ $+$per-frame filter; \texttt{clf$\to$filt} $=$ router then filter. The shipped production cell is \texttt{filt$\to$clf[robust8-nr]} (filter then the no-reject router; \texttt{robust8} with the reject class is the adjacent ablation, Section~\ref{sec:classifier_results}); the pipeline \emph{raises} recall over bare ($0.948\to0.991$) while adding $\sim$29~pp precision. \texttt{clf$\to$filt} is the same router in the other order (Section~\ref{sec:classifier_results} on why \texttt{filt$\to$clf} is shipped).}
    \label{tab:ablation_svanstrom}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lcccccl}
        \toprule
        cell & TP & FP & FN & $P$ & $R$ & F1 [95\% CI] \\
        \midrule
        RGB only (\texttt{ft4}, vs RGB GT)   & 1532 & 1845 & 141 & 0.454 & 0.916 & 0.607 [0.590--0.623] \\
        IR only (\texttt{v3b}, vs IR GT)     & 1610 &  174 &  31 & 0.903 & 0.981 & 0.940 [0.931--0.949] \\
        \midrule
        bare (both detectors)        & 3142 & 2019 & 172 & 0.609 & 0.948 & 0.742 [0.730--0.754] \\
        filt only (\texttt{mlp\_v5}, RGB)    & 3043 &  337 & 271 & 0.900 & 0.915 & 0.908 [0.900--0.914] \\
        filt only (\texttt{aligned}, IR)     & 3142 & 2018 & 172 & 0.609 & 0.948 & 0.742 [0.730--0.754] \\
        filt only (patch)            & 3004 &  782 & 310 & 0.793 & 0.907 & 0.846 [0.836--0.855] \\
        clf only [robust8-nr]        & 3136 & 1997 &  27 & 0.611 & 0.992 & 0.756 [0.745--0.768] \\
        clf only [robust8]           & 3092 &  354 &  31 & 0.897 & 0.990 & 0.941 [0.935--0.947] \\
        clf only [robust6]           & 3132 &  286 &  34 & 0.916 & 0.989 & 0.951 [0.946--0.957] \\
        clf only [sa32]              & 3137 &  187 &  30 & 0.944 & 0.991 & 0.967 [0.962--0.971] \\
        clf$\to$filt [robust8-nr]    & 3037 & 314 & 126 & 0.906 & 0.957 & 0.931 [0.923--0.937] \\
        clf$\to$filt [robust8]       & 2993 & 187 & 130 & 0.941 & 0.955 & 0.948 [0.943--0.954] \\
        clf$\to$filt [sa32]          & 3039 &  162 & 128 & 0.949 & 0.960 & 0.955 [0.949--0.960] \\
        \textbf{filt$\to$clf [robust8-nr]} & 3037 & 317 & 29 & \textbf{0.905} & \textbf{0.991} & \textbf{0.946 [0.938--0.950]} \\
        filt$\to$clf [robust8]       & 2996 &  191 &  35 & 0.940 & 0.989 & 0.964 [0.957--0.968] \\
        \bottomrule
    \end{tabular}}
\end{table}
% [source: thesis_eval/results/tier1_results.json (svanstrom B_pipeline); run=thesis_eval/pipeline_eval_unified.py]

\begin{table}[htbp]
    \centering
    \caption{Full-pipeline ablation, Anti-UAV paired ($n=4{,}000$ frames, even-strided across the 85{,}374-frame set, both modalities @640, IoU@0.5). Row conventions as in Table~\ref{tab:ablation_svanstrom} (\emph{RGB/IR only} score one detector vs its own GT; the trust-aware rows sum both modalities). The no-harm control: every pipeline cell is within $\sim$1~pp of bare or above it, the shipped no-reject \texttt{robust8-nr} included (this saturated surface rarely rejects, so dropping the reject class changes nothing here).}
    \label{tab:ablation_antiuav}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lcccccl}
        \toprule
        cell & TP & FP & FN & $P$ & $R$ & F1 [95\% CI] \\
        \midrule
        RGB only (\texttt{ft4}, vs RGB GT)   & 3657 &  41 &  68 & 0.989 & 0.982 & 0.985 [0.982--0.988] \\
        IR only (\texttt{v3b}, vs IR GT)     & 3775 & 133 & 173 & 0.966 & 0.956 & 0.961 [0.956--0.966] \\
        \midrule
        bare (both detectors)        & 7432 & 174 & 241 & 0.977 & 0.969 & 0.973 [0.970--0.976] \\
        filt only (\texttt{mlp\_v5}, RGB)    & 7430 & 172 & 243 & 0.977 & 0.968 & 0.973 [0.970--0.976] \\
        filt only (\texttt{aligned}, IR)     & 7432 & 173 & 241 & 0.977 & 0.969 & 0.973 [0.970--0.976] \\
        filt only (patch)            & 7430 & 174 & 243 & 0.977 & 0.968 & 0.973 [0.970--0.976] \\
        clf only [robust8-nr]        & 7430 & 160 &  81 & 0.979 & 0.989 & 0.984 [0.981--0.986] \\
        clf only [robust8]           & 7408 & 141 &  93 & 0.981 & 0.988 & 0.984 [0.982--0.987] \\
        clf only [robust6]           & 7384 & 156 & 170 & 0.979 & 0.978 & 0.978 [0.975--0.981] \\
        clf only [sa32]              & 7426 & 135 &  75 & 0.982 & 0.990 & 0.986 [0.984--0.988] \\
        clf$\to$filt [robust8-nr]    & 7428 & 157 & 83 & 0.979 & 0.989 & 0.984 [0.982--0.987] \\
        clf$\to$filt [robust8]       & 7406 & 139 & 95 & 0.982 & 0.987 & 0.984 [0.982--0.987] \\
        clf$\to$filt [sa32]          & 7424 & 134 &  77 & 0.982 & 0.990 & 0.986 [0.984--0.988] \\
        \textbf{filt$\to$clf [robust8-nr]} & 7428 & 157 & 81 & \textbf{0.979} & \textbf{0.989} & \textbf{0.984 [0.982--0.987]} \\
        filt$\to$clf [robust8]       & 7406 & 139 &  93 & 0.982 & 0.988 & 0.985 [0.982--0.987] \\
        \bottomrule
    \end{tabular}}
\end{table}
% [source: thesis_eval/results/tier1_results.json (antiuav B_pipeline); run=thesis_eval/pipeline_eval_unified.py]

\begin{table}[htbp]
    \centering
    \caption{Full-pipeline ablation, DUT Anti-UAV \emph{test split} ($n=2{,}200$ frames, $2{,}245$ GT drones, RGB \texttt{ft4} $+$ grayscale-IR \texttt{v3b}, both @960, IoU@0.5, trust-aware). The grayscale-IR channel is the IR detector's no-thermal \emph{fallback} mode (thermal model on grayscale RGB), not its native thermal performance. The full test split is used (not strided). Row conventions as in Table~\ref{tab:ablation_svanstrom} (\emph{RGB/IR only} score one detector vs its own GT; the trust-aware rows sum both modalities). Confuser-free (all-drone): filters can only delete true positives, so here the pipeline buys precision, not FP suppression. \texttt{filt$\to$clf[robust8-nr]} (the shipped no-reject router) is the production configuration; dropping the reject class recovers the recall \texttt{robust8} surrenders on this clean surface (at \texttt{filt$\to$clf}, $R\,0.574\to0.800$, F1 $0.702\to0.835$).}
    \label{tab:ablation_dut}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lcccccl}
        \toprule
        cell & TP & FP & FN & $P$ & $R$ & F1 [95\% CI] \\
        \midrule
        RGB only (\texttt{ft4}, vs RGB GT)            & 1917 & 104 &  328 & 0.949 & 0.854 & 0.899 [0.889--0.908] \\
        grayscale-IR only (\texttt{v3b}, vs IR GT)    & 1113 & 375 & 1132 & 0.748 & 0.496 & 0.596 [0.576--0.616] \\
        \midrule
        RGB \texttt{ft4} $+$ grayscale-IR \texttt{v3b} & 3030 & 479 & 1460 & 0.864 & 0.675 & 0.758 [0.747--0.769] \\
        filt only (\texttt{mlp\_v5}, RGB)        & 2820 & 443 & 1670 & 0.863 & 0.620 & 0.722 [0.715--0.739] \\
        filt only (\texttt{aligned}, IR)         & 2796 & 418 & 1694 & 0.871 & 0.626 & 0.728 [0.715--0.736] \\
        filt only (patch)            & 2931 & 470 & 1559 & 0.862 & 0.653 & 0.743 [0.732--0.754] \\
        clf only [robust8-nr]        & 3024 & 464 &  522 & 0.867 & 0.853 & 0.860 [0.850--0.869] \\
        clf only [robust8]           & 2586 & 302 & 1301 & 0.895 & 0.665 & 0.763 [0.749--0.778] \\
        clf only [robust6]           & 2415 & 374 & 1808 & 0.866 & 0.572 & 0.689 [0.674--0.705] \\
        clf only [sa32]              & 2651 & 430 & 1480 & 0.860 & 0.642 & 0.735 [0.721--0.750] \\
        clf$\to$filt [robust8-nr]    & 2558 & 372 & 988 & 0.873 & 0.721 & 0.790 [0.781--0.803] \\
        clf$\to$filt [robust8]       & 2234 & 247 & 1653 & 0.900 & 0.574 & 0.701 [0.686--0.717] \\
        clf$\to$filt [sa32]          & 2265 & 361 & 1866 & 0.863 & 0.548 & 0.670 [0.655--0.686] \\
        \textbf{filt$\to$clf [robust8-nr]} & 2558 & 372 & 638 & \textbf{0.873} & \textbf{0.800} & \textbf{0.835 [0.827--0.849]} \\
        filt$\to$clf [robust8]       & 2182 & 237 & 1564 & 0.901 & 0.574 & 0.702 [0.691--0.725] \\
        \bottomrule
    \end{tabular}}
\end{table}
% [source: thesis_eval/results_dut/tier1_results.json (dut_antiuav_960 A_bare + B_pipeline); run=thesis_eval/pipeline_eval_unified.py; ledger=dut-resolution-pipeline]

\begin{table}[htbp]
    \centering
    \caption{Confuser-surface FP reduction. RGB confusers: $n=2{,}633$ (full test split) @640. IR confusers: $n=4{,}000$ of 5{,}237 (even spread) @640. Fire $=$ fraction of frames with $\geq$1 surviving detection. The shipped \texttt{filt$\to$clf[robust8-nr]} equals \texttt{filt only (mlp)} (and equals \texttt{clf$\to$filt[robust8-nr]} to the digit, since a no-reject router never vetoes a confuser frame, so composition order is immaterial here); the \texttt{robust8} ablation reaches $0.0011$ (RGB) by adding the reject net, the suppression the shipped stack trades for recall.}
    \label{tab:ablation_confusers}
    \begin{tabular}{lcc}
        \toprule
        cell & RGB confusers & IR confusers \\
        \midrule
        bare                      & 0.304 [0.287--0.321] & 0.294 [0.280--0.309] \\
        clf only [robust8-nr]     & 0.304 [0.287--0.321] & 0.294 [0.280--0.309] \\
        clf only [robust8]        & 0.049 [0.041--0.057] & 0.273 [0.259--0.287] \\
        clf only [robust6]        & 0.035 [0.028--0.043] & 0.223 [0.209--0.235] \\
        clf only [sa32]           & 0.003 [0.001--0.005] & 0.263 [0.249--0.277] \\
        filt only (mlp)           & 0.014 [0.010--0.019] & 0.028 [0.023--0.033] \\
        filt only (patch)         & 0.102 [0.091--0.114] & 0.246 [0.233--0.260] \\
        \textbf{filt$\to$clf [robust8-nr]} & \textbf{0.014 [0.010--0.019]} & \textbf{0.028 [0.023--0.033]} \\
        clf$\to$filt [robust6]    & 0.0011 [0.000--0.003] & 0.019 [0.014--0.024] \\
        clf$\to$filt [robust8]    & 0.0011 [0.000--0.003] & 0.024 [0.019--0.030] \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: thesis_eval/results/tier1_results.json (rgb_confuser, ir_confusers C_confuser)]

\begin{table}[htbp]
    \centering
    \caption{Solo-surface ablation (single modality vs own GT). IR test split: $n=4{,}000$ of 9{,}612; RGB test split: $n=4{,}000$ of 17{,}209; SelCom: full 311. The \texttt{clf} rows show the router on a single live channel: the reject-class \texttt{robust8} collapses recall (it routes against a dead second modality), while the shipped no-reject \texttt{robust8-nr} degrades gracefully to the bare detector, which is why single-modality deployments can run the full router. The filter's in-distribution recall cost is the honest counterweight to its OOD benefit.}
    \label{tab:ablation_solo}
    \begin{tabular}{llccc}
        \toprule
        surface & cell & $P$ & $R$ & F1 [95\% CI] \\
        \midrule
        IR test split & bare (\texttt{v3b})   & 0.942 & 0.981 & 0.961 [0.955--0.967] \\
        IR test split & clf [robust8]         & 0.947 & 0.923 & 0.935 [0.928--0.942] \\
        IR test split & clf [robust8-nr]      & 0.942 & 0.981 & 0.961 [0.955--0.967] \\
        IR test split & filt (aligned, thermal) & 0.945 & 0.939 & 0.942 [0.934--0.949] \\
        IR test split & filt (patch)          & 0.949 & 0.931 & 0.940 [0.933--0.947] \\
        RGB test split & bare (\texttt{ft4})  & 0.954 & 0.899 & 0.926 [0.919--0.933] \\
        RGB test split & clf [robust8]        & 0.981 & 0.383 & 0.551 [0.532--0.570] \\
        RGB test split & clf [robust8-nr]     & 0.954 & 0.899 & 0.926 [0.919--0.933] \\
        RGB test split & filt (mlp)           & 0.960 & 0.887 & 0.922 [0.915--0.929] \\
        RGB test split & filt (patch)         & 0.957 & 0.832 & 0.890 [0.881--0.898] \\
        SelCom val & bare (\texttt{ft4})      & 0.858 & 0.451 & 0.591 [0.532--0.639] \\
        SelCom val & clf [robust8]            & 0.881 & 0.125 & 0.220 [0.161--0.276] \\
        SelCom val & clf [robust8-nr]         & 0.858 & 0.451 & 0.591 [0.531--0.639] \\
        SelCom val & filt (mlp)               & 0.950 & 0.451 & 0.612 [0.551--0.659] \\
        SelCom val & filt (patch)             & 0.858 & 0.451 & 0.591 [0.532--0.639] \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: thesis_eval/results/tier1_results.json (ir_dset_final, rgb_dataset_test, selcom_val S4_verifier)]

