\chapter{Empirical Evaluation}
\label{ch:hitl}

This chapter answers the three research questions, building from the components up to the system. The component deep-dives (Section~\ref{sec:components_eval}) examine each stage in turn --- the RGB detector's three training stances, the IR detector's six-revision HITL case study, the per-detection confuser filter, and the trust-classifier comparison --- each isolated on the job it owns, and including the design-evolution evidence measured under earlier configurations, always labelled as such. The Model MRI findings (Section~\ref{sec:mri_findings}) then show \emph{why} those components work: the discrimination they exploit is already linearly present in the detectors' own features. The full-pipeline ablation (Section~\ref{sec:pipeline_ablation}) composes them, isolating what every stage contributes on every canonical surface under the single protocol of Section~\ref{sec:eval_protocol}, with temporal (segment-level) evidence on the consecutive-frame video surface (Section~\ref{sec:temporal_results}). The grayscale cross-modal finding (Section~\ref{sec:grayscale}), threats to validity, and limitations close the chapter. Every table states its $n$; headline cells carry $95\%$ bootstrap confidence intervals in brackets: the central $95\%$ range of the metric over $1{,}000$ resamples of the evaluation frames drawn with replacement, so two cells whose intervals do not overlap differ by more than sampling noise.

% ======================================================================
\section{Component Deep-Dives}
\label{sec:components_eval}

\subsection{RGB Detector}
\label{sec:rgb_results}

\begin{table}[htbp]
    \centering
    \caption{RGB variants on Svanstr\"om @ \texttt{imgsz=1280} (IoP@0.5, drone class; full 28{,}710 frames, the pre-protocol full-set run, internally consistent). Aggressive retraining destroys recall on small Svanstr\"om drones while leaving its own in-domain test performance intact.}
    \label{tab:rgb_comparison}
    \begin{tabular}{lccccc}
        \toprule
        Model & Drone $P$ & Drone $R$ & Bird halluc & Airplane halluc & Heli halluc \\
        \midrule
        baseline           & 0.940 & \textbf{0.961} & 94.4\% & 74.6\% & 66.2\% \\
        \texttt{hardneg\_v3more}  & 0.941 & 0.950 & 94.2\% & 64.7\% & 41.9\% \\
        \texttt{retrained\_v2}    & 0.943 & 0.306 & \textbf{3.4\%}  & \textbf{5.6\%}  & \textbf{4.5\%}  \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: ledger=retrainedv2-recall-collapse; eval=rgb_svan_baseline,rgb_svan_hardneg,rgb_svan_retrainedv2; run=eval/diagnose_failures_all.py; cache=eval/results/_failure_diagnosis/svanstrom_1280_by_category.csv; config=svan_iop_1280 (full set)]

The three stances trace the lever that motivated the whole architecture (Section~\ref{sec:rgb_three_variants}): hard-negative mining is class-specific (helicopters yield, birds do not), and the one stance that suppresses birds collapses drone recall. The dedicated resolution sweep (Section~\ref{sec:resolution_arch}, Figure~\ref{fig:resolution}) puts the \texttt{imgsz=640} regime in within-model terms: the baseline loses $28$~pp of recall at 640 and \texttt{retrained\_v2} drops further from its already-collapsed level. (That sweep reports \texttt{retrained\_v2} at $R=0.323$ at 1280 against the $0.306$ here: the same already-collapsed level measured on its stride-7 harness, $n=4{,}102$, rather than this full $28{,}710$-frame set, a sampling difference of $1.7$~pp, not an inconsistency.)
% [source: eval/results/svan_resolution_sweep.json; run=eval/svan_resolution_sweep.py] The collapse does not show in-domain: on the composite corpus's \emph{own} test split the same weights score $F1=0.949$, above the baseline's $0.942$, and on SelCom they total-collapse to $F1=0.007$ (1~TP of 295~GT). In-domain evaluation alone would have selected the detector that fails deployed; this is the sharpest single argument for the OOD surfaces the protocol mandates.
% [source: ledger=retrainedv2-recall-collapse,rgb-collapse-ood-specific; eval=rgb_rgbds_baseline,rgb_rgbds_retrainedv2,selcom_retrainedv2_1280; config=rgb_dataset_iou_640,selcom_iop_1280]

\begin{table}[htbp]
    \centering
    \caption{SelCom CCTV fine-tune; IoP@0.5, conf=0.25, val split 311~imgs / 295~GT. Doubling \texttt{imgsz} doubles recall \emph{and} raises precision: a resolution win, not a trade.}
    \label{tab:selcom}
    \begin{tabular}{lcccccc}
        \toprule
        Model & imgsz & TP & FP & FN & $P$ & $R$ \\
        \midrule
        baseline (\texttt{Yolo26n\_trained})        & 640  &   2 &  6 & 293 & 0.250 & 0.007 \\
        baseline                                    & 1280 &  26 & 37 & 269 & 0.413 & 0.088 \\
        \texttt{Yolo26n\_selcom\_mixed\_ft2}        & 640  &  72 & 50 & 223 & 0.590 & 0.244 \\
        \textbf{\texttt{Yolo26n\_selcom\_mixed\_ft2\_1280}} & \textbf{1280} & \textbf{138} & \textbf{43} & \textbf{157} & \textbf{0.762} & \textbf{0.468} \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: ledger=selcom-imgsz-win; eval=selcom_ft2_1280; run=RGB model/finetune_selcom.py; cache=runs/rgb_finetune_eval/Yolo26n_selcom_mixed_ft2_1280/comparison.json; config=selcom_iop_1280]

At \texttt{imgsz=960}, \texttt{selcom\_960} is the best SelCom-holdout drone-first RGB variant tested ($F1=0.585$ on the hold-out); it is deliberately not in the production stack (it loses to the baseline on Svanstr\"om medium-range drones) but is recorded as the deployment operating point for this camera.
% [source: ledger=selcom960-cross-surface-winner; eval=xsurf_selcom_holdout; config=selcom_holdout_persize]

\paragraph{Real-video cameo: the three-stance continuum generalises.}
On the 19-clip diagnostic (per-clip table in Appendix~\ref{app:datasets}), the three stances occupy the same continuum (\texttt{retrained\_v2} high-precision/low-recall at $P=0.909, R=0.453$; \texttt{selcom\_1280} high-recall/low-precision at $P=0.649, R=0.812$; baseline between), and no single RGB variant dominates across scenes. The full six-mode table (including the IR-on-grayscale rows it introduced) is Table~\ref{tab:realvideo_master} in Section~\ref{sec:grayscale}.
% [source: ledger=no-pareto-dominance-video; eval=vid_drone_baseline; config=video_drone_iop]

\subsection{IR Detector: the Six-Revision HITL Case Study}
\label{sec:ir_evolution}

The IR detector was developed across five numbered revisions (V2 through V6) plus a \texttt{Final} retrain and a \texttt{v3b} corrective fine-tune, by the loop of Section~\ref{sec:hitl_method}. Earlier per-version best-epoch mAP50 numbers are not directly comparable because the val split changed with the dataset. Every version was therefore re-evaluated on the same fixed \texttt{IR\_dset\_final} test split at \texttt{imgsz=640} (script: \texttt{eval/eval\_ir\_versions.py}).

\begin{table}[htbp]
    \centering
    \caption{IR detector versions evaluated on the fixed \texttt{IR\_dset\_final} test split @ \texttt{imgsz=640}. Comparable across versions; supersedes the per-version best-epoch mAP narrative. \texttt{v3b} is the production weight.}
    \label{tab:ir_evolution}
    \begin{tabular}{lcccccc}
        \toprule
        Version & mAP50 & mAP50-95 & $P$ & $R$ & F1 & Status \\
        \midrule
        V2     & 0.458 & 0.219 & 0.661 & 0.406 & 0.503 & superseded \\
        V3     & 0.571 & 0.242 & 0.648 & 0.579 & 0.611 & superseded \\
        V4     & 0.722 & 0.393 & 0.895 & 0.669 & 0.765 & superseded \\
        V5     & 0.694 & 0.446 & 0.768 & 0.709 & 0.737 & superseded (regression) \\
        V6     & 0.956 & 0.571 & 0.921 & 0.941 & 0.931 & superseded \\
        Final  & \textbf{0.977} & \textbf{0.602} & 0.955 & \textbf{0.980} & 0.967 & base for prod \\
        \textbf{v3b (production)} & 0.972 & 0.591 & \textbf{0.957} & 0.977 & \textbf{0.967} & current \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{fig4_ir_evolution}
    \caption{IR detector P/R trajectory across the five numbered revisions (V2--V6) plus the \texttt{Final} retrain and production \texttt{v3b}, on the fixed \texttt{IR\_dset\_final} test split. The V5 regression (precision $0.895 \to 0.768$) is recovered by V6 and the production \texttt{v3b}.}
    % [source: ledger=ir-version-progression; eval=ir_final_v3b; run=eval/ir_version_comparison.py; cache=eval/results/ir_version_comparison/ir_comparison_test_640_2026-05-16T20-04-39.csv; config=ir_final_640]
    \label{fig:ir_evolution}
\end{figure}

The trajectory reads in four passes. \emph{V2 $\to$ V3}: a narrower curated subset trades a little precision for a large recall gain ($0.406 \to 0.579$), the premise for the mining cycles that follow. \emph{V3 $\to$ V4}: precision jumps $0.648 \to 0.895$ with recall barely moving, the signature of an FP-mining cycle. \emph{V5, the regression}: precision drops to $0.768$ because a large positive batch was injected all at once, bypassing the review loop. The un-reviewed batch carried unlabelled drones; V5 fires on them and is scored as FP. The model did not get worse; the dataset state did, which is precisely the coupling Section~\ref{sec:coevolution} warns about. \emph{V6/\texttt{Final}}: corpus cleanup recovers and surpasses ($F1=0.967$). \texttt{Final} and \texttt{v3b} are indistinguishable on this split; \texttt{v3b} is retained as production because its 2-epoch corrective fine-tune addresses deployment failures (GUI regressions, hot-pixel scenes) not represented here.
% [source: ledger=ir-version-progression; eval=ir_final_v2..ir_final_v3b; run=eval/ir_version_comparison.py; cache=eval/results/ir_version_comparison/ir_comparison_test_640_2026-05-16T20-04-39.csv; config=ir_final_640]

\paragraph{OOD bound.} The IR detector's strength is in-distribution. Its off-distribution \emph{recall} on independent thermal drone footage is untested on verified data (exploratory runs on community thermal sets suggested substantial degradation, but those sets' label quality could not be verified and their numbers are not reported); this is a stated limitation (Section~\ref{sec:threats}), and one of the reasons the RGB detector is retained as a parallel channel rather than treating thermal as primary.

\subsection{Confuser Filter}
\label{sec:verifier_results}
\label{sec:patch_threshold}
\label{sec:patch_audit}

The production filter supersedes a MobileNetV3 \emph{patch} CNN; that predecessor's audit (every confuser bucket below the $0.90$ decisiveness bar, airplanes worst, and bit-for-bit identical to the bare detector on CCTV-scale crops) motivated the feature-reuse design and is collected in Appendix~\ref{app:design_history}.

\paragraph{The feature-reuse generation.}
\begin{table}[htbp]
    \centering
    \caption{Feature-reuse filter (the production \texttt{mlp\_v5\_v4}) vs the MobileNetV3 patch filter (v2), both on the FT4 RGB detector. $F1$ is drone-class (IoP@0.5 on Svanstr\"om/SelCom, IoU@0.5 on Anti-UAV/\texttt{rgb\_dataset}); \emph{halluc} is the confuser hallucination rate. \texttt{mlp\_v5\_v4} wins every confuser-rich surface and, with the bird-split re-mine, also closes the prior \texttt{rgb\_dataset} carve-out ($0.792\to0.916$), now leading the patch filter there too. (The Svanstr\"om bare $F1$ shown here, $0.596$, is on this table's stride-9 comparison sample; the canonical even-strided $n{=}4{,}000$ value is $0.607$, Table~\ref{tab:ablation_svanstrom} --- a sampling difference, not an inconsistency.)}
    \label{tab:distill_verifier}
    \begin{tabular}{lcccccc}
        \toprule
        & \multicolumn{3}{c}{Drone $F1$} & \multicolumn{3}{c}{Halluc. rate} \\
        \cmidrule(lr){2-4} \cmidrule(lr){5-7}
        Surface & bare FT4 & + patch v2 & + \texttt{mlp\_v5} & bare & patch v2 & \texttt{mlp\_v5} \\
        \midrule
        Svanstr\"om (IoP@0.5)      & 0.596 & 0.768 & \textbf{0.861} & 0.470 & 0.163 & \textbf{0.044} \\
        Anti-UAV (IoU@0.5)         & 0.986 & 0.986 & 0.984          & 0.011 & 0.011 & \textbf{0.012} \\
        SelCom CCTV (IoP@0.5)      & 0.591 & 0.591 & \textbf{0.612} & 0.071 & 0.071 & \textbf{0.022} \\
        \texttt{rgb\_dataset} (IoU)& \textbf{0.929} & 0.904 & \textbf{0.916} & 0.028 & 0.026 & \textbf{0.029} \\
        confuser-only              & ---   & ---   & ---            & 0.317 & 0.107 & \textbf{0.021} \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: ledger=v5-beats-patch,v5-rgbds-ceiling; eval=v5_svan_mlp,v5_rgbds_mlp,v5_confuser_mlp; run=eval/eval_v4_vs_patch.py; config=svan_iop_1280_s9,rgb_dataset_iou_640,confuser_test_640]

% fig:distill_verifier_bar removed (review item: the bar chart duplicates tab:distill_verifier on the same page; the table is kept).

\paragraph{The carve-out, diagnosed and then closed.}
\label{sec:mlp_recall_drop}
The earlier \texttt{mlp\_v5} filter regressed on the photo-style \texttt{rgb\_dataset} split to $F1=0.792$ ($-11$~pp), driven by a drone recall collapse ($0.899 \to 0.691$). The Model MRI localised why: the falsely-vetoed drones were not low-confidence (confidence-scalar difference between vetoed and kept: $0.000$). They were \emph{smaller}, with a distinct deep-feature signature, and sat \emph{farther from the confuser distribution than the kept drones did} (centroid distance $16.5$ vs $11.1$) while being far from the filter's training-drone cluster ($15.4$). The filter rejected them because they did not resemble its training drones, not because they resembled birds: an OOD \emph{coverage} gap, not a structural overlap.
% [source: ledger=mlp-v5-recall-drop-is-ood-coverage; run=eval/diagnose_mlp_recall_drop.py; cache=eval/results/_offline_pipeline/cache]

That diagnosis prescribed its own fix, and the production filter implements it. \texttt{mlp\_v5\_v4} re-mines the filter's training corpus with the missing manifold filled in: a size$\times$source-balanced drone re-sample (small \texttt{rgb\_dataset} drones from the \texttt{train}$+$\texttt{val} splits, the cells the original stride-8 quota left empty) plus an in-distribution bird-confuser split, the same diverse-data lever that made the IR filter recall-safe. The coverage gap closes directly: \texttt{rgb\_dataset\_test} recall recovers $0.691 \to 0.887$ (held out; F1 $0.792 \to 0.916$, Table~\ref{tab:distill_verifier}) without retreating on any other RGB drone surface, and the per-size cells confirm the recovery is exactly where the gap was (Table~\ref{tab:per_size}, sub-32~px). The earlier no-retrain fail-open gate ($k$-NN distance to the confuser reference) is therefore superseded: it recovered $91$--$100\%$ of the vetoed drones at a $5$--$10\%$ confuser leak but backfired on cluttered Svanstr\"om (precision $0.887 \to 0.631$), and is retained only as design-history (Figure~\ref{fig:failopen_expanded}) for the operating point the re-mine made unnecessary.
% [source: ledger=filter-stack-v4-thermalonly-promoted; provenance=docs/analysis/2026-06-18_filter_provenance_train_heldout.md]

\paragraph{Held-out bird suppression (TEST split).}
The RGB filter \texttt{mlp\_v5\_v4} is \emph{trained} on the v2 balanced corpus ($19{,}334$ drone / $13{,}597$ confuser: balanced \texttt{rgb\_dataset} drones from \texttt{dataset/\{train,val\}} $+$ Anti-UAV \texttt{val}/RGB; pure-SelCom mined crops with the $311$ \texttt{selcom\_val} files blocklisted; \texttt{rgb\_confusers\_merged/\{train,val\}}) \emph{plus the bird.v1i TRAIN split} ($728$ of $1{,}212$ images, $60/40$ seed-0 name split). It is \emph{evaluated} on the held-out \textbf{bird.v1i TEST split} ($484$ unseen images, $230$ bird fires): \texttt{mlp\_v5\_v4} keeps only $30/230$ ($13\%$) where the shipped \texttt{mlp\_v5} kept $91/230$ ($40\%$), so the production filter \emph{beats} its predecessor on \emph{unseen} birds (AUROC $0.981$). The four cached drone/confuser surfaces above are genuine disjoint test splits (\texttt{rgb\_dataset\_test}, \texttt{antiuav\_rgb}, \texttt{selcom\_val} by blocklist, \texttt{rgb\_confuser}); the Svanstr\"om RGB numbers are read as a shipped-vs-candidate $\Delta$. The single residual cost is $+5$ non-bird FP on the cached \texttt{rgb\_confuser} surface (Table~\ref{tab:ablation_confusers}, $29\to39$).
% [source: provenance=docs/analysis/2026-06-18_filter_provenance_train_heldout.md; eval=eval/eval_birdtest_heldout.py]

% fig:failopen_expanded moved to Appendix~\ref{app:design_history} (design-history consolidation).

\paragraph{The thermal-native IR filter.}
\label{sec:grayscale_verifier}
The production IR filter is a single thermal-native head (\texttt{mlp\_aligned\_thermalonly}), trained directly on thermal confuser crops (Section~\ref{sec:ir_xmodal_verifier}). It is evaluated on a held-out gate: CBAM, a novel thermal aerial-confuser set excluded from its training.

\begin{table}[htbp]
    \centering
    \caption{Thermal-native deploy filter (\texttt{mlp\_aligned\_thermalonly}, \texttt{conf}$=$0.40, \texttt{thr}$=$0.05) vs the bare IR detector; FP in parentheses. CBAM is held out of training. Trained directly on thermal confuser crops, the filter cuts held-out novel-confuser FPs $48\to6$ ($-88\%$) \emph{and} recovers the thermal drone recall the earlier grayscale-harvested filter collapsed (CBAM recall $0.967$, no collapse); its only cost is $-3.7$~pp recall on the genuinely airplane-like \texttt{ir\_dset} drones no threshold separates. The MobileNetV3 patch filter on the same held-out CBAM cuts only $7$ FP.}
    \label{tab:ir_aligned}
    \begin{tabular}{lrccc}
        \toprule
        Surface & $n$ & bare IR (\texttt{v3b}) $P/R/F1$ (FP) & + aligned MLP $P/R/F1$ (FP) & $\Delta R / \Delta F1$ \\
        \midrule
        \textbf{CBAM (held out)}  & 180  & $0.547/0.967/0.699$ (48) & $\mathbf{0.905/0.967/0.935}$ (\textbf{6}) & $0.000 / \mathbf{+0.236}$ \\
        \texttt{ir\_dset\_final}  & 4806 & $0.965/0.965/0.965$ (109) & $0.969/0.928/0.948$ (98) & $-0.037 / -0.017$ \\
        \texttt{ir\_video} test   & 831  & $0.909/0.977/0.942$ (80) & $0.909/0.977/0.942$ (80) & $0.000 / 0.000$ \\
        Anti-UAV test             & 4269 & $0.983/0.942/0.962$ (68) & $0.983/0.942/0.962$ (68) & $0.000 / 0.000$ \\
        \midrule
        \emph{patch filter on CBAM} & 180 & --- & $0.564/0.883/0.688$ (41) & (cut only 7 FP) \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: ledger=ir-grayscale-harvest-solves-thermal-filter; eval=ir_aligned_cbam_heldout,ir_aligned_irdset_final; run=mri_train_aligned,eval_run_aligned_full; config=cbam_ir_640,ir_final_640]

Two properties define the thermal head: a drone-diversity re-mine ($\sim$8k thermal drones from Svanstr\"om IR, Anti-UAV \texttt{val}/IR, \texttt{IR\_dset\_final}/\texttt{train}, \texttt{IR\_video}/\texttt{train}, plus GT-aware CBAM-\texttt{train} drones) keeps it recall-safe on held-out thermal, and \emph{thermal-native} confuser crops (\texttt{IR\_confusers}/\texttt{train}, category$\times$size-balanced, plus GT-aware CBAM-\texttt{train} confusers) provide its rejection signal: this is the change that closed the airplane gap the earlier filter could not. Held-out generalisation is genuine: on the \texttt{IR\_confusers} \texttt{val}$+$\texttt{test} re-mine ($388$ disjoint fires), the thermal head keeps only $22$ ($94\%$ removed) against the shipped filter's $90$ ($77\%$); ir\_dset\_final, antiuav\_ir, and ir\_video are all evaluated on their \texttt{test} splits (Svanstr\"om IR drone recall holds flat at $0.966$). This held-out $90\to22$ is distinct from the on-cache \texttt{IR\_confusers} fire in Table~\ref{tab:ablation_confusers}, whose surface is the filter's own \texttt{train} split. The airplane category remains the relatively weakest, now as an in-distribution recall trade rather than an open suppression hole.
% [source: ledger=filter-stack-v4-thermalonly-promoted; provenance=docs/analysis/2026-06-18_filter_provenance_train_heldout.md; eval=eval/eval_ir_heldout.py]

\paragraph{Operating points: the recall/false-positive dial.}
Each filter's threshold is a tunable operating point, not a fixed property of the network. Figure~\ref{fig:filter_operating} sweeps the per-detection $P(\text{drone})$ threshold of both production filters, plotting drone-detection recall (the fraction of true-drone detections retained, pooled over that filter's drone surfaces) against the confuser fire-rate on the matching confuser surface. Two shapes emerge. The RGB \texttt{mlp\_v5\_v4} sits on a flat high-recall shoulder: at the shipped $0.25$ it keeps $95.6\%$ of true-drone detections at a $\sim$1\% confuser fire-rate, and stays recall-transparent ($\ge 97\%$) well below that, so its recall is essentially free to recover. The thermal-native head now \emph{suppresses} thermal confusers (the airplane gap the earlier filter could not reach): its fire-rate falls with the threshold rather than staying flat, and the residual is the genuinely-ambiguous airplane-like fraction the filter keeps recall-safe rather than a threshold artefact (Section~\ref{sec:limits}). The reading for a deployer is that the shipped points sit deliberately on the high-precision side. Where the limit is the threshold (RGB), recall is recoverable almost for free by lowering it; where the limit is coverage (the small in-distribution airplane-like thermal fraction), lowering helps only partially and at a steeper false-positive cost. The shipped configuration chooses precision where confusers dominate; a recall-first deployment moves left along these curves.
% [source: run=eval/filter_operating_sweep.py; cache=eval/results/filter_operating_sweep.json; surfaces per tab:ablation_confusers]

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{fig_filter_operating}
    \caption{Filter operating points (detection level). For each production confuser filter, drone detection recall (blue, left axis; fraction of GT-matched true-drone detections retained, pooled over the drone surfaces) and confuser fire-rate (red dashed, right axis; matching confuser surface) as the per-detection $P(\text{drone})$ threshold varies; the shipped point ($t=0.25$ RGB, $0.05$ IR-thermal) is marked. RGB sits on a flat high-recall shoulder; the thermal-native head now suppresses thermal confusers (fire falls with the threshold; the residual is the airplane-like in-distribution fraction). Source: \texttt{eval/filter\_operating\_sweep.py}.}
    \label{fig:filter_operating}
\end{figure}

% ======================================================================
\subsection{Trust Classifier}
\label{sec:classifier_results}
\label{sec:classifier_compare}

The production router is the no-reject \texttt{robust8-nr}; this section reports the design path that produced it, from statistical feature selection through the reject-class variants to the shipped no-reject form. The lineage is one feature set with one decision rule changed. Feature selection (Section~\ref{sec:feature_selection}) produced the six-feature \texttt{robust6} and its eight-feature extension \texttt{robust8}, both \emph{four-class} routers that can label a frame \texttt{reject} (trust neither modality). The shipped router keeps those eight features but \emph{drops the reject class}, delegating all false-positive removal to the per-frame filter. The reject-class variants flank each other in the pipeline ablation (Tables~\ref{tab:ablation_svanstrom},~\ref{tab:ablation_confusers}) exactly as the feature statistics predicted. On the measured surfaces \texttt{sa32} actually edges the statistical generation, both in-domain and on raw confuser suppression (clf-only RGB-confuser fire $0.003$ vs \texttt{robust6}'s $0.035$). But it does so by \emph{leaking scene statistics} --- discriminating which scene a frame came from rather than whether its detections are trustworthy (Section~\ref{sec:feature_selection}) --- so its lead is not trusted to generalise to unseen surfaces. It also costs $404\times$ more per frame and was trained against a superseded detector's calibration. The statistical \texttt{robust6}/\texttt{robust8} generation is therefore preferred not because it suppresses more, but because it is leakage-free, cheap, and re-mined on the production \texttt{ft4}/\texttt{v3b} detectors; the shipped \texttt{robust8-nr} then drops the reject class, trading a little of \texttt{robust8}'s confuser suppression for the recall it recovers on clean, single-modality, and degraded-channel surfaces.

% [The earlier ``statistical generation, validated end-to-end'' paragraph + tab:robust6_pipeline were removed: that 5,000-frame alert ablation predated the ft4/v3b re-mine and its sa32-vs-robust6 ordering contradicted the canonical Tables~\ref{tab:ablation_svanstrom}/\ref{tab:ablation_confusers}. The canonical comparison is the paragraph above plus those tables.]

\paragraph{The shipped router: dropping the reject class.}
The reject class is a liability on any surface that is not a confuser-rich paired stream. When a modality is absent (single-camera deployment), degraded (the grayscale-IR fallback), or the surface is simply clean (DUT), \texttt{reject} fires on recoverable drone frames and recall collapses: clf-only $R\,0.383$ on the RGB test split, $0.125$ on SelCom, and a $24\%$ reject rate on confuser-free DUT (Tables~\ref{tab:ablation_solo},~\ref{tab:ablation_dut}). \texttt{robust8-nr} removes the class at its source: it retrains the eight-feature \texttt{robust8} with the neither-correct rows dropped, so the router always routes to RGB, IR, or both (argmax, no threshold) and the per-frame filter owns all false-positive rejection. This recovers the lost recall in full (composed router$+$filter F1, the only configuration deployed: DUT $0.701\to0.790$, RGB test split $0.551\to0.922$, SelCom $0.220\to0.612$; mean across drone surfaces $0.744\to0.850$; the no-reject router suppresses no confusers on its own, so these are never router-only numbers) and dissolves two artefacts of the reject class at once: the grayscale recall \emph{hole} (below) and the per-class threshold $\tau$ that patched it. The cost is confuser fire the reject net no longer compounds away: RGB-confuser frame fire $0.0011\to0.014$ (still near $1\%$, and damped by temporal voting, Section~\ref{sec:temporal_results}), thermal $0.024\to0.028$, real-video-confuser $0.098\to0.236$. We ship the no-reject router and keep \texttt{robust8} as the ablation that quantifies the reject net; on confuser-rich \emph{paired} streams (the Svanstr\"om flagship), where both modalities are live and a \texttt{reject} is rarely wrong, \texttt{robust8} remains the stronger choice (at the shipped \texttt{filt$\to$clf} order, $0.964$ vs $0.946$) and is the recommended configuration there. (The composed-F1 recovery figures above compare \texttt{robust8} and \texttt{robust8-nr} at a fixed \texttt{clf$\to$filt} composition, isolating the reject variable; the shipped \texttt{filt$\to$clf} order, chosen in Section~\ref{sec:pipeline_paired}, lifts the no-reject numbers further still, e.g.\ Svanstr\"om $0.931\to0.946$ and DUT $0.790\to0.835$.)

The classifier's design history --- the grayscale \texttt{trust\_rgb} hole and its threshold patch, a validated dual-classifier extension, the predecessor-configuration confuser-zoo comparison, and the real-video surface reversal that discredited single-benchmark ranking and motivated the statistical feature selection --- is collected in Appendix~\ref{app:design_history}.

\section{Model MRI Findings}
\label{sec:mri_findings}

The instrument is described in Section~\ref{sec:model_mri}; this section reports what it found. The through-line: the discrimination the pipeline needs already exists inside the detectors' feature spaces, linearly accessible, in both modalities. That is why the production filters are 517-D MLPs rather than CNNs, and why the trust classifier needs eight features rather than thirty-two.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.6\textwidth}\includegraphics[width=\textwidth]{fig8_mri_lda}\caption{}\end{subfigure}
    \begin{subfigure}{0.39\textwidth}\includegraphics[width=\textwidth]{fig8_mri_anova}\caption{}\end{subfigure}
    \caption{Model MRI of the FT4 detector's fused \texttt{p3}+\texttt{p5} features on the shipped $32{,}931$-detection corpus. (a) The linear-discriminant projection separates drone (blue) from confuser (red) into two cleanly-resolved peaks at $\approx 95\%$ single-threshold accuracy. (b) Per-block ANOVA $F$-statistic: \texttt{p5} is the dominant discriminative layer (max $F=42{,}346$, median $657$), ahead of \texttt{p3} and the detection metadata; the confidence scalar ranks only 6th.}
    % [source: ledger=mri-v5-report-regen,v5-lda-separability; eval=distill_cv_mlp; run=mri/cli.py; cache=mri/results/v5_report_regen/stats.json; config=distill_cv_5fold]
    \label{fig:mri_stats}
\end{figure}

\paragraph{The signal is linear, supervised, and deep.}
On the RGB detector, a single Fisher axis separates drone from confuser at $\approx$95\% threshold accuracy while unsupervised PCA finds near-zero cluster structure (silhouette $0.067$): the signal is real but invisible to distance-based methods, which is why a small trained MLP is the right reader. The strongest single discriminator is a \texttt{p5} semantic channel ($F=42{,}346$), $4\times$ stronger than the detection-confidence scalar: the filter reads \emph{what the detector saw}, not \emph{how sure it was}. The IR detector is the thermal twin: LDA $0.981$ over $14{,}697$ drone / $1{,}386$ confuser detections, strongest neuron $F=5{,}370$, top discriminators again \texttt{p5} channels with \texttt{meta:conf} far down the ranking.
% [source: ledger=v5-lda-separability,ir-features-separable; run=mri/cli.py; cache=mri/results/v5_report_regen/stats.json,mri/results/ir_v3b_report]

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{\textwidth}\includegraphics[width=\textwidth]{fig8_mri_act_drone}\caption{Drone (Svanstr\"om, conf$=0.72$)}\end{subfigure}
    \\[4pt]
    \begin{subfigure}{\textwidth}\includegraphics[width=\textwidth]{fig8_mri_act_confuser}\caption{Confuser (\texttt{rgb\_confusers} test, conf$=0.29$)}\end{subfigure}
    \caption{Model MRI spatial ``brain scan'' of the top discriminative neurons. Each row: the detection crop, the \texttt{p3} activation (stride~8, fine spatial detail), and the \texttt{p5} activation (stride~32, semantic depth). On the drone (a) \texttt{p3} binds to the airframe and \texttt{p5} forms a centred semantic blob; on the confuser (b) the same neurons fire diffusely across background structure. This activation-signature difference is what the feature-reuse filter reads.}
    \label{fig:mri_activation}
\end{figure}

\paragraph{The instrument audits its own paper trail.}
Re-running the MRI on the shipped V5 corpus corrected two stale figures and one headline claim in an earlier hand-written report (the dominant discriminator is \texttt{p5}, not the confidence score). The same diagnostic pattern (localise the failure in feature space, prescribe the recall-safe fix) recovered both the RGB carve-out diagnosis (Section~\ref{sec:verifier_results}) and the IR filter (Section~\ref{sec:ir_xmodal_verifier}).
% [source: ledger=mri-v5-report-regen; run=mri/cli.py; cache=mri/results/v5_report_regen/stats.json]

% ======================================================================
\section{Full-Pipeline Ablation}
\label{sec:pipeline_ablation}
\label{ch:experiments}

\paragraph{Stack under test.}
The production stack: RGB detector \texttt{ft4}, IR detector \texttt{v3b}, the no-reject trust router \texttt{robust8-nr}, and the two per-frame confuser filters, composed in the filter-then-router order (\texttt{filt$\to$clf}; the design is detailed in Section~\ref{sec:classifier_results}). The router carries \emph{no} reject class: false-positive rejection is delegated to the per-frame filter, the better tool for it (per-detection confuser discrimination on the detector's own internal features). That leaves the router only to choose which modality to trust. Two different threshold stages should not be conflated. A \emph{detector confidence floor} discards a detection before it enters the pipeline; these caches run both detectors at \texttt{conf}$=$0.25 (the floor at which the router's features were mined). Dropping the reject class does not change this floor: it is a detector setting, independent of the router's class set. The deployment IR floor is higher, $0.40$, the IR detector's F1-optimal operating point (Section~\ref{sec:design_rationale}). The evaluation is insensitive to it because the filter rescores every surviving detection, so one floor is used for both detectors. A \emph{filter threshold} is then applied to the filter's own $P(\text{drone})$ output, the probability a surviving detection must \emph{clear to be kept}: \texttt{mlp\_v5} (RGB) at $0.25$, and the thermal-native IR filter at $0.05$ on thermal input (Section~\ref{sec:grayscale_verifier}). A \emph{lower} bar keeps more detections, so the thermal $0.05$ is the more permissive of the two. It is deliberately low because the IR detector barely hallucinates on thermal input ($1.8\%$ near-domain confuser fire, Section~\ref{sec:model_mri}), so its filter should intervene as little as possible (recall-safe) --- not because thermal needs aggressive filtering. It is a filter threshold on thermal input, not the IR detector's deployment floor. Ablation arms swap exactly one element at a time: the router (\texttt{robust8}, the reject-class version of the same eight features; \texttt{sa32}, the strongest hand-engineered classifier; \texttt{robust6}, the six-feature statistical base the router was derived from), the filter (the superseded MobileNetV3 patch CNN at \texttt{patch\_thr}$=$0.5), the composition order (\texttt{clf$\to$filt} against the shipped \texttt{filt$\to$clf}), or the stage's presence. All cells replay the same cached detections (Section~\ref{sec:unified_cache}); scoring is per-modality trust-aware, each modality against its own ground truth and never unioned (Section~\ref{sec:scoring_audit}).

\begin{table}[htbp]
    \centering
    \caption{Pipeline ablation in aggregate, across all evaluation surfaces: bare detectors vs the shipped pipeline (per-frame confuser filter then the no-reject router \texttt{robust8-nr}, \texttt{filt$\to$clf}). Drone surfaces report drone $F1$ (higher better; IoP@0.5 on Svanstr\"om/SelCom, IoU@0.5 elsewhere); the two confuser surfaces report frame fire rate (lower better). On the single-modality surfaces (IR test, SelCom) the router is inactive, so the shipped column there is detector\,$+$\,filter. The component-by-component isolation behind these numbers is in the deep-dives of this chapter; the full per-surface, per-stage cell matrix --- with every comparison arm --- is in Appendix~\ref{app:full_ablation}.}
    \label{tab:ablation_overview}
    \footnotesize
    \begin{tabular}{lccccccc}
        \toprule
        & \multicolumn{5}{c}{Drone $F1$ $\uparrow$} & \multicolumn{2}{c}{Confuser fire $\downarrow$} \\
        \cmidrule(lr){2-6} \cmidrule(lr){7-8}
        Stage & Svan & Anti-UAV & DUT & IR-test & SelCom & RGB & IR \\
        \midrule
        bare detectors & 0.742 & 0.973 & 0.758 & 0.961 & 0.591 & 0.304 & 0.294 \\
        \textbf{shipped pipeline} & \textbf{0.946} & \textbf{0.984} & \textbf{0.835} & 0.942 & 0.612 & \textbf{0.014} & \textbf{0.028} \\
        \bottomrule
    \end{tabular}
    \normalsize
\end{table}
% [source: thesis_eval/results*/tier1_results.json (A_bare + B_pipeline per surface); shipped = filt->clf[robust8-nr]; solo surfaces = detector+filter (router inactive)]

\subsection{Paired Drone Benchmarks: the Cost Side}
\label{sec:pipeline_paired}

On drone-positive paired surfaces the question is what the pipeline \emph{costs}: every vetoed detection here is potentially a missed drone.

% tab:ablation_svanstrom moved to Appendix~\ref{app:full_ablation} (production-only body; full ablation in appendix).

% tab:ablation_antiuav moved to Appendix~\ref{app:full_ablation} (production-only body; full ablation in appendix).

Five readings, each attributable to one element.

\emph{The pipeline maintains recall, and in fact raises it (RQ1).} On Svanstr\"om the production cell's recall is \emph{higher} than bare ($0.948 \to 0.991$) while precision rises $0.609 \to 0.901$. That precision gain is RGB-side: the bare detector's confuser false positives (the RGB-only row alone carries $1{,}845$ of them) are removed by the \texttt{mlp\_v5} filter, whose drone/confuser separability is established independently by the Model MRI (Section~\ref{sec:mri_findings}). The recall gain is a separate, routing effect under per-modality scoring: the router stops the system being scored on the modality that is blind on a given frame, so its misses leave the books (bare FN $172 \to 27$ at the router stage). No new detection is created downstream; the architecture cannot do that. On Anti-UAV the pipeline is no-harm by every cell, which is exactly the role of a saturated control.

\emph{In the shipped stack the filter suppresses the false positives; the reject class is a complementary net we trade away (RQ2).} The shipped no-reject router routes every frame, so on Svanstr\"om it removes almost no bare false positives on its own ($2019 \to 1997$); the \texttt{mlp\_v5} filter does that work ($2019 \to 337$ alone, $\to 314$ composed). The reject-class \texttt{robust8} ablation tells the complementary half of the story: its router \emph{alone} removes $82\%$ of bare FPs ($2019 \to 354$) by vetoing whole untrustworthy frames, a different failure mode than the filter's per-detection veto, which is why composing \texttt{robust8} with the filter reaches $187$ FP. Each stage catches what the other misses: the filter cannot recover a frame whose trusted modality is wrong, and the reject class cannot veto a single confuser detection inside a trusted frame. We ship the no-reject router and delegate false-positive removal to the filter (Section~\ref{sec:classifier_results}); \texttt{robust8} keeps that second net at the recall cost quantified throughout this section.

\emph{Composition order is a recall/precision dial, and for the no-reject router \texttt{filt$\to$clf} is the better side of it.} \texttt{filt$\to$clf} keeps recall at the router's level ($0.991$ on Svanstr\"om) because the router re-routes on the already-filtered detections; \texttt{clf$\to$filt} trades $\sim$3~pp of that recall ($0.960$) for marginal precision. With a reject class the order mattered for confuser suppression (reject vetoes whole frames \emph{before} the filter, the historic reason \texttt{clf$\to$filt} was shipped), but the no-reject router never vetoes a confuser frame, so the two orders give \emph{identical} confuser fire (Table~\ref{tab:ablation_confusers}); with that tie broken we ship \texttt{filt$\to$clf} for the recall (Svanstr\"om F1 $0.946$ vs $0.931$, DUT $0.835$ vs $0.790$).

\emph{Why the no-reject router is shipped.} Among the reject-class routers, the 32-feature \texttt{sa32} leads every in-domain cell by $0.2$--$1.8$~pp, but it needs an expensive per-frame OpenCV pass, was trained against a superseded detector's calibration, and leaks scene statistics: its features discriminate \emph{which scene} a frame came from rather than \emph{whether} its detections are trustworthy, so its in-domain lead cannot be trusted to carry to surfaces it has not seen (Section~\ref{sec:feature_selection}). \texttt{robust8} matches it within a point on eight free features at a $404\times$ cheaper decision, with \texttt{robust6} between them. On the confuser-rich Svanstr\"om flagship the reject class earns its keep (at the shipped \texttt{filt$\to$clf} order, $\texttt{robust8}$ $0.964$ vs the no-reject $0.946$, at far lower confuser fire). The shipped router is nonetheless the no-reject \texttt{robust8-nr}: a single global router must serve paired, single-modality, and degraded-channel deployments alike, and the reject class collapses recall on the latter two (Tables~\ref{tab:ablation_dut},~\ref{tab:ablation_solo}); averaged across surfaces the no-reject router leads on the \emph{composed} router$+$filter F1 the deployment actually runs ($0.850$ vs $0.744$). These are composed means, not router-only: the no-reject router suppresses no confusers on its own (that is the filter's job), so it is never deployed without the filter behind it. \texttt{robust8}/\texttt{robust6}/\texttt{sa32} are retained as ablations and the trade is detailed in Section~\ref{sec:classifier_results}.

\emph{Dual-modality routing matches the better modality on every surface (RQ3).} Table~\ref{tab:rq3} scores each modality against its \emph{own} ground truth (never a union) and the routed system trust-aware on the same frames. The per-surface reversal is the point: IR dominates Svanstr\"om ($0.940$ vs RGB's $0.607$), RGB dominates Anti-UAV ($0.985$ vs IR's $0.961$); a fixed single-modality choice loses badly on whichever surface its sensor is weak. The router matches or edges the stronger modality on each \emph{without being told which it is} (Svanstr\"om $0.946$, just above IR-only's $0.940$; Anti-UAV $0.984$, level with RGB-only's $0.985$). That is routing's value: not a number a single modality could exceed on its own home surface, but the ability to serve \emph{both} regimes from one operating point, since which modality wins reverses with the conditions (night, glare, range) a deployment cannot choose in advance (Section~\ref{sec:scoring_audit}).

\begin{table}[htbp]
    \centering
    \caption{RQ3: single modality vs routed pipeline (bare per-modality rows vs the production cell). Each modality is scored against its own GT; the routed row is trust-aware. The modality ranking \emph{reverses} between surfaces; the routed no-reject system matches the stronger modality on each without being told which it is.}
    \label{tab:rq3}
    \begin{tabular}{lll}
        \toprule
        configuration & Svanstr\"om F1 & Anti-UAV F1 \\
        \midrule
        RGB only (\texttt{ft4}, bare)  & 0.607 [0.590--0.623] & 0.985 [0.982--0.988] \\
        IR only (\texttt{v3b}, bare)   & 0.940 [0.931--0.949] & 0.961 [0.956--0.966] \\
        \textbf{Routed (production, \texttt{robust8-nr}, filt$\to$clf)}   & \textbf{0.946 [0.938--0.950]} & \textbf{0.984 [0.982--0.987]} \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: thesis_eval/results/tier1_results.json (A_bare + B_pipeline)]

\paragraph{A clean in-domain check: DUT Anti-UAV (\emph{test split}).}
The two surfaces above are IR-dominant (Svanstr\"om) or saturated (Anti-UAV). The DUT Anti-UAV \emph{test split}~\cite{zhao2022dutantiuav} adds an RGB-strong, confuser-free check: an all-drone RGB benchmark on which the RGB detector \texttt{ft4} is in-domain (DUT was one source in the composite \texttt{rgb\_dataset} it trained on), paired with the thermal \texttt{v3b} detector fed grayscale-converted RGB, i.e.\ the no-thermal cross-domain fallback. \textbf{The grayscale-IR column measures this fallback mode, not the IR detector's native thermal performance}: on real thermal it reaches $F1\,0.940$ (Svanstr\"om, Table~\ref{tab:rq3}) and $0.961$ (Anti-UAV); the much lower numbers here reflect a thermal model run on grayscale RGB, an input it was never trained on, and should not be read as the IR detector's capability. Only the official test split is used (no DUT frame here was a training image for the router or filters). Because no confuser is present, every filter veto can only remove a true positive, so the pipeline's one available contribution is precision. We report at \texttt{imgsz}$=$960, the resolution-optimal setting for this small-drone surface (Section~\ref{sec:resolution_arch}; drones down to $25\times9$~px need the pixels).

% tab:ablation_dut moved to Appendix~\ref{app:full_ablation} (production-only body; full ablation in appendix).

Two readings. First, dropping the reject class is what makes the fused stack competitive on a clean RGB surface: the shipped \texttt{filt$\to$clf[robust8-nr]} reaches F1 $0.835$ (recall $0.800$), recovering the recall the reject-class \texttt{robust8} surrenders here by rejecting $24\%$ of these confuser-free frames; the per-frame filters remain a small recall tax where they can only delete true positives. Second, the strongest single configuration is still RGB-alone: \texttt{ft4} reaches $F1\,0.899$ against its own ground truth, above the fused $0.835$, because the cross-domain grayscale-IR channel --- a fallback mode far below its native thermal $F1\,0.940$ (Table~\ref{tab:rq3}) --- drags the fused number down. On an RGB-strong surface the fused stack buys a live second channel and a bounded precision floor rather than peak F1, and the no-reject router keeps that cost small.

\paragraph{Why the shipped router drops the reject class.}
The four-class \texttt{robust8} can output \texttt{reject} (trust neither modality), which on a clean drone surface discards recoverable recall --- the DUT gap above ($24\%$ of confuser-free frames rejected) and the single-modality collapse of Table~\ref{tab:ablation_solo}. The shipped \texttt{robust8-nr} drops that class (same eight features, an argmax route to RGB, IR, or both) and delegates all false-positive removal to the per-frame filter; averaged across drone surfaces it leads on composed F1 ($0.850$ vs $0.744$). The cost is confuser fire the filter no longer has the reject net to compound (RGB frame fire $0.0011 \to 0.014$, still ${\sim}1\%$ and damped further by temporal voting); we judge that trade worth the recall, and \texttt{robust8} is retained as the ablation that quantifies what the reject class buys (Section~\ref{sec:classifier_results}).

\subsection{Out-of-Distribution Confusers: the Benefit Side}
\label{sec:pipeline_confusers}

On confuser-only surfaces no drone is present; every surviving detection is a false positive, and the headline is the frame fire rate. These surfaces are evaluated in the single-modality deployment regime they represent (an RGB-only camera for the RGB confusers; a thermal camera for the IR confusers), with the router gating the one live channel.

% tab:ablation_confusers moved to Appendix~\ref{app:full_ablation} (production-only body; full ablation in appendix).

Three observations, drawn together in Figure~\ref{fig:pipeline_ablation}. First, the \emph{filter} is the decisive stage on RGB confusers: the per-frame \texttt{mlp\_v5} cuts FP detections $835 \to 39$ where the patch CNN manages only $835 \to 282$, so the feature-reuse filter is both $37$--$72\times$ faster than the CNN it replaced and ${\sim}10\times$ stronger on the surface that matters. Second, the reject class compounds with the filter, and the shipped stack trades part of that away: the \texttt{robust8} ablation stacks reject on top of the filter to reach $0.11\%$ ($3$ of $2{,}633$ frames), whereas the shipped no-reject \texttt{robust8-nr} leaves that work to the filter alone at $1.4\%$ ($39$ of $2{,}633$) --- from nearly one frame in three at the bare detector to roughly one in seventy, the deliberate cost of the recall it recovers on clean and single-modality surfaces.
% [source: thesis_eval/results/tier1_results.json (rgb_confuser C_confuser)]

Third, the thermal-airplane gap is now \emph{largely closed} by thermal-native filter training (Section~\ref{sec:grayscale_verifier}): on the \texttt{IR\_confusers} surface the shipped no-reject cell suppresses thermal-confuser fire from the bare $0.294$ to $0.028$ ($-90\%$). The surface is still airplane-dominated and airplanes remain every filter generation's weakest category \emph{relatively}, but they no longer ``resist'': the residual fire is the genuinely-ambiguous fraction no threshold separates from drones, not an untrained-confuser-class gap. The remaining cost is an in-distribution recall trade on airplane-like thermal drones (Section~\ref{sec:limits}), not an open suppression hole.
% [source: thesis_eval/results/tier1_results.json (ir_confusers C_confuser)]

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{fig_pipeline_ablation}
    \caption{Pipeline ablation at a glance, drawn for the reject-class \texttt{robust8} cascade (the strongest-suppression configuration; the shipped no-reject \texttt{robust8-nr} trades part of the confuser suppression for recall, Table~\ref{tab:ablation_confusers}). (a) Drone-positive paired surfaces: the router supplies most of the F1 lift and the filter finishes it; Anti-UAV confirms no-harm. Whiskers are 95\% bootstrap confidence intervals (the central range of the metric over $1{,}000$ resamples of the frames; bars whose whiskers do not overlap differ by more than sampling noise). (b) Confuser surfaces: bare $\to$ patch $\to$ \texttt{mlp\_v5} $\to$ router $\to$ composed frame fire rates, with near-total suppression on RGB ($30.4\% \to 0.11\%$ composed) and thermal-confuser fire now largely suppressed by the thermal-native filter ($29.4\% \to 2.4\%$ composed); the shipped no-reject stack delegates suppression to the filter alone and sits at $1.4\%$ on RGB. Generated from \texttt{tier1\_results.json} by \texttt{thesis\_eval/gen\_ablation\_figure.py}.}
    \label{fig:pipeline_ablation}
\end{figure}

\subsection{Solo In-Distribution Surfaces}
\label{sec:pipeline_solo}

The in-distribution test splits and the CCTV val set are single-modality surfaces: they receive the bare detector row (Part A) and the filter-only ablation, plus the router gating the one live channel.

% tab:ablation_solo moved to Appendix~\ref{app:full_ablation} (production-only body; full ablation in appendix).

Three per-surface readings. On the IR test split the thermal-native aligned filter costs ${\sim}2.6$~pp recall for a precision nudge --- it now vetoes a few genuinely airplane-like \texttt{ir\_dset} drones no threshold separates (Section~\ref{sec:grayscale_verifier}) --- but stays recall-safe relative to the patch CNN's $5.0$~pp cost on the same frames. On the RGB test split the feature-reuse filter's former coverage carve-out is now \emph{closed} by the bird-split re-mine (\texttt{mlp\_v5\_v4}): recall recovers $0.691 \to 0.887$, within ${\sim}0.4$~pp F1 of the bare detector, so the $-11.7$~pp regression the earlier filter showed here is gone. On SelCom the filter cuts false positives $22 \to 7$ at \emph{zero} recall cost, while the patch CNN, never having seen CCTV-scale crops, is bit-for-bit the bare detector --- the abstention behaviour that motivated the feature-reuse design. Finally, a regime note: the reject-class \texttt{robust8} collapses recall on these single-modality surfaces (it routes against a dead second channel), but the shipped no-reject \texttt{robust8-nr} degrades gracefully to the bare detector instead, so single-modality deployments can run the shipped router safely; detector $+$ filter remains the GUI's minimal single-camera configuration.
% [source: thesis_eval/results/tier1_results.json (S4_verifier clf rows)]

\subsection{Per-Size Attribution: Where the Resolvable Floor Bites}
\label{sec:per_size}

Several claims in this thesis lean on drone \emph{size}: the resolution floor of Section~\ref{sec:resolution_arch}, the \texttt{retrained\_v2} small-drone collapse, and the confuser filter's coverage carve-out. The unified cache stores every ground-truth and detection box, so per-size precision/recall replays exactly; buckets are ground-truth $\sqrt{\text{area}}$ in native-image pixels, with TP/FN bucketed by the ground-truth box and FP by the predicted box. A bucket's recall is only reported when it contains ground truth.

\begin{table}[htbp]
    \centering
    \caption{Per-size recall, bare $\to$ $+$filter, on the two surfaces that carry size-dependent claims (production detectors, Tier-1 configurations). The earlier RGB-test small-drone carve-out is closed by the bird-split re-mine (\texttt{mlp\_v5\_v4}): $+$filter recall now tracks bare in every bucket. Svanstr\"om IR ground truth is dominated by sub-16~px drones, which is why \texttt{imgsz} matters there.}
    \label{tab:per_size}
    \begin{tabular}{llcccc}
        \toprule
        surface & detector & bucket & $n_{gt}$ & bare $R$ & $+$filt $R$ \\
        \midrule
        rgb\_dataset test & \texttt{ft4}  & $<$16 px  & 726   & 0.782 & 0.767 \\
                          &               & 16--32 px & 607   & 0.865 & 0.844 \\
                          &               & 32--64 px & 702   & 0.936 & 0.920 \\
                          &               & $\geq$64 px & 1{,}417 & 0.956 & 0.951 \\
        \midrule
        Svanstr\"om       & \texttt{ft4} (RGB)  & $<$16 px  & 27    & 0.630 & 0.593 \\
                          &               & 16--32 px & 994   & 0.897 & 0.823 \\
                          &               & 32--64 px & 633   & 0.956 & 0.918 \\
        \midrule
        Svanstr\"om       & \texttt{v3b} (IR)   & $<$16 px  & 993   & 0.970 & 0.970 \\
                          &               & 16--32 px & 643   & 0.998 & 0.998 \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: thesis_eval/results/notes_round1_results.json (SZ_per_size); run=thesis_eval/notes_round1_replays.py]

Two attributions emerge. First, the confuser filter's former RGB-test regression (Section~\ref{sec:mlp_recall_drop}) was a \emph{small-drone} recall cost that the production \texttt{mlp\_v5\_v4} closes: where the earlier filter collapsed sub-32~px recall, the bird-split re-mine restores coverage so $+$filter recall now tracks bare recall across every bucket (drones above 64~px were never affected). Second, the resolvable-floor story is quantified: Svanstr\"om's median drone is $29.8$~px ($\sqrt{\text{area}}$, native $640{\times}512$ RGB) and its IR ground truth is majority sub-16~px (median $14.8$~px), so a $640$-pixel inference side leaves the median IR drone ${\sim}15$ native pixels across --- the measured basis for the \texttt{imgsz=1280} requirement on the Svanstr\"om RGB channel (Section~\ref{sec:canonical_configs}).

% Background Failure Profile subsection removed (not needed; the object-driven-not-scene point is in the methodology IR-corpus discussion).
\subsection{The Filter Buys Back the Detector Floor}
\label{sec:lowconf_mode}

The production confidence floors (\texttt{rgb\_conf}$=$0.25, \texttt{ir\_conf}$=$0.40) exist for one reason: to keep the bare detector's false positives in check. With false-positive control delegated to the per-frame filter, that reason weakens. A detector-confidence sweep ($0.05$--$0.50$, crossed with the filter, replayed from a dedicated floor-$0.05$ cache) therefore asks whether \emph{lowering the floor and letting the filter absorb the flood} beats the defaults.

On the deployment-anchor surface it does. On SelCom (full $311$-image val), dropping the floor to $0.05$ raises recall $0.451 \to 0.678$ ($+22.7$~pp) and the filter restores the precision the low floor surrenders ($0.531 \to 0.707$), for $F1 = 0.692$ $[0.654$--$0.730]$ against the default's bare $0.591$ and filtered $0.612$ $[0.551$--$0.659]$. The filter is \emph{recall-transparent} here (filtered recall is identical to bare recall at every floor), which is precisely the mechanism the design promises: the detector proposes, the filter disposes, and nothing true is lost. The move is also confuser-safe: at floor $0.05$ the RGB-confuser corpus floods the bare detector with $1{,}281$ FP detections and the filter holds the line at $50$ ($96.1\%$ suppression; the default operating point leaves $39$). One bound applies: the gain over the \emph{filtered} default is marginal at the CI level (the intervals overlap), while the gain over the bare default is clean.
% [source: ledger=recall-plus-filter-ood-mode; eval=selcom_confsweep_lowconf_mlp,rgb_confuser_confsweep_lowconf; run=thesis_eval/conf_sweep_replay.py; cache=thesis_eval/results/conf_sweep/conf_sweep_results.json]

\begin{table}[htbp]
    \centering
    \caption{Detector-floor sweep $\times$ filter on SelCom (full 311 images, IoP@0.5, \texttt{imgsz=1280}). The filter is recall-transparent at every floor; lowering the floor converts directly into recall.}
    \label{tab:lowconf_selcom}
    \begin{tabular}{lcc}
        \toprule
        detector floor & bare $P/R/F1$ & $+$\texttt{mlp\_v5} $P/R/F1$ \\
        \midrule
        0.25 (default) & 0.858 / 0.451 / 0.591 & 0.950 / 0.451 / 0.612 \\
        0.10           & 0.658 / 0.573 / 0.612 & 0.816 / 0.573 / 0.673 \\
        \textbf{0.05}  & 0.531 / 0.678 / 0.595 & \textbf{0.707 / 0.678 / 0.692} \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: eval=selcom_confsweep_lowconf_mlp; run=thesis_eval/conf_sweep_replay.py]

The mode does \emph{not} generalise to a new global default, and the sweep maps its boundary precisely. On in-domain RGB the bare default remains the safe choice at every floor (the production \texttt{mlp\_v5\_v4} no longer carries a small-drone carve-out here, Section~\ref{sec:verifier_results}, but the low-floor mode buys nothing on this already-strong surface). On the thermal side the floor must stay at $0.40$: dropping it balloons bare thermal-confuser FPs $1{,}051 \to 1{,}911$, and although the thermal-native filter now suppresses most thermal confusers at the default floor (Table~\ref{tab:ablation_confusers}), the sub-floor flood is dominated by the airplane-like detections it deliberately keeps recall-safe, so the low-floor mode is not worth its precision cost there. On the saturated Anti-UAV pipeline (a $500$-frame screening subsample) every cell sits within CI of the default's $0.976$; there is nothing to gain. The result is therefore an \emph{operating mode}, not a setting: on hard out-of-distribution RGB surfaces where the detector is recall-starved, drop the RGB floor to $0.05$--$0.10$ and let the filter own precision; everywhere else the defaults stand. One regime caveat travels with it: the trust router's features were mined at the $0.25$ floor, so sub-floor router cells are deployed-behaviour estimates rather than retraining claims.
% [source: ledger=recall-plus-filter-ood-mode; eval=antiuav_confsweep_pipeline,ir_confsweep_lowconf; run=thesis_eval/conf_sweep_replay.py; cache=docs/analysis/2026-06-11_conf_sweep_recall_plus_filter.md]

\subsection{Runtime Cost}
\label{sec:pipeline_speed}

\begin{table}[htbp]
    \centering
    \caption{Per-stage runtime (GTX 1050 Ti). The production stages are cheap enough to run on every frame; the predecessors they replaced were the reason gating architectures existed at all.}
    \label{tab:speed}
    \begin{tabular}{llll}
        \toprule
        stage & predecessor & production & speedup \\
        \midrule
        trust router    & \texttt{fusion\_no\_fn} 38.3 ms/frame & \texttt{robust8-nr} 0.095 ms/frame & $\sim$404$\times$ \\
        confuser filter & patch CNN 59--112 ms/det & \texttt{mlp\_v5} 1.3--2.1 ms/det & 37--72$\times$ \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: ledger=robust6-speed-feature-efficiency,v5-ship-per-frame; run=eval/bench_speed.py; cache=docs/analysis/2026-06-04_consolidated_sell_new_stack.md]

Total pipeline overhead beyond the two detector forward passes is $1$--$4\%$. This is what dissolves the alert-gating compromise of the predecessor design (Section~\ref{sec:alert_gate}): when the filter costs two milliseconds, it simply runs always.

% ======================================================================
\section{Temporal Evidence at the Segment Grain}
\label{sec:temporal_results}

The deployed system does not alert per frame: an $N$-of-$M$ smoother (Section~\ref{sec:temporal}) aggregates per-frame decisions into alert events. The segment-level evaluation runs on the 19-clip real-video surface, the one canonical surface with consecutive frames ($1{,}359$ drone-clip frames across 9 clips; $1{,}250$ confuser-clip frames across 10), using sliding 3-frame windows with a 2-of-3 vote per clip, in the dual-channel video regime (\texttt{ft4} on the RGB frame, \texttt{v3b} on its grayscale conversion, \texttt{is\_grayscale}$=$1).

\begin{table}[htbp]
    \centering
    \caption{Segment-level (2-of-3 windows) evaluation on the real-video clips. Fully out-of-distribution for every component. The per-frame filter thresholds are the benchmark-tuned production values; the last column is the relative confuser-fire cut from bare. The patch arm here is a per-frame approximation of the alert-gated original.}
    \label{tab:temporal_production}
    \begin{tabular}{lcccc}
        \toprule
        cell & window $P$ & window $R$ & window F1 & confuser window fire (cut) \\
        \midrule
        bare (both channels)            & 0.957 & 0.753 & 0.843 & 0.350 (---) \\
        filt only (mlp)                 & 0.954 & 0.489 & 0.646 & 0.213 ($-39\%$) \\
        filt only (patch, per-frame)    & 0.957 & 0.689 & 0.801 & 0.182 ($-48\%$) \\
        clf only [robust8-nr]           & 0.957 & 0.753 & 0.843 & 0.350 ($-0\%$) \\
        clf only [robust8]              & 0.967 & 0.520 & 0.676 & 0.101 ($-71\%$) \\
        clf only [robust6]              & 0.999 & 0.584 & 0.737 & 0.081 ($-77\%$) \\
        clf only [sa32]                 & 0.967 & 0.582 & 0.727 & 0.120 ($-66\%$) \\
        clf$\to$filt [robust8-nr]       & 0.954 & 0.489 & 0.646 & 0.213 ($-39\%$) \\
        clf$\to$filt [robust8]          & 0.975 & 0.377 & 0.544 & 0.076 ($-78\%$) \\
        clf$\to$filt [robust6]          & 0.998 & 0.412 & 0.583 & 0.059 ($-83\%$) \\
        clf$\to$filt patch [sa32]       & 0.969 & 0.535 & 0.689 & 0.080 ($-77\%$) \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: thesis_eval/results/temporal_results.json; run=thesis_eval/temporal_replay.py (window 2-of-3, patch_thr=0.7, mlp rgb@0.25/gray@0.25)]
% [nr rows: thesis_eval/results_noreject/temporal_results.json — clf[robust8-nr]=bare; clf->filt[robust8-nr]=filt-only (no-reject router passes every confuser frame to the filter)]

This surface is fully OOD for every component, and it is where the pipeline's costs show. Three results. \emph{First, the trade is real on operational video, and the shipped no-reject router sits on the recall side of it:} \texttt{clf$\to$filt[robust8-nr]} keeps window recall $0.489$ (F1 $0.646$, the best composed F1 here) while cutting confuser fire $39\%$, whereas the reject-class compositions (\texttt{robust8}/\texttt{robust6}) push the cut to $78$--$83\%$ but veto OOD drones aggressively (window recall $0.38$--$0.41$), the video counterpart of the filter's coverage limit on fully out-of-distribution footage (Section~\ref{sec:verifier_results}). The per-frame filter thresholds were tuned on benchmark surfaces, so on fully OOD video they over-veto, which is exactly where the deployment's alert-gate placement matters. \emph{Second, the gate position matters more than the gate:} the predecessor design consulted its filter only at the alert moment, and under that placement the same surface showed segment F1 \emph{gains} (below); a per-frame veto applies the same discrimination thousands of times more often, so every false veto compounds. The measured per-frame numbers therefore \emph{validate the GUI's alert-gate placement} for OOD video rather than contradicting the pipeline: routing per-frame (cheap, $-71$--$77\%$ fire at $10$--$17$~pp F1) plus verification at the alert gate is the operating point the deployment uses. \emph{Third, temporal voting is recall-neutral and precision-positive here} (window vs frame: $\Delta R \approx \pm 0.01$ on every cell, confuser fire consistently lower), confirming the smoother's role as a stabiliser rather than a recall recoverer when detections are already temporally consistent.

A filter-threshold sweep over the cached probabilities settles whether the veto cost is an operating-point artifact: it is not. Lowering the shared filter threshold from $0.25$ to $0.01$ recovers most of the composed cells' recall \emph{relative to router-only} (robust8: $R$ $0.377 \to 0.507$ against the router's $0.520$; robust6: $0.412 \to 0.577$ against $0.584$): at a near-zero threshold the per-frame filter costs only $\sim$1~pp recall on top of the router while still trimming window fire by $7$--$9\%$ relative. But the true drones' filter probabilities on this surface are smeared across the $[0.01, 0.25)$ interval rather than concentrated near 1, so no threshold simultaneously keeps them and rejects confusers: the video-deployment choice is a near-zero per-frame threshold (cheap, marginal benefit) or the alert gate (the predecessor's measured-gain placement). The full sweep is \texttt{thesis\_eval/results/video\_thr\_sweep.md}.
% [source: thesis_eval/results/video_thr_sweep.json; run=thesis_eval/video_thr_sweep.py]

\paragraph{Design-evolution evidence (predecessor stack).}
The temporal smoother's value was first established on this surface with the predecessor stack (baseline RGB, \texttt{sa32}, alert-gated patch filter); those numbers are retained as the design-evolution record. At the segment grain that cascade \emph{gained} drone $F1$ for all four RGB variants tested ($+6.6$ to $+16.5$~pp; baseline RGB $0.760 \to 0.826$) while cutting confuser FPR $39$--$81\%$ (baseline: $0.512 \to 0.162$), and it compressed the spread between detector variants from $15.5$ to $5.6$~pp F1: the cascade absorbs detector variance. Per-frame metrics on the same runs \emph{fell} for three of four variants, which is the wrong-unit-of-analysis lesson: the per-frame grain counts every wrong box, the deployed system counts alerts.
% [source: ledger=cascade-segment-recovers,cascade-tightens-variance,cascade-perframe-misleading; eval=pipe_vid_baseline_seg,pipe_vid_baseline_pf; config=pipe_video_drone_iop (predecessor configuration: baseline RGB + sa32 + alert-gated patch @0.7)]

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{fig8_cascade_segment}
    \caption{Design-evolution record (predecessor stack: baseline RGB, \texttt{sa32}, alert-gated patch): segment-level drone $F1$ and confuser FPR, Stage-1 RGB (grey) versus the full cascade, on the real-video diagnostic. (a) The cascade lifts drone $F1$ for every variant. (b) It cuts confuser FPR by $39$--$81\%$. The production-stack re-run of this surface is Table~\ref{tab:temporal_production}.}
    \label{fig:cascade_segment_fig}
\end{figure}

The per-category breakdown of the predecessor runs carries one durable lesson: the cascade's suppression concentrates on birds ($45$--$57\times$ on the worst variants), is moderate on helicopters, and is essentially inert on airplanes ($\sim$0.21 FPR across all variants); the patch filter is genuinely uncertain on airplane crops (median confuser probability $0.540$ vs $0.904$ for birds). Airplanes remain the weakest category for every filter generation (Section~\ref{sec:verifier_results}).
% [source: ledger=cascade-bird-vs-airplane-asymmetry; eval=pipe_percat_sa32; config=pipe_video_confuser]

% ======================================================================
\section{The Grayscale Finding}
\label{sec:grayscale}

The IR detector was trained exclusively on thermal infrared data. The grayscale-RGB mode (Section~\ref{sec:ir_grayscale_mode}) exists as a hardware fallback: convert the RGB stream to single-channel grayscale, feed it to the thermal model. The finding is a recall result: run zero-shot on grayscale-converted visible light, the thermal-trained detector still \emph{detects} drones when the RGB channel fails, at a recall competitive with a dedicated RGB detector, and with lower confuser fire. It was built as a conservative degradation mode and turned out to be a usable second detection channel.

\subsection{The Fair Three-Way Comparison}

On Svanstr\"om, at each model's canonical configuration and against the same ground truth:

\begin{table}[htbp]
    \centering
    \caption{The grayscale three-way on Svanstr\"om ($n=4{,}000$ of 28{,}710, IoP@0.5, bare detectors). The thermal-trained detector on grayscale-converted RGB performs within $2.7$~pp F1 of the dedicated RGB detector (with higher precision and lower recall), while the raw-RGB control isolates the grayscale conversion as the load-bearing step.}
    \label{tab:gray_threeway}
    \begin{tabular}{lccc}
        \toprule
        configuration & $P$ & $R$ & F1 [95\% CI] \\
        \midrule
        RGB detector (\texttt{ft4}) on RGB, @1280 & 0.454 & 0.916 & 0.607 [0.590--0.623] \\
        IR detector (\texttt{v3b}) on raw RGB, @640 & 0.362 & 0.126 & 0.187 [0.165--0.210] \\
        IR detector (\texttt{v3b}) on grayscale RGB, @640 & 0.543 & 0.621 & 0.580 [0.561--0.599] \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: thesis_eval/results/tier1_results.json (svanstrom A ft4/rgb; svanstrom_rawrgb A; svanstrom_gray A v3b/ir)]

The control row settles the mechanism question at benchmark grade: fed the raw three-channel RGB, the thermal model collapses to $F1=0.187$; fed the \emph{same frames} through a one-line grayscale conversion, it reaches $0.580$, a tripling that isolates the single-channel intensity input as the load-bearing step. The zero-shot thermal model is then $2.7$~pp F1 behind the dedicated RGB detector on visible-light frames, with the opposite error profile (more precise, less complete). On the real-video diagnostic, where the finding was first observed, the same mode more than doubles the raw-RGB control's aggregate F1 ($0.295 \to 0.636$), nearly halves bird-confuser FPPI ($0.261 \to 0.142$), and on the hardest bird-cluttered clip ties the best RGB detector ($F1$ $0.837$ vs $0.840$, a tie at that clip's sample size):
% [source: ledger=ir-grayscale-fallback; eval=vid_drone_ir_gray,vid_conf_ir_gray; run=eval/eval_video_tests.py; config=video_drone_iop,video_confuser]

\begin{table}[htbp]
    \centering
    \caption{Real-video six-mode diagnostic (the finding's discovery surface; 9 drone clips / 1{,}234 GT instances, 10 confuser clips / 1{,}250 frames, IoP@0.5). The IR-on-grayscale rows are the discovery; the raw-RGB row is the control.}
    \label{tab:realvideo_master}
    \begin{tabular}{lccccccc}
        \toprule
        & \multicolumn{3}{c}{Drone (9 videos)} & \multicolumn{4}{c}{Confuser FPPI (10 videos)} \\
        \cmidrule(lr){2-4} \cmidrule(lr){5-8}
        Mode & $P$ & $R$ & $F1$ & All & Bird & Airplane & Helicopter \\
        \midrule
        RGB baseline           & 0.771 & 0.749 & \textbf{0.760} & 0.512 & 0.960 & 0.451 & 0.278 \\
        RGB \texttt{retrained\_v2} & \textbf{0.909} & 0.453 & 0.605 & 0.196 & 0.139 & 0.385 & 0.133 \\
        RGB \texttt{selcom\_1280}  & 0.649 & \textbf{0.812} & 0.721 & 0.709 & 1.528 & 0.493 & 0.333 \\
        RGB \texttt{selcom\_640}   & 0.807 & 0.666 & 0.730 & 0.260 & 0.313 & 0.359 & 0.179 \\
        \textbf{IR on grayscale-RGB} & 0.743 & 0.557 & 0.636 & \textbf{0.158} & \textbf{0.142} & \textbf{0.283} & \textbf{0.103} \\
        IR on raw RGB          & 0.647 & 0.191 & 0.295 & 0.150 & 0.261 & 0.158 & 0.079 \\
        \bottomrule
    \end{tabular}
\end{table}
% [source: ledger=ir-grayscale-fallback,no-pareto-dominance-video; eval=vid_drone_*,vid_conf_*; run=eval/eval_video_tests.py; config=video_drone_iop]

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{fig_grayscale_panel}
    \caption{Cross-modal transfer, illustrated on one Anti-UAV night frame (each panel cropped around the drone). \emph{Left}: the production RGB detector (\texttt{ft4}) on the RGB frame, for reference. \emph{Middle}: the IR detector (\texttt{v3b}), trained zero-shot on thermal only, applied to the grayscale-converted frame; it lands the drone (IoU $0.81$ with the green GT box). \emph{Right}: the same IR detector on the raw 3-channel RGB collapses (IoU $0.00$, no box). The transfer is carried by the single-channel intensity input; the colour information that distinguishes thermal from visible is what breaks it.}
    \label{fig:grayscale_qualitative}
\end{figure}

\subsection{Mechanism and Bounds}

The likely mechanism is shared low-level statistics. Thermal IR and grayscale RGB are both single-channel intensity images that render small drones as silhouettes against sky, and a detector trained on thermal silhouette gradients retains them when colour --- the one channel property that genuinely differs --- is removed (Figure~\ref{fig:grayscale_qualitative}). The transfer is partial and patterned: on clean takeoff clips the dedicated RGB detectors win by $28$--$41$~pp F1 per clip (texture-rich close-ups favour visible-light training), while on small-silhouette bird-cluttered scenes the thermal model's discrimination dominates. The IR model has also never seen a visible-light bird, yet its grayscale bird-FPPI ($0.142$) lands within sampling error of a detector explicitly hard-negative-mined for them ($0.139$): thermal-domain confuser training transfers along with the detection.
% [source: ledger=ir-grayscale-fallback; eval=vid_conf_ir_gray; config=video_confuser]

To our knowledge, no published study quantifies this specific transfer: a thermal-only-trained drone detector evaluated zero-shot on grayscale-converted visible-light video against drone ground truth. The adjacent literatures (modality hallucination, RGB-to-thermal translation, domain-adversarial alignment) all provide cross-modal information at training time (Section~\ref{sec:lit_xmodal_cascade}); here the transfer emerges from shared low-level structure alone.

\subsection{The Fallback Value and the Honest Bound}

The finding's practical value is as a \emph{fallback}: grayscale mode gives a thermal-equipped system a usable second detection channel when the thermal camera is absent, recovering drone recall on visible-light video the RGB detector alone would otherwise have to carry. The recall it delivers is real but patterned, and the bound is the one fail: on clean texture-rich close-ups the dedicated RGB detectors win by $28$--$41$~pp F1 per clip, while the transfer wins on small bird-cluttered silhouettes (the mechanism subsection above). Grayscale detection runs as the raw thermal model on grayscale input ($F1=0.580$ bare on Svanstr\"om, Table~\ref{tab:gray_threeway}); it is a degradation channel, not a primary detection surface.
% [source: ledger=ir-grayscale-fallback; thesis_eval/results/tier1_results.json (svanstrom_gray A v3b/ir)]

% ======================================================================
\section{Threats to Validity}
\label{sec:threats}

\paragraph{Internal validity.}
Three threats. First, the RGB detector's training stances and the patch filter's versions were iterated against Svanstr\"om evaluation results; some design decisions are implicitly biased toward Svanstr\"om performance. The real-video clips and the SelCom footage, neither consulted during those iterations, are the partial mitigation. Second, \texttt{imgsz} acts as a hidden hyperparameter; the protocol (Section~\ref{sec:canonical_configs}) pins it per surface, and the one cross-resolution comparison the thesis needs (Svanstr\"om RGB@1280 vs IR@640) is flagged where it appears. Third, the trust classifier was trained against a specific RGB confidence calibration; swapping the RGB detector without re-mining classifier data risks silent degradation (the documented \texttt{retrained\_v2\_32feat} failure, Section~\ref{sec:classifier_variants}).

\paragraph{Construct validity.}
IoP at 0.5 is more lenient than IoU at 0.5 for small-object detection; numbers here are not directly comparable to IoU-based literature without conversion (Section~\ref{sec:metrics}). Trust-aware scoring is the second construct choice, justified in Section~\ref{sec:scoring_audit}; both rules are declared per table.

\paragraph{External validity.}
The IR detector and the trust classifier train on frames and sequences that overlap both paired evaluation surfaces; the overlap is quantified per component, and bounded by the held-out clean split, in Section~\ref{sec:svanstrom_audit}: Anti-UAV conclusions are unchanged on $57{,}542$ held-out frames ($0.973 \to 0.977$ bare, $0.984 \to 0.986$ pipeline), the Svanstr\"om IR detector's solo numbers carry at most $7.3$~pp of in-distribution inflation (part of it sequence difficulty, per the leakage-free RGB control), and the cascade leakage bound is small (the reject-class \texttt{robust8} moves $-1.4$~pp, $0.948 \to 0.934$; the shipped \texttt{robust8-nr} $-3.5$~pp, $0.946 \to 0.911$). The deployment evidence is one site (SelCom) and one camera class; generalisation to other CCTV installations is untested.
% [source: runs/clean_split/clean_split_results.json]

\paragraph{Sampling and uncertainty.}
Large surfaces are evaluated on $\approx$4{,}000-frame even-spread samples (Section~\ref{sec:canonical_configs}); all tables print $n$ and the source size, and headline cells carry 95\% bootstrap CIs. Differences within overlapping CIs are treated as ties; this includes the grayscale three-way's $2.7$~pp gap, which is reported as ``slightly behind'', not as parity. Per-clip real-video numbers ($20$--$340$ frames per clip) are indicative; aggregates are the reliable quantities.

\paragraph{Conclusion validity.}
Single-run point estimates are never compared at sub-CI granularity; the production-stack choices are justified on cost and OOD behaviour where in-domain differences are within noise (Section~\ref{sec:pipeline_ablation}).

\section{Limitations and Future Work}
\label{sec:limits}

\paragraph{Edge latency.} All evaluation ran on a GTX 1050~Ti; the cascade is interactive there, but Jetson-class edge latency is unmeasured. The per-stage budget (Table~\ref{tab:speed}) makes the port plausible; it remains future work.

\paragraph{GUI integration of the aligned filter.} The production design runs \texttt{mlp\_v5\_ir\_aligned} per-frame on the IR branch; it is validated in the evaluation engine, while its operator-GUI wiring is an open engineering item at the time of writing.

\paragraph{The airplane gap (largely closed).} Airplanes were every earlier filter generation's weakest category (patch: $52\%$ catch on Svanstr\"om airplanes, below the decisiveness bar; the grayscale-harvested thermal filter left the thermal-confuser surface airplane-dominated). The production thermal-native head (Section~\ref{sec:grayscale_verifier}) closes most of this: thermal-confuser fire falls $29.4\% \to 2.8\%$ on-cache and $94\%$ of held-out IR confusers are removed, by training on thermal airplane crops directly. The residual is no longer an untrained-confuser hole but an in-distribution recall trade ($-3.7$~pp on genuinely airplane-like \texttt{ir\_dset} thermal drones no threshold separates from airplanes); choosing that operating point, and reducing it with more diverse thermal airplane-vs-drone data, is the remaining future-work item.

\paragraph{IR OOD recall.} The IR detector's recall on independent, verified thermal drone footage outside its corpus families is unmeasured (the available community thermal sets could not be quality-verified and are not used); this is the evaluation gap the RGB parallel channel hedges against.

\paragraph{Track-level classification.} The temporal smoother maintains per-track state and renders flight paths, but does not yet classify on track dynamics (velocity, persistence). Drones, birds, and aircraft have distinct flight signatures; a track-based classifier is a largely orthogonal evidence channel and the most promising single direction, particularly for the residual airplane-like thermal drones that appearance alone cannot fully separate.

\paragraph{Open evaluation items.} The thermal-confuser per-category breakdown formerly registered here has since been run (a CPU replay of the existing cache): the \emph{bare}-detector \texttt{IR\_confusers} fire is airplane-driven ($35.2\%$ bare airplane fire vs $12.2\%$ bird and $0\%$ helicopter), which is why the thermal-native filter that now suppresses most of it (Section~\ref{sec:grayscale_verifier}) had to be trained on thermal airplane crops specifically. The video-tuned filter-threshold question is closed by the sweep in Section~\ref{sec:temporal_results}.
% [source: thesis_eval/results/notes_round1_results.json (ir_confusers CAT)]
