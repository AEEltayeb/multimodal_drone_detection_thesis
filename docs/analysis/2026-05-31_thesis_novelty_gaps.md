# Thesis novelty gaps — contributions with evidence but absent from the draft (2026-05-31)

Generated by the `thesis` skill (novelty mode): `ledger` rows flagged `thesis_contribution`,
cross-checked against `docs/thesis_working.tex` (keyword grep). These have **strong evidence
in the knowledge base but 0 mentions in the thesis** — ranked by load-bearing-ness.

> Caveat: the thesis is mid-draft (Ch5–9 largely unwritten per `thesis_deliverables.md`), so
> some gaps are "not written yet." But the V5 line post-dates the thesis structure and is the
> most recent/strongest work — it is not yet represented anywhere.

| Contribution | Evidence (ledger) | Thesis mentions | Why it matters |
|---|---|---|---|
| **V5 distilled feature-space verifier** | v5-beats-patch, v5-lda-separability, v5-ship-per-frame | **0** (`distill`/`MLP verifier`/`embedding`=0) | Production-candidate verifier; +8–10pp Svan F1, 46–72× faster than patch v2. Biggest gap — §4.4 still describes patch v2 only. |
| **Scene-fingerprint overfitting** | scene-fingerprint-overfit | **0** (`fingerprint`=0) | Clean methodological finding (brightness/pos_x memorize scene identity under seq-split). |
| **Linear separability of ROI features (LDA)** | v5-lda-separability | **~0** (`LDA`=0, `separab`=1) | Grounds the verifier's accuracy in feature geometry; 0.949 binary / 0.954 4-class. |
| **Dual-classifier architecture** | dual-classifier-v3 | **0** | Solves the RGB-fallback bird-confuser case (0.247→1.000 F1). |
| **Backbone-freeze controls forgetting** | ft4-backbone-freeze | **0** (`freeze=15`=0) | freeze=15 necessary+sufficient vs catastrophic forgetting during confuser injection. |
| **Embedding-distillation (ancestor of V5)** | embedding-distillation-cv | **0** | YOLO p5 embeddings → 0.71→0.99 F1; the conceptual precursor. |
| **selcom_960 cross-surface winner** | selcom960-cross-surface-winner | **0** (`selcom_960`=0) | Candidate production RGB not in the stack discussion. |

Present (good): **Pareto framing** (8 mentions) — no-pareto-dominance-video is covered.

## Recommended action
Draft (via `thesis draft`) new subsections for the V5 line (verifier + LDA + distillation),
scene-fingerprint, dual-classifier, and backbone-freeze — all evidence-backed and cited. These
are the strongest unrepresented contributions.
