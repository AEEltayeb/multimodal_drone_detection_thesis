# Verify v1 — Intro slice (Abstract + Introduction)

**Date:** 2026-06-18 · **Agent:** read-only verification · **Slice:** `main.tex` (Abstract, L154-163) + `chapters/introduction.tex` (whole file; RQs L37-41) · **Notes range:** `C:\Users\User\Desktop\thesis notes.txt` L1-248.

**Ground-truth used:** `thesis_eval/results_noreject/tier1_results.json` (SHIPPED robust8-nr), `thesis_eval/results/tier1_results.json` (robust8-reject + bare), `mri/results/{v5_report_regen,ir_v3b_report}/stats.json`, `eval/results/_offline_pipeline/ir_native_vs_aligned.json`, `eval/results/_failure_diagnosis/svanstrom_1280_by_category.csv`, `knowledge/{evals,ledger}.csv`, `references.bib`, `thesis_eval/_audit_headline_numbers.py`, `runs/README.md`.

**Headline status:** The intro slice has clearly been rewritten to the **robust8-nr shipped stack** and to nearly all of the notes' qualitative directives. Numbers in the live `.tex` are overwhelmingly BACKED. The single systemic risk is an **audit-coverage gap** (see TOP FIXES #1): the audit's hand-written `CHECKS` list still pins the *robust8-reject* values (0.948 / 0.11%), so it does **not** actually re-verify several headline cells the prose prints (0.946 / 0.991 / 1.4%). Those cells ARE correct against the NR JSON (confirmed below) — but they are guarded only by the NR block, and the README map (L18-19) is itself stale (says 0.949 / 1.1% / 0.15%).

---

## TABLE 1 — Notes reverify

| Note (line) | Directive | Status | Thesis loc (file:line + quote) | Evidence |
|---|---|---|---|---|
| L3-5 | Confuser problem is NOT global; "concentrated" wording over-implies a global issue | **FOLLOWED** | abstract main.tex:156 "on much non-confuser footage it raises no false alarm at all"; intro:15 "The false-alarm problem is not generic; on much footage the detector never fires falsely at all." | Both surfaces now lead with the not-global framing. |
| L7-13 | State each stage exists for a distinct purpose (RGB=day, IR=night/fog, classifier=routing, filters=FP suppression, temporal=track+ignore transient halluc) | **FOLLOWED** | abstract:156 "an RGB detector for daytime detection and a thermal detector for night and low-visibility conditions, a per-frame trust classifier that routes…, a per-detection MLP confuser filter…, and a temporal aggregator that tracks the target and rejects transient hallucinations" | Verbatim coverage of every stage-purpose the note asked for. |
| L17-21 | Say baseline detectors ARE good so the pipeline must prove worth more dramatically | **FOLLOWED** | abstract:158 "Because the bare detectors are already good, the pipeline has to prove its worth precisely where they fail." | Explicit. |
| L19 (number) | abstract showed 0.742→0.949, R 0.948→0.958 (STALE per brief) | **FOLLOWED (updated)** | abstract:159 now "0.742 to 0.946 while raising recall (0.948 → 0.991)" | Matches shipped robust8-nr filt→clf (see Table 2). Stale 0.949/0.958 are gone. |
| L23 | Re-eval HITL on a SINGLE shared test split (0.5 was on its own split) | **FOLLOWED** | intro:55 "Every revision, first to last, is scored on the same fixed held-out test split of the final IR corpus ($F1\;0.503 \to 0.967$, $n=9{,}612$)" | kb evals: all `ir_final_*` rows on config `ir_final_640` (same split); V2=0.503, v3b=0.967. |
| L24-25 | HITL was NECESSARY for IR because thermal/paired drone data is scarce vs RGB | **FOLLOWED** | abstract:162 "clean thermal drone data is far scarcer than RGB…had to be co-developed"; intro:55 "public drone datasets are difficult to obtain in RGB, far more so in thermal or paired form" | Present in both. |
| L29-30 | "increasingly implicated in unauthorised surveillance" needs a source | **FOLLOWED** | intro:6 "…disruption of civil airspace operations~\cite{shi2018counteruas,taha2019drone,samaras2019deep}" | 3 keys cited; all 3 exist in references.bib. (Whether these CUAS surveys actually substantiate the *prevalence* claim is a citation-quality call → Table 3 NEEDS-CITE.) |
| L31-34 | radar/RF/acoustic limits need a source; also mention fiber-optic drones | **FOLLOWED** | intro:6 "…defeated by autonomous pre-programmed flight, and more recently by fiber-optic-guided drones, fielded at scale since 2024…~\cite{csis2025dronewar}; acoustic arrays degrade under wind…" | Fiber-optic added + `csis2025dronewar` cited (key exists). Radar/RF/acoustic still lean on the same 3 survey cites → Table 3. |
| L37-44, L46 | Detector P/R claims need test-split disclosure + "ordinary footage solved" needs stats | **FOLLOWED** | intro:8 "(4{,}000 even-spread paired frames at imgsz=640; Anti-UAV frames appear in the detector's training corpus, so this is an in-distribution sanity floor…)"; "Section~\ref{sec:failure_profile} quantifies this with a background-tagged failure profile." | Split now disclosed (in-distribution caveat added — addresses note exactly). Stat claim deferred to §failure_profile (forward-ref). |
| L51-52 | Name the exact confuser dataset + want visual examples / global-datasets pie | **FOLLOWED (partial)** | intro:15 "On the `rgb_confusers_merged` test split, a 2,633-image out-of-distribution confuser corpus…(Section~\ref{sec:ds_confusers}, Figure~\ref{fig:confuser_examples})" | Dataset named + example figure ref'd. Global-datasets piechart is out of this slice's scope (intro doesn't claim it). |
| L56-59 | "appearance gap too narrow for the detector" CONTRADICTS the MRI being able to separate them | **FOLLOWED** | intro:15 "…too narrow for the detector to exploit *at its decision layer*…The separation does, however, demonstrably exist deeper in the detector, in its own intermediate feature space (Section~\ref{sec:model_mri})" | Contradiction resolved exactly as the note demanded (decision-layer vs feature-space). |
| L63-66 | Cite/anchor the hardneg_v3more eval numbers to a reproducible source | **FOLLOWED** | intro:18 + source-comment L20 "cache=eval/results/_failure_diagnosis/svanstrom_1280_by_category.csv" | CSV confirmed: heli 66.2→41.9%, airplane →64.7%, bird 94.2% all match. |
| L70-74 | retrained_v2 collapse needs per-size stats / OOD-specific framing | **FOLLOWED** | intro:19 "The collapse is out-of-distribution-specific…on its own in-domain test split the same weights score F1=0.949, above the baseline's 0.942"; intro:23 per-size buckets added | kb: retrainedv2 in-domain 0.9494, baseline 0.9419. Per-size 29.8px/0.63 confirmed in notes_round1. |
| L76-82 | Resolution + failure-profile (background/contrast/angle) need specs, both RGB+IR | **FOLLOWED** | intro:23 "(on Svanström…median drone GT √area is 29.8 px…recall falls from 0.90–0.96 in the 16–64 px buckets to 0.63 below 16 px)"; forward-refs §per_size, §failure_profile | Numbers match notes_round1 + failure_profile JSON. |
| L84-89 | "thermal not always present" framing is pointless; system is dual-modality by design; want RGB-only/IR-only ablations | **FOLLOWED** | intro:23 "…so the system is designed dual-modality from the start, and which modality dominates *reverses* with the scene"; routed-matches-stronger framing w/ Table~\ref{tab:rq3} | Reframed from "fragile/not always present" to designed-dual + reversal. |
| L94-96, L162-163 | "feature-reuse verifier" too vague → be specific / section tag | **FOLLOWED** | intro:26 "a per-detection *MLP confuser filter* (a small MLP that re-reads the 517-dimensional ROI feature vector the detector already computed…, Section~\ref{sec:distill_verifier})" | Defined inline + section ref. |
| L98-100 | "developed and tested at partner's site" is FALSE — only tested on partner RGB footage, not deployed, no IR | **FOLLOWED** | intro:28 "The system is not deployed there; the partner provided RGB footage on which the RGB branch was fine-tuned and evaluated" | Corrected exactly. |
| L102-103 | "median √area ≈37 px" — state the image size | **FOLLOWED** | intro:28 "median √area of ≈37 px in the native 1920×1080 frame" | Frame size stated. |
| L105-108 | "grounding is where field gaps were observed" belittles testing to one env; also mention SelCom finetune didn't regress | **FOLLOWED** | intro:28 "The field gaps above were observed across *all* of the evaluation surfaces…with the CCTV footage contributing the small-drone-in-clutter case"; "ft4 fine-tune lifted SelCom val F1…0.145 to 0.591 while leaving the composite in-domain test corpus essentially unchanged, 0.942 → 0.929" | Both points addressed; non-regression quantified (kb-confirmed). |
| L111-116 | "anchors engineering choices" wording wrong — goal is lightweight/accessible/no expensive HW | **FOLLOWED** | intro:28 "every component must run interactively on modest hardware with no specialised accelerators beyond a consumer GPU (the stage-cost budget of Section~\ref{sec:pipeline_speed}…)" | Reframed to lightweight/modest-HW. (HW *study* not done — note's parenthetical "need to conduct a study" remains OPEN-by-design; not claimed.) |
| L123-128 | RQ1: don't frame as "without retraining"; ask "where retraining fails" | **FOLLOWED** | intro:38 "Retraining the base detector is the cheap first answer…this work tried it first…*Where retraining fails*, can a layered detection pipeline…" | Reframed precisely. |
| L131-134 | RQ2: call it confuser FILTER not verifier; add definition tag if undefined | **FOLLOWED** | intro:39 "the *confuser filter* (the per-detection MLP defined above)" | Renamed + back-reference to the inline definition. |
| L137-144 | RQ3: was turned from a question into an explanation; keep the scoring detail for its section | **FOLLOWED** | intro:40 "Does per-frame routing between two modalities outperform either modality alone? The scoring protocol this question requires is defined in Section~\ref{sec:metrics}." | Now a clean question + section pointer; the union/own-GT explanation removed. |
| L147-157 | "statistics-before-training" para is misplaced in RQ section + over-claims (used for ALL models). It was only classifier (robust6/8) + MLP filters; "leakage-aware feature selection" only on classifier | **FOLLOWED** | intro:43 "For the trust classifiers and the confuser filters (and only for those; the detectors were trained conventionally)…a leakage-ratio statistic, applied to the candidate routing features, excluded scene-fingerprint features from the trust classifier." | Scope corrected to classifier+filters; leakage explicitly scoped to routing features. (Still sits at end of RQ section, not a separate section — minor; note's main complaint was scope, which is fixed.) |
| L161-163 | contributions: filter not verifier; explain the features | **FOLLOWED** | intro:50 "the trusted modality's detections are then filtered by the per-detection MLP confuser filter (Section~\ref{sec:distill_verifier})" | Done. |
| L165-166 | "Under one evaluation standard" — explain why (looks suspicious) | **FOLLOWED** | intro:50 "because earlier project numbers had mixed inference sizes, matching rules, and splits and were not mutually comparable (Section~\ref{sec:eval_protocol})" | Rationale added. |
| L171-174, L181 | traceability/repro: need a "runs" folder the reader can verify; name the confuser dataset | **FOLLOWED** | intro:50 "Every number is traceable: Appendix~\ref{app:provenance} maps each headline figure to its results file and replay command in the `runs/` directory"; "rgb_confusers_merged out-of-distribution corpus (2,633 frames)" | `runs/` exists w/ README map; dataset named. (Reader-facing adequacy → Table 3.) |
| L184-190 | speed claims need a table / assessment | **FOLLOWED** | intro:50 "The stages are cheap by construction (Table~\ref{tab:speed}): …0.095 ms…~404×…1.3–2.1 ms…37–72×" | Table ref'd (`tab:speed`). |
| L192-197 | leakage ratio is classifier-only; phrasing conflates it | **FOLLOWED** | intro:53 "a leakage ratio that exposes scene-fingerprint features (applied in this work to select the trust classifier's routing features)" | Scoped to classifier. |
| L199-201 | "linear separability ≈95% RGB, 98.1% IR" — tested on what datasets? what's OOD? | **FOLLOWED** | intro:53 "0.952 (RGB) and 0.981 (IR) on the detectors' own cached feature corpora; these are in-corpus diagnoses that license a training attempt, and the filter's out-of-distribution evals (Chapter~\ref{ch:hitl}) are what validate it" | In-corpus vs OOD-validation distinction added exactly as asked. |
| L203-208 | "aligned grayscale into thermal feature space (AUROC 0.500→0.919)" — explain (shift + Z value) | **FOLLOWED** | intro:53 "grayscale-converted RGB lands in a shifted, rescaled region…the discriminative directions are shared but the per-feature means and variances differ, so a second input scaler fitted on grayscale detections (per-feature standardisation) recovers the transfer…where CORAL reaches only 0.707" | Z-score/shift mechanism explained; the note's own hypothesis ("shift by a Z value") confirmed correct. |
| L210-212 | "re-running it corrected two figures in an earlier hand-written report" — irrelevant to contributions; explain | **FOLLOWED (removed)** | Not present in current intro contributions. | The sentence was cut from the contributions list. |
| L214-216 | "fixed held-out test split (0.503→0.967)" — is it TRULY fixed? must test on ir_dset_final split | **FOLLOWED** | intro:55 "scored on the same fixed held-out test split of the final IR corpus" | kb confirms all `ir_final_*` on `ir_final_640`. |
| L218-219 | V5 regression recovery was DUE to HITL | **FOLLOWED** | intro:55 "the recovery was itself a product of the loop: the same review process that the ingestion bypassed is what located and corrected the regression" | Explicit. |
| L221-222 | grayscale cross-modal F1 0.580 vs 0.607 — on what dataset / how many frames? | **FOLLOWED** | intro:61 "F1 0.580 vs 0.607 on the Svanström visible channel, $n=4{,}000$, matched configuration" | Surface + n now stated; matches T-JSON. |
| L224-225 | "ties it on the hardest bird-cluttered video clip" — which clip? | **FOLLOWED** | intro:61 "(`flock_of_seagulls_attack_drone_beach`, F1 0.837 vs 0.840)" | Clip named; ledger-confirmed. |
| L227 | "517-D MLP a sufficient verifier" — it OUTPERFORMS the MobileNet trained on the exact negatives; source it (or drop) | **FOLLOWED** | intro:61 "not merely a sufficient confuser filter but a better one than the MobileNetV3 patch classifier that was trained on the very negatives the detector fails on (Section~\ref{sec:verifier_results})" | Stronger claim made + section ref + eval source-comment (`patch_catch_v2_svan`). |
| L230-233 | "third observation: multi-modal F1 depends on scoring" is obvious; don't headline in contributions | **FOLLOWED (removed)** | Not in current contributions; scoring is treated as protocol in §metrics (RQ3 pointer). | The "third observation" bullet is gone. |
| L235-243 | repro layer: how will the reader/evaluator actually CONFIRM? table? obvious in workspace? GitHub? | **FOLLOWED (partial)** | intro:64 "Appendix~\ref{app:provenance} gives the reader the concrete map: headline number → canonical results file in `runs/` → replay command → automated audit check." | Concrete chain + appendix promised. Whether App provenance + GitHub truly let an external evaluator reproduce is outside this slice → flagged Table 3. |
| L246-248 | "make any future adaptation auditable" — how is it auditable HERE? | **FOLLOWED** | intro:69 (ethics) "an auditor can re-run the replay commands against the published caches and verify that the reported behaviour is the shipped behaviour." | Made concrete (re-run replays vs published caches). |
| L52, L78-79 (visual examples / piechart / failure-profile P&R) | want figures + global datasets viz | **OPEN-by-design** | Forward-refs only in this slice (`fig:confuser_examples`, `fig:confuser_fp_examples`, §failure_profile, §per_size). | Figures live in methodology/empirical chapters — out of slice; not a regression. |

**No REINTRODUCED / REGRESSED notes found in this slice.** Every actionable directive in L1-248 is either FOLLOWED in the live `.tex` or is a figure/study that belongs to another chapter (OPEN-by-design).

---

## TABLE 2 — Numbers

All values cross-checked against the cited JSON/CSV/kb. "robust8-nr" = `clf->filt[robust8_nr_drop]` / `filt->clf[robust8_nr_drop]` in `results_noreject/tier1_results.json` (the SHIPPED stack; composition = filt→clf).

| Number + context | Thesis loc | Source | BACKED? |
|---|---|---|---|
| Svan paired F1 0.742 → **0.946** | abstract:159; intro:50 | `results_noreject` svan filt→clf[r8_nr] f1=0.9459; bare 0.7415 | ✓ |
| Svan recall 0.948 → **0.991** | abstract:159; intro:50 | same, R=0.9905; bare R=0.9481 | ✓ |
| Svan precision 0.609 → **0.905** | intro:50 | same, P=0.9052; bare P=0.6088 | ✓ |
| OOD confuser fire 30.4% → **1.4%** (filter) | abstract:159; intro:50 | `results_noreject` rgb_confuser filt→clf[r8_nr] fire=0.0144; bare 0.3035 | ✓ |
| reject-class variant **0.11%** composed | abstract:159; intro:50 | `results` rgb_confuser clf→filt[robust8] fire=0.00110 | ✓ |
| router-alone 30.4% → 4.9% (abstract narrative implies stage decomposition) | abstract:159 ("the filter suppresses the false positives") | `results` clf[robust8] fire=0.0490 | ✓ (intro prose no longer prints the 4.9% intermediate; abstract attributes suppression to filter — consistent) |
| Anti-UAV 0.973 → **0.984** | abstract:159; intro:50 | `results_noreject` antiuav bare 0.9728 → filt→clf[r8_nr] 0.9842 | ✓ |
| Anti-UAV bare ft4 **P=0.989, R=0.982, 41 FP / 4,000** | abstract:156; intro:8 | `results` antiuav A_bare ft4/rgb P=0.9889 R=0.9817 FP=41 | ✓ |
| RGB halluc **2.8%** of composite test corpus | abstract:156; intro:8 | kb `v5_rgbds_bare` halluc=0.0276 → 2.8% | ✓ |
| bird-only Svanström up to **94%** fire | abstract:156 | `svanstrom_1280_by_category.csv` BIRD det_rate=94.4% | ✓ |
| retrained_v2 collapse **0.961 → 0.306** | abstract:156; intro:19 | csv DRONE recall: baseline 0.959 (≈0.961 hardneg row), retrained_v2 0.306 | ✓ (0.961 = baseline pre-collapse small-drone R; recall 0.306 exact) |
| per-category fire **39.0% bird / 58.0% heli / 23.4% airplane** @640 | intro:15 | `notes_round1` rgb_confuser CAT bare: 0.3896/0.5802/0.2339 | ✓ |
| Svanström @1280 **94.4% bird / 74.6% airplane / 66.2% heli** | intro:15 | csv: 94.4 / 74.6 / 66.2 | ✓ |
| hardneg_v3more **heli 66.2→41.9%, airplane →64.7%, bird 94.2%** | intro:18 | csv hardneg rows: 41.9 / 64.7 / 94.2 | ✓ |
| retrained_v2 bird fire **→3.4%** | intro:19 | csv retrained_v2 BIRD det_rate=3.4% | ✓ |
| retrained_v2 in-domain **F1 0.949 > baseline 0.942** | intro:19 | kb `rgb_rgbds_retrainedv2`=0.9494, `rgb_rgbds_baseline`=0.9419 | ✓ |
| Svan median GT √area **29.8 px** (native 640×512); recall 0.90–0.96 (16–64px) → **0.63** (<16px) | intro:23 | `notes_round1` svan SZ median 29.8; <16px bare R=0.6296; 16-32px 0.8974 | ✓ |
| thermal leads Svan **0.940** vs visible **0.607**; Anti-UAV visible **0.985** vs **0.961** | intro:23 | `results` svan v3b/ir=0.9401, ft4/rgb=0.6067; antiuav ft4 bare 0.9853, v3b 0.9610 | ✓ |
| SelCom ft4 val F1 **0.145 → 0.591** | intro:28 | kb `selcom_baseline_1280`=0.145, `v5_selcom_bare`=0.5911 | ✓ |
| composite in-domain **0.942 → 0.929** (ft4 non-regression) | intro:28 | kb baseline 0.9419, `v5_rgbds_bare`(ft4)=0.9290 | ✓ |
| SelCom median √area **≈37 px** (native 1920×1080) | intro:28 | source-comment "median 36.8px native" (SelCom manifest) | ✓ (≈37 rounding) |
| trust classifier **0.095 ms/frame (~404×)**; filter **1.3–2.1 ms/det (37–72×)** | abstract:159; intro:50 | kb ledger `robust6-speed-feature-efficiency`, `latency-edge-unmeasured` (run=eval/bench_speed.py) | ✓ (kb-backed; not independently re-timed this pass) |
| abstract speed "0.095 ms and ~2 ms…37–404×" | abstract:159 | same ledger rows | ✓ (37–404× brackets the 37–72× / 404× pair) |
| LDA separability **0.952 RGB, 0.981 IR** | abstract:162; intro:53 | `v5_report_regen/stats.json` lda=0.9517; `ir_v3b_report` lda=0.9813 | ✓ |
| transfer AUROC **0.500 → 0.919**; CORAL **0.707** | abstract:162; intro:53 | `ir_native_vs_aligned.json` + `mri/docs/ir_grayscale_verifier_report.md` table: raw 0.500 / CORAL 0.707 / z-score 0.919 | ✓ |
| IR HITL **F1 0.503 → 0.967**, **n=9,612** | abstract:162; intro:55 | kb `ir_final_v2`=0.503, `ir_final_v3b`=0.967 (config ir_final_640) | ✓ |
| V5 protocol-breach cost **12.7 points of precision** | intro:55 | ledger `ir-version-progression` / `ir_v5_regression_cause` (memory: "12.7 points of precision") | ✓ (ledger-backed; exact ΔP not re-derived this pass — see note) |
| grayscale within ±2.7 pp; cross-modal **0.580 vs 0.607** (n=4,000) | abstract:162; intro:61 | `results` svanstrom_gray v3b=0.5796, svanstrom ft4=0.6067 (Δ=2.71pp) | ✓ |
| clip tie **0.837 vs 0.840** (flock_of_seagulls_attack_drone_beach) | intro:61 | ledger `ir-grayscale-fallback` "F1 0.837 vs 0.840" | ✓ |
| corpus artifacts: **129,130** IR / **27,024** confuser / **28,710** Svan paired / **2,609** video frames / **1,234** GT | intro:57 | source-comments cite `tab:ds_ir_components`, `sec:ds_youtube` (methodology/appendix tables) | ⚠ NOT re-verifiable in this slice — these totals live in Ch3/appendix tables, outside the audited JSONs. Flag for the Ch3 agent; not independently confirmed here. |

**Audit-coverage caveat (not a value error):** `thesis_eval/_audit_headline_numbers.py` reports **187/187 pass**, but its `CHECKS` list pins the **robust8-reject** Svan composed cell at **0.948** (L20) and the composed confuser at **0.11%** (L37) — i.e. it does NOT assert the prose's **0.946 / 0.991 / 1.4%**. Those prose cells are guarded only by the NR block (L206-234), which I independently confirmed against the JSON. `runs/README.md` L18-19 is **stale** (still prints "0.742 → 0.949 (R 0.948→0.958, P 0.609→0.939)" and "30.4% → 4.9% → 1.1% → 0.15%") — it disagrees with the live abstract. The README is a doc, not an asserted source, so no number is *wrong*, but the provenance map the thesis points the reader to (Appendix→runs/README) is out of date.

---

## TABLE 3 — Claims

| Claim (short) | Thesis loc | Status | Note |
|---|---|---|---|
| UAVs "increasingly implicated in unauthorised surveillance, perimeter incursion…" | intro:6 | **NEEDS-CITE** | Cited to `shi2018counteruas,taha2019drone,samaras2019deep` (CUAS *sensing* surveys). Those substantiate sensing limits, not the *prevalence/incident* claim. A separate citation-hunt agent should confirm a source actually backs the prevalence assertion (note L30). |
| radar small-cross-section / RF needs active link / acoustic degrades | intro:6 | **NEEDS-CITE** | Same 3 survey cites carry all three modality-limit claims. Plausibly in-scope for those surveys, but not separately attributed; flag per note L34. |
| fiber-optic-guided drones "fielded at scale since 2024…emit no RF signature" | intro:6 | **CITED** `csis2025dronewar` | Key exists in references.bib; content of the claim (2024 scale-up) should be spot-checked by the cite agent but is properly attributed. |
| YOLO family delivers high drone P/R at real-time rates | intro:8 | **CITED** `redmon2016yolo,ultralytics2024` | Keys exist. |
| in-distribution sanity-floor caveat (Anti-UAV in training corpus) | intro:8 | **EVIDENCED** §lit_drone (forward-ref) + audit cell | Honest framing; backed by the bare ft4 cell. |
| "Detector recall, once lost, cannot be recovered downstream" | intro:19 | **EVIDENCED** (argument) | Sound design argument, not a cited fact; correctly stated as reasoning. |
| MRI "trains the production confuser filters automatically from its own measurements" | intro:53 | **POTENTIAL LOCKED-DECISION CONFLICT** (MRI↔MLP) | Brief rule #2: filters were NOT trained *from* the MRI; MRI only diagnoses separability. This sentence ("Its output **trains** the production confuser filters automatically") reads as MRI→training causation. The abstract twin (main.tex:162 "trains the production confuser filters automatically from its own measurements") has the same wording. **Flag for human review** — verify against the locked decision; if filters are trained by a separate pipeline this overstates the MRI's role. (Intro:53's earlier clause is correctly hedged: "in-corpus diagnoses that license a training attempt…OOD evals are what validate it" — so the over-reach is localized to the final sentence.) |
| "statistics before training…for the trust classifiers and the confuser filters (and only for those; the detectors were trained conventionally)" | intro:43; abstract:162 | **EVIDENCED / scope-correct** | Matches brief rule #2 (applies only to filters + robust6/8, not detectors/all classifiers). Note L147-157 explicitly satisfied. No over-reach to detectors. |
| grayscale→thermal alignment, "second input scaler fitted on grayscale detections," AUROC 0.500→0.919 | intro:53; abstract:162 | **EVIDENCED** `ir_native_vs_aligned.json` + report | ⚠ **Locked-content watch (grayscale-FILTER):** brief rule #1 says ALL grayscale-FILTER content must go, but the grayscale→thermal *alignment* is framed here as a **Model-MRI finding** / **cross-modal transfer** (contribution 2 + finding 1), NOT as a shipped grayscale filter head. This is the permitted "grayscale=finding" lane, and the slice does NOT mention a grayscale filter/"two heads"/"one network two scalers"/grayscale-confuser-harvest/`tab:gray_thermal_auroc`/`fig:ir_gray_align`. **Borderline:** "recovers the transfer, raising cross-modal **verification** AUROC" — the word "verification" + "trains the production confuser filters" (above) edges toward implying a grayscale *filter* exists. Flag for human: confirm the grayscale alignment is presented strictly as a measurement/finding and that no *grayscale filter* is implied to ship. |
| 517-D MLP "a better [filter] than the MobileNetV3 patch classifier trained on the very negatives the detector fails on" | intro:61 | **EVIDENCED** §verifier_results + kb `patch_catch_v2_svan`,`v5_svan_mlp/patch` | Stronger-than-sufficient claim now sourced (satisfies note L227). |
| "Every number is traceable…Appendix provenance → runs/ → replay → audit" | intro:50, 64 | **EVIDENCED (with caveat)** | `runs/` + README + audit script all exist. BUT README map is stale (Table 2 caveat) and an external evaluator's ability to reproduce depends on App `app:provenance` content (out of slice). Note L243/L248 substantively addressed; residual = keep README in sync. |
| Model cards (`*.model_card.yaml`) for provenance | abstract / intro | **UNSUPPORTED (opportunity, not error)** | Brief item #3: every model has a co-located `*.model_card.yaml`, but neither the abstract nor the intro cites them. **Good insertion spots:** intro:50 (contribution 1, after "Every number is traceable…") and intro:64 (reproducibility-layer sentence) — adding "and each evaluated model carries a co-located model card (Appendix~\ref{app:models})" would strengthen the provenance/traceability story the chapter already makes. Also a natural fit in contribution 4 (curated artifacts). Not currently mentioned anywhere in the slice. |

---

## TOP FIXES (ranked)

1. **Audit + README do not actually guard the shipped headline numbers (provenance drift).** The abstract/intro print the **robust8-nr** cells (Svan **0.946**, R **0.991**, P **0.905**; confuser **1.4%**), all of which I confirmed against `results_noreject/tier1_results.json`. But `thesis_eval/_audit_headline_numbers.py` `CHECKS` still pins the **robust8-reject** Svan composed = **0.948** (L20) and composed confuser **0.11%** (L37), and `runs/README.md` L18-19 still prints the *older* **0.949 / R 0.958 / 1.1% / 0.15%**. → The thesis's own "every number is traceable" chain points the reader at a stale map. **Fix (owner, not this agent): update the README number→file map to the nr values and add nr-headline asserts to the audit's CHECKS so the shipped cells are actually re-verified.** No prose number is wrong.

2. **MRI↔MLP over-reach: "Its output trains the production confuser filters automatically from its own measurements."** (intro:53 *and* abstract:162). Per locked rule #2 the MRI **diagnoses**, it does not train the filters. The neighbouring clauses are correctly hedged; only this trailing sentence asserts MRI→training. **Flag for human edit** to "its measurements *motivate and license* the filters" (or similar) unless training-from-MRI is literally true.

3. **Grayscale-FILTER boundary watch (locked rule #1).** The grayscale→thermal alignment (AUROC 0.500→0.919) is correctly cast as a **finding/transfer** result, and none of the banned grayscale-filter artifacts appear in this slice — good. But the phrase "cross-modal **verification** AUROC" + the rule-#2 sentence above together edge toward implying a shipped grayscale *filter*. **Confirm the alignment stays a measurement/finding and no grayscale filter is implied.**

4. **Model cards uncited (rule #3).** No `*.model_card.yaml` reference anywhere in abstract/intro despite every model having one. **Add a provenance citation** at intro:50 (contribution 1 / "Every number is traceable") and/or intro:64 (reproducibility layer) and contribution 4 — cheap, strengthens traceability.

5. **Two NEEDS-CITE prevalence/modality claims (intro:6).** "increasingly implicated in unauthorised surveillance…" and the radar/RF/acoustic limit triplet both lean on the same 3 CUAS *sensing* surveys, which may not substantiate the *prevalence* assertion. **Hand to the citation agent** (notes L30, L34) to confirm/replace.

6. **Corpus-total numbers unverifiable in this slice (intro:57: 129,130 / 27,024 / 28,710 / 2,609 / 1,234).** They live in Ch3/appendix tables, outside the audited JSONs. **Pass to the methodology/appendix verification agent** — not confirmable here, so not asserted as backed.

*(Items 2–6 are flags for the human/other agents; this agent made no edits.)*
