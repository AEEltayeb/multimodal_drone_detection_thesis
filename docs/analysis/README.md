# Analysis directory

Long-form analyses, strategy memos, and experiment write-ups go here as topic-titled `.md` files. Each file must contain a **Delivered** section listing every artifact the analysis produced (CSV, JSON, weights, scripts, plots) with its absolute path so it can be located months later without re-deriving.

## Index

- **[2026-06-11_conf_sweep_recall_plus_filter.md](2026-06-11_conf_sweep_recall_plus_filter.md) — CONF SWEEP × MLP FILTER (recall+filter = OOD mode, not a global default).** Detector-conf sweep 0.05–0.50 × {bare, +MLP verifier} on 8 surfaces from a floor-0.05 re-detect cache, zero-GPU replay (reproduces Tier-1 exactly at the 0.25 floor). Headline: **selcom conf 0.05 + mlp_v5 → F1 0.692 vs 0.591 default (+10pp; recall +22.7pp; filter recall-transparent)** and confuser-safe (1281→45 FP). Does NOT generalize: in-domain RGB −12.5pp (v5 carve-out), IR can't lower (thermal filter absorbs only ~18% of the FP flood), Anti-UAV full pipeline saturated at defaults (0.976), aligned_gray still over-vetoes gray drones (R 0.62→0.075, matches Tier-1). Ledger `recall-plus-filter-ood-mode`; scripts `thesis_eval/conf_sweep_replay.py` + `pipeline_cache_unified.py --conf`; 8 figures.
- **[2026-06-05_routing_grayscale_hole_resolved.md](2026-06-05_routing_grayscale_hole_resolved.md) — RESOLVED (the grayscale trust_rgb hole is fixable with free signal, no GPU re-mine).** robust6's grayscale trust_rgb recall (0.12) is **two free fixes**, not a feature dead end: (1) add `rgb_mean_conf` — the confuser-finetuned detector's own confidence, AUROC **0.934** on the hole slice, generalizes — which **doubles** recall (0.118→0.263, bootstrap-sig) while *improving* thermal macro-F1; (2) **lower the trust_rgb decision threshold** — argmax suppresses the rare class, but P(trust_rgb) is well-separated (AUROC 0.879), so a per-class threshold lifts recall to 0.71 (t=0.15) / 0.82 (t=0.10) with graceful precision loss. The `is_grayscale` flag is a marginal optional add; XGBoost extracts regime-conditioning from the feature. **Corrects the earlier "free features exhausted → need the verifier" conclusion** (that measured argmax recall, not separability). Verifier (0.949) is now optional polish, not the lever. En route: fixed two bugs — `--free-only` leaked `contrast_diff`/`bg_delta_diff`, and the `*_diff` features smuggle the excluded presence flag via the 0.0-no-detect default (corr 0.93); added `--mask-diffs`. Plots: operating-point PR curve, regime-flag test.
- [2026-06-01_mlp_v5_recall_drop_mri.md](2026-06-01_mlp_v5_recall_drop_mri.md) — MRI diagnosis of *why* mlp_v5 vetoes real drones (recall drop). Statistically: vetoed drones are NOT low-conf (Δ=0) but **smaller + OOD-signature**, and **far from confusers** (centroid 16.48 vs kept 11.05) → coverage gap, not structural. Prescribes fix: targeted drone-diversity re-mine (RGB analog of the IR fix that worked) or size-aware threshold; explains why the untargeted `remine_rgb` failed. CPU/offline on cached feats.
- **[2026-06-01_statistical_feature_selection_STUDY.md](2026-06-01_statistical_feature_selection_STUDY.md) — STUDY / TEACH-ME (read first for the classifier story).** A-to-Z, exam-ready: how we used LDA/PCA/ANOVA + a leakage statistic (`F_domain_inclass/F_class`) to find the 6 features that matter, why scene-stat features are fingerprints (AUROC≈0.5, leakage 300+), how robust6 was trained on ft4 data, and all 3 ablation levels (feature screen → ft4 retrain → 5000-frame full-pipeline). Verdict: robust6 production-viable (matches sa32 in-domain, ~30% fewer OOD false alerts, 6 free features vs 32). Includes the thesis-chapter blueprint (§9) and an exam cheat-sheet (§10). Embeds the 4 fusion_* plots.
- **[2026-06-01_full_pipeline_offline_eval.md](2026-06-01_full_pipeline_offline_eval.md) — OFFLINE VERIFIER MATRIX.** Re-runnable (zero-GPU Phase B) full-pipeline eval, 1000 strided frames/surface, bare vs patch vs MLP for RGB + IR + grayscale. Reproduces mlp_v5 RGB win (svan 0.61→0.87, confuser 216→16 FP); confirms IR aligned_thermal (recall-safe + CBAM 0.70→0.85, FP 48→15) with ir_patch useless; NEW: aligned_gray crushes gray confuser FP (143→20) but over-vetoes small gray drones (R 0.55→0.16) → gate it. Scripts `eval/pipeline_cache.py` + `eval/pipeline_eval_offline.py`.
- [2026-05-31_ft4_lean_trust_classifier.md](2026-05-31_ft4_lean_trust_classifier.md) — Re-mined the trust/fusion classifier features from the CURRENT ft4+v3b stack (cheap features only) and compared feature subsets. Confirms the leakage thesis on the current detector: `robust6` (6 feats: conf + box geometry) beats `all19` overall and **more than doubles OOD-drone-video F1 (0.26→0.58)** by dropping brightness/position fingerprints. Pick = robust6; meta4 too lean (loses confuser rejection). Saved `trust_ft4_robust6.joblib`. Next: full-pipeline eval.
- [2026-05-31_fusion_feature_leakage_stats.md](2026-05-31_fusion_feature_leakage_stats.md) — Leakage-aware statistical feature selection for the trust/fusion classifier (reuses `mri.stats`). Introduces a `leakage_ratio = F_domain_inclass / F_class` statistic that flags scene-fingerprint features (`img_std`/`img_entropy`: AUROC≈0.5 but leakage 300+) up front, confirming the lean-line deprecations with a number. LDA separability 0.982. Recommends a ~10-feat robust core (confidences + box geometry + cross-modal), drops scene scalars, flags detection-presence flags as label-tautological. Screen on old-detector features; validate on ft4.
- [2026-05-31_mlp_v5_emails.md](2026-05-31_mlp_v5_emails.md) — Two tailored English email drafts (professor=research framing, company supervisor=deployment framing) announcing the V5 feature-distillation verifier. Includes problem statement, LDA/PCA/ANOVA evidence, MLP-vs-XGBoost rationale, metrics vs bare/patch, and per-email attachment lists.
- **[2026-05-31_model_performance_ledger.md](2026-05-31_model_performance_ledger.md) — PERFORMANCE LEDGER.** All models ranked by purpose (RGB drone F1, IR drone F1, IR-on-grayscale, RGB/IR confusion filters, 3-way trust classifiers) each tagged with surface+scoring config, plus the overall production stack. Surfaces every recorded number from `knowledge/`. Flags 3 gaps as to-dos: no FPS/latency data anywhere, classifier outdated vs current stack, full-pipeline re-eval needed.
- [2026-05-11_path_forward.md](2026-05-11_path_forward.md) — Should we retrain RGB again? Strategic read of the May 2026 ablation matrix; thesis + production angle.
- [2026-05-12_two_day_synthesis.md](2026-05-12_two_day_synthesis.md) — Two-day executive synthesis. Production stack pinned; five sub-questions answered with evidence; thesis story distilled. Start here.
- [2026-05-16_roboflow_ood_eval.md](2026-05-16_roboflow_ood_eval.md) — Roboflow 9-dataset OOD eval (RGB baseline + retrained_v2 + IR + patch verifier, no classifier, imgsz=640). Confirms baseline RGB pick, exposes IR brittleness on `ir_drone_night`, quantifies patch verifier's OOD ceiling.
- [2026-05-16_rgb_camera_spec_and_fault_profile.md](2026-05-16_rgb_camera_spec_and_fault_profile.md) — RGB camera/sensor spec + per-model fault profile. TL;DR-first structure: pick a model by footage type, hard camera/scene constraints, imgsz×latency table, fault profile per model, 3-way FP classification (real/near-miss/duplicate), deployment go/no-go flowchart. Conclusion: imgsz=960 is the F1-optimal setting; clutter (Laplacian-var > 5000) is the binding selcom constraint; baseline + ft2_1280 split production duty between open-sky and urban-CCTV scenes. Includes plan for IR-side equivalent.
- [2026-05-17_failure_profile_by_dataset.md](2026-05-17_failure_profile_by_dataset.md) — Per-(model, dataset) confuser failure profile mined from existing eval CSVs. Key reads: retrained_v2 has learned a "don't fire on small things" rule (99.3% large FPs on rgb_helicopter); the IR airplane fire-rate gap (58% vs 16% across two datasets) is a small-object regime story; the Svanström baseline genuinely cannot discriminate helicopters from drones at the confidence level (HELI median conf 0.832 > DRONE_TP 0.822); patch verifier's airplane weakness is bimodality at the verifier itself.
- **[2026-05-17_thesis_revision_state.md](2026-05-17_thesis_revision_state.md) — ACTIVE.** Live state of the thesis revision. Pinned action items, narrative decisions, "what not to do" list. Read first when resuming after context compaction.
- **[2026-05-28_distillation_v5_journey.md](2026-05-28_distillation_v5_journey.md) — V5 chapter.** End-to-end story of replacing the RGB patch verifier with the V5 feature-distillation MLP. §1–13 cover catastrophe → recovery → production candidate; §14 (added 2026-05-29) diagnoses the rgb_dataset_test recall-ceiling carve-out as Svanstrom drone-weight dominance. Headline: V5 beats patch v2 by +10 pp F1 on Svanstrom at 50× the speed.
- **[2026-05-29_v5_reproduction_recipe_for_ir.md](2026-05-29_v5_reproduction_recipe_for_ir.md) — IR port plan.** Step-by-step recipe to reproduce V5 on the IR side: detector pick, dataset inventory, source-quota rebalance (cap any source ≤ 30% effective drone-grad weight per §14's lesson), training recipe, eval harness adaptation (`--modality ir`), decision gates, production swap. No IR-V5 has been trained yet — this is the plan.
